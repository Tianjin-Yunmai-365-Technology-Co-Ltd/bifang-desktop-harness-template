# 2026-09-08 ADR

## ADR-20260908-008：普通调用 Session 在任务完成后尝试三段式标题并按真实身份复读

- 日期：2026-09-08；状态：Accepted。
- 关联需求：项目负责人要求普通任务执行后也尝试把当前 Session 命名为 `{task}-{id}-{feature}` 并完成验证。
- 变更标识与所需版本：`change_id = HARNESS-CHANGE-ORDINARY-TASK-SESSION-TITLE`；`required_version` 为下一次高于 `202609020957` 的 Harness 时间版本，由发布流程决定物化。本决定不自动改变 `Version.md` 或发布状态。
- 决定：没有为当前结果调用 `create_thread` 的普通调用 Task，在完成授权结果和本次必需检查后、最终回复前，至多一次尝试更新当前显示标题。`{task}-{id}-{feature}` 是既有 `{任务}-{ID}-{摘要}` 的英文占位别名：任务是稳定动作或类别，ID 优先复用不可变 Task key 或稳定 `change_id`，缺失时在首次重命名前一次性分配非空 ASCII key并在同结果重试中复用，摘要是单一完成结果且不是 Git feature ref；状态不入标题，宿主返回的 thread/client id 不反填标题。
- 执行与验证：目标已经匹配时只验证，否则调用 `set_thread_title` 至多一次并省略 `threadId`，只指向当前调用 Task；随后以调用上下文、响应或重命名前对账取得的真实 `threadId` 调用 `list_threads` 做一次有界复读，同时核对可用的 host/project 身份，并比较宿主规范化标题原文。标题始终只供人阅读，身份继续由真实 id 与项目绑定判定。
- 分层与失败边界：内部 Subagent、agent thread 和内部 Worktree 不自行重命名；已经按 ADR-20260908-003 的创建契约固定标题的 Git Worktree 左侧 Task 只验证、不重写。工具缺失、身份不可得、候选歧义、重命名失败或后台复读未确认时，最终报告“已请求但未验证”或“重命名失败”及原因，不创建替代 Task、不无限重试，也不推翻代码、文档或测试已经证明的完成结论。
- 影响、风险与验证：影响 Agent Policy、根路由、README、日常实施及初始化保留 Skills、Harness validator、回归和本次实际 Codex Task 标题。风险是标题后台更新延迟、同名候选或错误目标；通过省略目标 id 的 current-task 更新、真实 id 对账和有界复读降低风险。本次必须运行静态契约回归与 Harness 硬门禁，并在当前调用 Task 上实际重命名后由 `list_threads` 复读。
- 恢复或调整标准：若 Codex 宿主以后提供原子重命名并返回可直接核验的最终标题，可用新 ADR 收敛复读步骤；若宿主不允许标题更新，保留明确的非阻断失败报告，不得用创建新 Task 模拟成功。任何标题格式变化仍须同时更新普通调用与显式创建契约，避免出现两套含义冲突的三段式标题。

## ADR-20260908-007：候选证据只在忽略目录原子推进，tracked 发布记忆延后到真实渠道发布后

- 日期：2026-09-08；状态：Accepted。
- 关联需求：发布闭环最终一致性复核发现，候选构建、制品收集、E2E、完整验收和就绪复核若直接写 `docs/verification/`、Product Status 或版本周期状态，就会使要求固定 `sourceCommit` 的具名 `Release` 变脏；逐文件复制或逐 manifest 改状态还会暴露混合候选。
- 变更标识与所需版本：`change_id = HARNESS-FIX-ATOMIC-CANDIDATE-EVIDENCE-LIFECYCLE`；`required_version` 为下一次高于 `202609020957` 的 Harness 时间版本，由发布流程决定物化。本决定不自动改变 `Version.md` 或发布状态。
- 候选决定：构建、收集、候选 E2E、完整验收、`pending` → `accepted` 和就绪复核全过程都保持当前分支为 clean 的具名 `Release`，且 `HEAD`、本地/远端 `Release` 与所有 manifest `sourceCommit` 精确相同。全部候选文件、摘要、选择、签名/公证、人工签署、运行证据和状态只存在于根 `.gitignore` 精确忽略的 `release/` 原子集合、manifest 声明的相邻证据和最终回复，不写任何 tracked 项目记忆。
- 原子与复核决定：每个平台 producer 和 collector 都在仓库外的同文件系统 sibling 暂存完整普通文件集合，先验证 manifest、相邻摘要和精确集合，再以目录级原子替换提交；不得向活动 `release/` 逐文件复制。验收在准入时及写最终状态前两次验证完整发布双信封和仓库身份，E2E/冒烟后重新计算所有制品、资源和相邻证据最终字节；全部通过后才在 sibling 暂存完整 accepted 集合，并以一次目录替换原子更新全部 manifest。任何字节、HEAD、ref 或信封漂移都拒绝候选，不能留下 pending/accepted 混合状态。
- 就绪与发布后决定：就绪复核纯只读，只报告精确 `Release` ref/OID 和阻断项，不改 manifest 或 tracked 文件。只有真实渠道发布成功后，发布执行方才从该已发布 `Release` 开始后续受管 feature 生命周期，追加 Verification、正式发布和 Product Status 记录，并以精确已发布版本/40 位源码提交执行 `$desktop-manage-version finalize-release`；失败、取消、标签、上传尝试、pending/accepted 或 ready 都不能提前触发。
- 独立审计边界：回顾性人工复核或长期审计可以独立触发自己的受管 feature 生命周期并保留历史，但不得改写活动候选、覆盖既有失败，或反向把候选判为 `accepted`/`ready`。候选所需的人类签署仍是硬门禁，只是其候选期证据写入 `release/` 声明集合，真实发布后才转录进 tracked 历史。
- 取代范围：本决定只取代 ADR-20260805-002、ADR-20260805-004 及后续条目中“候选 E2E/完整验收/人工复核直接写 tracked Verification 或 Product Status”的子句；它们关于日常维护不制造记忆、真实证据不得改判、Verification 历史保留和五类按日记忆只保留最新快照的其余决定继续有效。tag、上传、商店/渠道发布和 `Release` 到默认分支仍不因本决定获得授权。
- 影响、风险与验证：影响发布/验证/工程/Rust/Agent Policy 事实源、候选 producer/collector/verify/ready Skills、跨平台 workflow、当前 Product Spec、Harness validator、专项回归和当日 Changelog。进程中断会留下 sibling 暂存目录，或渠道成功后 tracked 记录暂时位于后续 feature 叶子；通过唯一 sibling、目标非重解析点、精确文件集、目录原子替换、双次信封/字节复算和受管分支链降低风险。专项负例必须拒绝逐文件复制、manifest 前替换、缺少二次信封/最终字节复核、逐 manifest 状态更新、候选阶段 tracked 写入和写入型 ready。
- 恢复或调整标准：只有真实文件系统/渠道证明目录级原子替换不可用时，才可用新 ADR 设计同等失败关闭的事务日志与恢复协议；不得退回活动目录原地覆盖或允许候选修改其 own source commit。若未来提供正式渠道发布自动化，仍必须把发布成功作为 tracked 记录/finalize 的唯一前提，并单独取得标签、上传和渠道副作用授权。

## ADR-20260908-006：非必要语义审查改为每次发布显式选择，日常流程只保留硬门禁

- 日期：2026-09-08；状态：Accepted。
- 关联需求：项目负责人要求简化整个流程中的审查逻辑，把非必要审查延迟到发布流程中询问，并在完成后复核全链一致性。
- 变更标识与所需版本：`change_id = HARNESS-CHANGE-PER-RELEASE-SEMANTIC-REVIEW`；`required_version` 为下一次高于 `202609020957` 的 Harness 时间版本，由发布流程决定物化。本决定不自动改变 `Version.md` 或发布状态。
- 背景：默认 Harness 校验同时输出软行数候选和临时标记提示，日常实现、Task 整合、构建与验收规则又散落着含义不同的 review 表述，容易把机械安全门禁、可选语义审查和真正不可逆交付所需的人类签署混为一谈，并给普通开发增加无必要等待。
- 选择决定：每次显式发布在任何本地发布提交、测试或构建前解析 `reviewSelection: enabled | disabled`；当前请求已明确时复用，否则询问一次。同一发布的修复重跑复用原选择，新发布重新询问；选择不写入持久 Agent 策略。安全、隐私、不可逆操作、对外兼容契约或产品/渠道硬要求强制启用，普通偏好不能关闭。
- 执行决定：日常 `$desktop-implement-change`、Task/Worktree 整合和默认 `validate_harness.py` 只运行范围、秘密、clean、测试、祖先/OID、语法、硬行数等适用机械门禁。只有发布审查启用时，Harness 才显式运行 `validate_harness.py --release-review` 收集软行数候选与 `TODO`/`FIXME`/`HACK` 提示，并由 Agent 对冻结发布范围完成一次集中语义审查；`disabled` 记录 `Not run`、原因与剩余风险，不得伪装为通过。
- 证据决定：启用审查以已推送活动叶子的 `sourceHead` 为终点，显式形成状态、五项完成声明与公开证据摘要；最终构建 `sourceCommit = releaseHead`。prepare-release 把这些字段、冻结基线/源码终点/树差异 SHA-256 组成 `releaseReview`，并与性能/macOS 签名 `candidateSelections`、`lastClosedChain` 写入同一个 closing commit。helper 不产生语义结论，只校验显式字段、祖先关系，并逐提交拒绝 `sourceHead..preCloseHead` 对发布日志和被触发 Changelog 之外的路径改后恢复。关闭审查时证据/完成声明/`reviewedSourceCommit` 缺席，只保存 `Not run` 原因和风险。
- 候选入口与取代范围：所有正式发布候选都必须先经 `$desktop-prepare-release` 形成上述关闭提交；Rust/Tauri 构建在任何清理、测试或编译前及写 manifest 前运行只读 `verify-release-review`，只能从当前 clean `Release` 消费两份封存信封。进程中断后的同一发布复用该状态；缺失、过期、远端/本地收尾未完成或与当前硬要求冲突时只报告未就绪，构建 Skill 不得从对话制造 `passed` 或 `Not run`。E2E 仍在关闭链后的构建阶段单独解析。本决定取代 ADR-20260826-002 中 Rust/前端建议区间自动列出、通用文本日常职责复核与 checker 常态提示的部分，以及 ADR-20260806-003 中“所有人工维护文本超过 500 行必须在日常流程进入语义复核”的部分；各类型建议/硬阈值、职责判断标准、生成物排除和硬超限失败继续有效，但建议候选与职责语义判断只在当次发布审查启用时集中执行。
- 保留边界：提交范围/秘密、必要测试、clean、Git 线性和远端 OID、签名、公证、渠道硬要求及真正不可逆交付所需的人类签署永远不受该选择控制；E2E 与 GUI 性能继续各自按次选择，互不替代。
- 理由：把高成本且依赖发布累计范围的语义审查放到发布入口，可以缩短普通开发闭环；明确 `Not run` 与证据缺席又能让跳过风险保持可见，而不会削弱可机械证明的安全边界。
- 风险与验证：关闭审查会留下职责膨胀、临时标记或行为偏差未被集中发现的风险；manifest 必须公开原因与剩余风险，硬风险强制启用。专项回归必须覆盖默认无软提示、显式 flag 产生两类提示、选择重用/重问、硬要求、证据缺席与审查后提交边界；完整 Harness validator 锁定规则、Skills 与候选 schema 一致性。
- 恢复或调整标准：若真实发布证明某类语义审查必须长期强制，应把其精确风险或渠道要求声明为硬门禁；不得把全部软提示无差别恢复到日常流程。若增加持久默认值，必须另行批准其 schema、迁移和覆盖优先级。

## ADR-20260908-005：受管工具恢复必须使用官方最新兼容稳定版并形成用户级全局 PATH 闭环

- 日期：2026-09-08；状态：Accepted。
- 关联需求：项目负责人要求再次确认环境门禁；环境不满足时下载最新依赖，并且全局安装。
- 变更标识与所需版本：`change_id = HARNESS-CHANGE-USER-GLOBAL-DEVELOPMENT-ENVIRONMENT-RECOVERY`；`required_version` 为下一次高于 `202609020957` 的 Harness 时间版本，由发布流程决定物化。本决定不自动改变 `Version.md` 或发布状态。
- 背景：ADR-20260907-002 已允许安装缺失工具和升级明确低于门禁的稳定版，但只要求同一进程复探，未机械保证安装结果可由后续任务和新 shell 复用；脚本中的测试覆盖变量若在生产环境可用，还可能改变下载源、安装根或探测 PATH。
- 安装决定：触发范围保持不变；中性初始化在首次脚手架写入前主动门禁，已初始化下游只在真实测试/构建已经因受管环境失败后恢复并单次重试。需要写入时，Rust 使用官方当前 stable，Node.js 使用官方当前最新且落在项目兼容段的稳定版，pnpm 用官方 HTTPS registry 解析兼容范围内当前稳定版；Git 和 xwin 继续使用各自既有受管渠道。范围内现有稳定版不被无故替换，上界越界、预发布、不可解析或损坏状态仍失败关闭。
- 全局决定：Rust、Node.js/npm 与 pnpm 固定安装在当前用户受管全局根，不写项目目录、不注入 shim；Rust 使用确定的用户 `.cargo`/`.rustup` 并忽略继承的 `CARGO_HOME`/`RUSTUP_HOME`。Windows 去重后持久写入 User PATH；Unix 把真实工具链接到稳定的 `~/.local/share/agent-first-harness/bin`，受管 env 原子替换，profile 通过同目录随机临时文件、原字节快照比较后原子替换，检测到并发变化即停止。Git 与系统编译器仅在平台原生受信包管理器明确要求时允许系统级/管理员边界，权限需求必须显式可见且不得静默提权，无既有权限路线就停止。
- 复探与安全决定：任何下载/安装前，逐级确认将使用的安装根、profile/config/env/fish 都是普通非 symlink/reparse 路径，受管文件首行 marker 正确，稳定链接最终目标仅可位于同一受管根；冲突零下载、零安装失败。Node marker 必须绑定本次已校验官方归档的精确版本与摘要，既有目录复用及最终复探都要求 `node --version` 精确相等。PATH 删除空段、所有相对/cwd 段和重复项，POSIX 解析保持绝对字面 glob 不展开。写入后先在当前进程复探同一 stable minor 且达到 MSRV 的 `rustup`/`rustc`/`cargo`、Git、Node/npm 和适用 pnpm，再由全新 PowerShell 仅从持久 User/Machine PATH、或新登录 shell 从持久 profile/系统 PATH 绑定同一路径与版本实际执行；包括仅 Git 改变在内的任何变化都不能跳过，任何一步失败都阻断。Rust installer 要求官方 HTTPS、摘要与 `--no-modify-path`，pnpm 固定官方 registry 与 `--ignore-scripts`。下载源、安装根、探测 PATH 和持久化重定向仅在显式 `AFH_TEST_MODE=1` 时可覆盖。
- 只读边界：`--check-only` / `-CheckOnly` 始终零写入，只报告 `missing`、`upgrade-required` 或失败原因；不得触碰 User PATH、profile、注册表、下载源、项目清单或锁文件。
- 取代与保留范围：本决定补充 ADR-20260907-002 的“同一进程复探”和用户级安装语义；其环境触发范围、低于下界升级、范围内复用、上界/预发布/损坏拒绝及禁止兼容回退全部保留。它不授权新包管理器、静默管理员提权或真实构建前的例行预检；唯一系统级例外是 Git/系统编译器由既有平台原生受信管理器明确要求的可见权限边界。
- 理由：稳定用户级入口既满足后续任务全局复用，又避免管理员级系统污染；把“最新”约束为官方当前且兼容项目范围的稳定版，避免盲目安装不受支持的未来主版本。
- 风险与验证：profile/注册表写入、路径大小写、旧版本目录、shell 类型、symlink/reparse、字面 glob 与并发进程都可能导致越界写入或新会话不可发现。隔离测试必须覆盖受管根不受项目 Rust 环境变量影响、安装前路径/marker/链接冲突零副作用、随机临时文件与 profile 并发保护、Windows User PATH 与 Unix 稳定 bin/env/profile、新 shell 全工具路径和版本绑定、仅 Git 变化、空/所有相对/重复项与 POSIX glob、Node marker 摘要和精确所选版本，以及 Rust/Node/npm/pnpm 的版本形态；本轮不得为测试实际安装或升级用户工具。
- 恢复或调整标准：若某宿主无法安全持久化用户 PATH，应在写入前失败并报告精确人工恢复方式，不能退回项目内 shim 或仅当前会话通过。若官方分发方式改变，须先更新来源校验、兼容范围和隔离回归。

## ADR-20260908-004：左侧 Task 结果只通过受管条件快进整合到活动叶子并单独发布

- 日期：2026-09-08；状态：Accepted。
- 关联需求：项目负责人要求检查自动分支与条件合并逻辑是否闭环。
- 变更标识与所需版本：`change_id = HARNESS-FIX-FEATURE-TASK-INTEGRATION-CLOSURE`；`required_version` 为下一次高于 `202609020957` 的 Harness 时间版本，由发布流程决定物化。本决定不自动改变 `Version.md` 或发布状态。
- 背景：ADR-20260907-003 要求协调方把唯一产品写入 Task 以 fast-forward 整合回活动 feature 叶子，但 helper 只有 `start`、`publish` 和 `release`，没有一个受管命令机械证明 Task Worktree、冻结 OID、线性提交与远端叶子条件后再移动本地叶子；文字约定无法独立关闭该缝隙。
- 决定：`$desktop-manage-git-branch-chain` 新增 `integrate-task`。协调方只可在登记 active leaf 的独立 clean Worktree 中，显式传入匹配 `codex/task-<ascii-kebab-task-slug>` 的本地分支、同仓库登记且独占该分支的 clean Task Worktree，以及冻结的 40 位小写 Task HEAD。
- 条件快进：写 ref 前要求活动链存在且整条父/OID/远端快照有效，本地叶子等于已推送远端 OID，Task HEAD 是叶子的非空严格线性后代、无 merge，Task 范围内任一提交都未触碰受保护状态文件（后续恢复原字节也不例外），Task ref 不在远端；除目标 Task 外的 `codex/task-*`/`codex/unit-*` 写入 Worktree和链内 feature 占用都会阻断。通过后只运行 `git merge --ff-only <task-head>`，并复核两个 Worktree、状态、链内 refs、远端叶子和默认分支均未漂移。
- 发布与清理：成功返回 `published: false` 和 `nextCommand: publish`；命令不 push、不删 Task Worktree/ref、不改状态 JSON。协调方必须随后独立运行既有 `publish`，复读远端叶子等于新 OID后才允许清理 Task 或从该 OID 创建下一写入 Task。任何前置失败保持叶子不变；后置竞态失败不得谎报发布完成。
- 保护范围：`integrate-task` 不能操作 `main`、`master`、动态默认分支或 `Release`，不能 rebase、cherry-pick、自动解冲突、强推或引入 merge commit；`release` 原子事务和用户自行完成的 `Release`→默认分支 Merge/PR 边界不变。
- 理由：把 Task 交付到叶子的唯一状态转换写成条件快进命令，能用 Git OID 与 Worktree registry 机械证明“唯一写入 Task → 本地叶子 → 远端叶子”的每一跳，同时让 push 失败保持可重试而不混淆本地整合状态。
- 风险与验证：Task ref/Worktree 在检查后竞态变化可能让命令失败；保守后置复核会阻断并要求人工检查，不能回滚未知并发写入。裸远端回归必须覆盖成功但未 push、随后 publish、dirty/错误分支、活动叶子未完全推送、OID/祖先/merge/状态漂移、Task 远端 ref、重复写入 Worktree与默认分支不变。
- 恢复或调整标准：若未来允许多个产品写入 Task 并行，必须用新 ADR 定义确定性 DAG、冲突解决和发布线性化；在此之前不得用普通 merge 或手工 ref 更新绕过 `integrate-task`。

## ADR-20260908-003：Git Worktree 左侧 Task 使用派发前稳定 key 形成固定显示标题

- 日期：2026-09-08；状态：Accepted。
- 关联需求：项目负责人要求 Worktree + Task 会话按 `{任务}-{ID}-{摘要}` 命名。
- 变更标识与所需版本：`change_id = HARNESS-CHANGE-WORKTREE-TASK-TITLE-CONTRACT`；`required_version` 为下一次高于 `202609020957` 的 Harness 时间版本，由发布流程决定物化。本决定不自动改变 `Version.md` 或发布状态。
- 背景：既有“动作 + 单一结果 + 可选编号”没有固定字段边界，也不能在 `create_thread` 返回前得到 `threadId`；若用返回 ID 反填标题会产生二次命名和 queued Task 漂移，若把显示标题直接作为 Git slug 又会把中文与状态带入 ref。
- 决定：每个 Git Worktree 左侧 Task 在派发前生成并写入描述的不可变 Task key；显式调用 `create_thread(title="{任务}-{ID}-{摘要}")`，三个部分均非空，`ID` 只使用该 key。调用后才返回的 `threadId`/`clientThreadId` 和 Ready/Active/Blocked 等状态不得进入或反填标题；后续身份仍由真实 id 与 `projectId` 判断，并原样展示列表工具返回的规范化标题。
- 技术标识边界：显示标题与独立 ASCII `task-slug` 是两个事实；只有后者用于 `codex/task-<task-slug>`、内部 `codex/unit-*` 以及 helper 参数。内部 Subagent/agent thread 不创建用户左侧 Task，也不得冒充满足显示标题契约。非 Git Local Task 继续使用简短“动作 + 单一结果”。
- 取代范围：本决定取代 ADR-20260902-002 中 Git Worktree 左侧 Task 标题使用“动作 + 单一结果 + 可选编号”的子句，以及 ADR-20260828-001 中同类 Task 标题使用“动作 + 结果”的子句；两项旧决定的单一结果边界、描述必填内容、项目绑定、独立 Worktree、可审查提交和干净交付要求继续有效。非 Git Local Task 的简短标题不在取代范围内。
- 影响范围：Agent Policy、README、实例化/初始化/日常实施/并行 Skills、Product Spec、Harness validator 和工作流回归；不改变 Task 项目绑定、`SETUP_PENDING` 有界返回、Worktree common-dir/registry 或单结果交付边界。
- 理由：派发前稳定 key 使显示名称在 Ready 与 queued 两条路径上确定且可引用；将人类可读标题与安全 Git slug 分离，避免命名规则反向污染 refs 或身份验证。
- 风险与验证：协调方可能漏传 title、复用 key 或把返回 id/状态写入标题；静态契约与回归必须覆盖所有可创建入口、禁止旧模板和显示标题当 ref。真正唯一性仍由宿主 id 提供，标题不承担安全身份。
- 恢复或调整标准：若 Codex 宿主提供原生不可变用户可见 Task key，可明确映射到 `ID`；不得静默改用调用后返回的临时 ID，任何格式变化必须同步所有创建入口和描述模板。

## ADR-20260908-002：macOS 通知开关按真实授权状态恢复并定位当前应用系统设置

- 日期：2026-09-08；状态：Accepted。
- 关联需求：项目负责人要求启用系统通知的 macOS 下游在用户打开应用通知开关时检查权限，无权限就打开系统设置的通知窗口。
- 变更标识与所需版本：`change_id = HARNESS-CHANGE-MACOS-NOTIFICATION-PERMISSION-RECOVERY`；`required_version` 为下一次高于 `202609020957` 的 Harness 时间版本，由发布流程决定物化。本决定不自动改变 `Version.md` 或发布状态。
- 背景：既有通知 Skill 只要求“先请求权限后持久化”，没有区分系统已经拒绝、尚未询问、临时/临界授权等状态；反复调用授权请求不会帮助已拒绝用户，打开通用通知面板又可能无法定位当前应用，接受 WebView URL/bundle id 则扩大导航与注入边界。
- 决定：macOS 从 `false` 打开应用通知开关时，在生命周期拥有的串行 worker 内先调用 `get_notification_settings().await`。`Authorized`、`Provisional`、`Ephemeral` 可继续；仅 `NotDetermined` 调用 `request_auth().await` 并复读；`Denied`、归一化的 `Restricted`/`Unknown` 或请求后仍无权都保持应用偏好 `false`。
- 设置恢复：无权限分支调用 Rust-only opener；它只接收内部 `AppHandle`，从 `app.config().identifier` 获取当前 bundle identifier，并附加到固定 `x-apple.systempreferences:com.apple.Notifications-Settings.extension?id=` 系统入口。WebView 不得传 URL 或 identifier，不得使用 shell 字符串；打开失败返回独立稳定可见错误。启动时和关闭开关时都不主动查询、弹框或打开设置，关闭也不伪称撤销 OS 权限。
- 候选前提：macOS 通知的最终安装候选必须满足该库的 bundle code-signing 前提；若发布签名选择仍是默认 `disabled/not-requested`，构建按 ADR-20260908-001 在测试/bundle 前失败关闭并要求用户下一轮主动启用签名，不能自动补签或以 E2E 关闭绕过。
- 取代范围：本决定取代 ADR-20260831-005 中“初始化调试 E2E 对启用通知执行真实授权/投递检查”的子句。当前初始化 E2E 只验证默认关闭设置、结构和失败可见性，不触发真实权限提示或投递；实际权限状态、设置跳转与投递只在满足签名前提的最终安装候选中验证。该旧决定的显式能力选择、串行 worker、授权先于持久化、默认关闭、禁用无残留和自启状态恢复继续有效。
- 影响范围：系统通知与 GUI 初始化 E2E Skills、生命周期静态检查器/fixture、工程/Rust/验证事实和 Product Spec；Windows/Linux 官方 Tauri 通知路线、默认关闭、中性无产品触发点和禁用无残留边界不变。
- 理由：按真实状态只询问一次并在拒绝后直达当前应用设置，是可恢复且不骚扰用户的最短路径；把 bundle id 留在可信 Rust 配置内避免把任意系统 URL 导航能力暴露给 WebView。
- 风险与验证：Apple 设置 URL 属于平台入口，未来系统版本可能变化或 opener 失败；必须让失败可见并保持偏好关闭，不能谎报启用。Node 契约回归必须覆盖全部授权状态、只请求 `NotDetermined`、复读、固定前缀、内部 identifier、无 WebView 参数和 opener 失败；真实 macOS 初始化 E2E 仍需观察系统设置窗口定位。
- 恢复或调整标准：若 macOS 官方提供更稳定的当前应用通知设置 API，应在保留同等输入隔离与状态机后替换固定入口；不得退回重复权限请求、静默失败或任意 URL opener。

## ADR-20260908-001：macOS 候选先解析签名意图，默认 unsigned 且零可用性探测

- 日期：2026-09-08；状态：Accepted。
- 关联需求：项目负责人要求优化 Mac 发布路径：签名和公证默认不必须，只有用户设置过或主动要求时才执行。
- 变更标识与所需版本：`change_id = HARNESS-CHANGE-MACOS-SIGNING-INTENT-FIRST`；`required_version` 为下一次高于 `202609020957` 的 Harness 时间版本，由发布流程决定物化。本决定不自动改变 `Version.md` 或发布状态。
- 背景：既有路径以宿主是否存在 Developer ID、凭据和 `notarytool` 条件决定签名尝试，可能在用户只需要普通候选时探测 Keychain/环境并增加耗时，也会把“机器可用”误当成“用户有意签名”。
- 意图决定：macOS 候选在任何发布提交、测试、可用性探测或 bundle 前先解析并锁定 `macosSigningSelection: enabled | disabled` 与唯一来源。只有渠道硬要求、本次用户主动要求或项目已有批准的持久签名公证配置时启用；其他情况固定 `disabled/not-requested`。多个来源按 `channel-required > requested > configured > not-requested` 归一化。本机恰好存在身份、工具、环境变量或 Keychain profile 只表示潜在可用性，绝不能改变选择；选择、来源及关闭原因/风险进入当前 closing commit 的 `candidateSelections`，构建只读消费。
- 关闭路径：默认及明确禁用直接使用 `--no-sign`，记录 `unsigned`、`notarizationStatus: not-run`、非空原因与剩余分发风险；不得运行签名/公证 probe，`notarizationEvidence` 和任何由身份、凭据、profile 探测派生的证据必须缺席。关闭不是失败，也不能冒充已验证分发。
- 启用路径：只有启用后才检查 Apple 宿主、Developer ID Application、完整且单一的环境凭据或已授权 Keychain profile、`notarytool` 和 `stapler`。签名、公证、stapling 与验证仍全有或全无；任何条件缺失或执行失败都阻断，绝不得以 `--no-sign` 重试或静默降级，也不得输出“仅签名”中间候选。
- 保留边界：Tauri updater archive/`.sig` 的更新制品签名是独立安全合同，不由安装包签名选择关闭；本决定不授权创建/索取凭据、修改 Keychain、接受 Apple 许可、上传、商店提交或正式发布。
- 通知运行前提：`mac-usernotifications` 要求应用 bundle 至少经过代码签名，而本发布合同不在 `disabled/not-requested` 分支暗中增加 ad-hoc 或第四种签名来源。因此 `system_notification = enabled` 与 unsigned 是不可验收组合，构建必须在测试/bundle 前停止并提示用户下一轮主动启用签名公证，或先经产品变更关闭通知；E2E `disabled` 不能绕过。此失败关闭不改变“默认不探测、不自行签名”的决定。
- 理由：意图先行把隐私敏感探测和高成本公证限制在确实需要的发布中，同时保留启用后完整的分发真实性门禁；来源优先级让持久配置、本次请求和渠道要求不会产生歧义。
- 风险与验证：unsigned DMG 可能触发 Gatekeeper 警告或不满足渠道规则，因此 manifest 与最终报告必须公开风险，渠道硬要求强制启用。专项回归必须拒绝默认探测、启用后 unsigned fallback、来源歧义和关闭路径残留证据，并锁定构建/收集/验收/发布准备一致性。
- 恢复或调整标准：若产品或渠道决定所有 macOS 分发都必须签名，应把该要求声明为持久批准配置或渠道硬门禁；不得通过探测到本机身份自动恢复隐式签名。

## ADR-20260907-003：产品变更使用串行 feature 链，发布原子汇入 Release，默认分支只由用户晋升

- 日期：2026-09-07；状态：Accepted。
- 关联需求：项目负责人要求新需求与 Bug 修复自动创建带摘要和日期的 feature 分支、记录父子链路并把全部变更推送远端；程序发布时把整链合并到 Release、删除本地和远端整链，禁止 Agent 直接操作主分支，并把 Release 到主分支的 Merge/PR 留给用户。
- 变更标识与所需版本：`change_id = HARNESS-FEAT-GIT-FEATURE-BRANCH-CHAIN-LIFECYCLE`；`required_version` 为下一次高于 `202609020957` 的 Harness 时间版本，由发布流程决定物化。本决定不自动改变 `Version.md` 或发布状态。
- 背景：既有规则只为 Codex 左侧 Task 和内部 Subagent 约定临时 `codex/task-*`/`codex/unit-*` 分支，日常实现仍可落在任意当前分支，明确发布只形成本地提交且禁止 push。它无法证明多个需求的先后父子关系、远端是否包含全部闭环，也不能在发布时安全区分应清理的产品分支与临时 Task/链外分支。直接让 Agent合并主分支又会越过项目负责人要求的人工作业边界。
- 命名与串行决定：完成初始化且已有可访问远端的下游，在每个新需求、Bug 或会写入仓库的维护工作开始前，使用 `$desktop-manage-git-branch-chain` 创建 `feature-{ascii-kebab-summary}-{YYYYMMDD}`；日期固定取 `Asia/Shanghai`，Bug 同样使用 `feature-`。受管命令要求稳定版 Git `>=2.36.0`，以覆盖 `git worktree list --porcelain -z`，现有更旧稳定版继续由环境门禁的受管升级路线处理。首条链优先从已推送 `Release` 开始，尚无 `Release` 时才以当时远端默认分支的冻结 OID 为基线；活动链中的新节点只从已推送叶子创建，同一需求的诊断、实现、相关测试/review 与同范围修正复用叶子。空摘要、命名冲突、dirty 状态、远端歧义或 Worktree 占用均失败关闭，不猜测后缀。
- 状态与推送决定：受保护 `.harness/git-branch-chain.json` 的活动状态保存 schema、登记远端、冻结 default/基线 OID、顺序节点、父 ref/OID 与活动叶子；关闭状态保存 `releaseHeadBefore`、每个节点关闭前 OID、`releaseReview` 和 `candidateSelections`。后两者与关闭状态原子进入同一提交；缺少它们的旧关闭提交只可在远端事务已经完成时做本地收尾，不能新推进 `Release`。关闭提交的 OID 无法自引用进自身 tree，故 `closingHead` 固定为 `null`，运行时由当前提交且其唯一直接父等于关闭前叶子严格推导；实际远端 OID 每次通过实时查询复读，不伪造为持久字段。`start` 先提交状态再创建并复读远端节点；每个日常逻辑闭环按提交规范形成提交后，只用显式完整 refspec 非强制快进活动叶子并复读 40 位 OID。远端优先精确 `origin`，否则只接受唯一远端；Agent 不配置远端或凭据，不强推、rebase、cherry-pick、打标签或上传。
- 保护分支决定：精确 `main`、`master`、动态远端默认分支与 `Release` 均只允许只读解析或作为合格基线，日常实现和普通 Git 命令不允许在其上写文件、暂存、提交、合并、推送或删除；下一条受管发布原子事务是推进 `Release` 的唯一例外。初始化仍有一次性、pre-remote 的本地 `main` 中性基线例外；远端配置后不存在日常主分支例外。
- 发布与清理决定：明确程序发布先把叶子源码与发布元数据提交全部推送。只有状态节点形成从冻结基线到叶子的严格父子线性历史、父 OID 未漂移、全部本地/远端 OID 相同、工作树干净且无其他 Worktree 占用时，才在叶子形成关闭状态提交，并用一次 atomic push 将精确 `Release` 快进到该提交，同时以每个登记远端 feature ref 的旧 OID 作为 lease 删除整链。禁止 wildcard、无精确期望 OID 的 force、非快进和链外删除；唯一 force 形式是为精确删除每个登记 ref 使用的 `--force-with-lease=<ref>:<old-oid>`。atomic push 失败保持所有 refs，远端成功后复读确认默认分支未变化，再删除精确本地 refs。只在本地收尾中断时按关闭状态幂等重试。
- Task 分层与取代范围：`codex/task-*`/`codex/unit-*` 保持临时实施分支，不写入产品链状态、不自行推送。严格线性发布与从同一叶子并行产生 sibling 提交不兼容，因此一条 active feature 叶子同一时刻最多一个产品写入 Task，且该 Task 不创建 sibling 写入单元；只读 Task/Subagent 可并行。协调方读取报告并只完成范围、秘密、任务测试、clean、OID/祖先、状态与占用等机械安全门禁后，以 fast-forward 把唯一写入 Task 整合回叶子并发布；非必要语义审查按 ADR-20260908-006 延迟到发布，后续写入 Task 只能从新远端 OID 创建。此决定取代 ADR-20260902-002 和 ADR-20260828-001 中“产品写入 Task 默认从默认/main 分支开始、协调方整合到默认分支”、普遍禁止 push，以及活动产品链仍可使用 sibling 写入 Task/单元的部分；精确项目绑定、独立 Worktree、非重叠所有权、可审查提交、干净交付和整合后清理在非活动链适用场景继续有效。
- 人工晋升边界：Agent 不执行、不代建自动化，也不创建 `Release` 到 `main`、`master` 或动态默认分支的 merge、push、删除或 PR。分支链关闭后只报告远端、`Release` ref 和 40 位提交，由用户自行 Merge/PR；该动作与真实渠道发布、标签、上传和版本周期重置仍是不同事件。
- 理由：一条只追加的显式父子链让需求先后、提交远端性和发布清理集合都可由 OID 机械证明；atomic update/delete 避免 `Release` 已前进但远端链只删一半；逐 ref lease 和默认分支冻结能阻止竞态误删。把默认分支晋升留给用户，保留最终审阅与仓库平台保护规则的控制权。
- 风险与验证：远端不支持 atomic push、远端策略禁止删除、身份/认证不可用、并行写入、命名碰撞或其他 Worktree 占用都会阻断自动闭环；这是保守失败而不是降级为部分删除或直接写主分支。标准库 helper 的裸远端回归必须覆盖首链/续链、Bug 命名、状态提交与逐次 push、保护分支拒绝、父/OID 漂移、非线性历史、remote 原子失败零删除、成功清链、默认分支不变和本地收尾幂等重试；release Git helper、初始化/升级 validator 与完整 Harness 回归同步锁定集成契约。Harness 源没有终端产品，因此本轮不构建发布候选或执行真实发布。
- 恢复或调整标准：若目标托管平台经真实证据不支持 atomic ref update，可新增经项目负责人批准、具有等价回滚与竞态证明的平台专用事务路线；不得静默降级为多次无回滚 push。若需要并行产品分支或不同命名，应以新 ADR 明确 DAG、冲突解决、清理集合和迁移；在此之前保持单链。默认分支名称变化可由动态解析自然适配，但任何让 Agent 执行 `Release` 晋升的变化都必须由项目负责人另行明确批准。

## ADR-20260907-002：受管开发环境低于最低门禁时自动升级且禁止兼容回退

- 日期：2026-09-07；状态：Accepted。
- 关联需求：项目负责人要求开发环境未达到最低门禁时直接升级当前系统环境，不得用兼容方案绕过。
- 变更标识与所需版本：`change_id = HARNESS-CHANGE-DEVELOPMENT-ENVIRONMENT-AUTO-UPGRADE`；`required_version` 为下一次高于 `202609020957` 的 Harness 时间版本，由发布流程决定物化。本决定不自动改变 `Version.md` 或发布状态。
- 背景：既有门禁已经能安装缺失工具、复用范围内稳定版并拒绝不兼容环境，但对“已安装且明确低于最低下界”的工具统一失败关闭。这会让可由既有受管渠道安全恢复的旧 Git、Rust、Node.js、pnpm 或 cargo-xwin 停在人工阻断，也可能诱使维护者降低项目门禁、回退依赖或寻找替代工具链来迁就宿主。
- 决定：保持既有触发边界；中性初始化仍在首次脚手架写入前主动执行一次，初始化完成后仍只在真实测试/构建命令已出现受管环境错误时针对性恢复并单次重试。写入模式对缺失适用工具执行安装，对可证明低于最低下界的可解析稳定工具按当前宿主的既有受管路线自动升级，对范围内稳定版原样复用。Node.js 25.x 按低于下一段允许下界 26.0.0 处理；`cargo-xwin <0.23.1` 升级，`>=0.24.0` 因越过显式上界继续阻断。
- 失败关闭与只读决定：存在显式上界时高于上界的版本、任何预发布版本、无法解析的输出或损坏安装都不属于“可证明低于下界”，必须失败关闭。`--check-only` / `-CheckOnly` 始终零写入，把可升级旧版本报告为 `upgrade-required`；实际写入成功分别报告 `installed` 或 `upgraded`，并以同一进程复探后才允许通过。
- 禁止兼容回退：门禁只能修复当前宿主环境，不得降低项目最低门禁、回退依赖或锁文件、注入 shim、改用更旧工具，或寻找替代工具链来适配旧环境。Git 继续只使用既有宿主包管理器，Rust/Node.js/pnpm/cargo-xwin 继续使用已验证官方来源；不安装新的包管理器，不削弱校验和、签名、提权、接口条件或单次重试边界。
- 取代与影响范围：本决定只取代 ADR-20260831-007 和 ADR-20260824-003 中“已有低于最低下界工具只阻断且不自动替换”的环境处置部分；它们关于兼容范围、依赖候选验证、锁文件、范围内复用、初始化/真实错误触发和单次重试的其余决定继续有效。ADR-20260820-001 对损坏 Homebrew formula 的失败关闭也不变。影响范围包括最新 Product Spec、README、工程/Rust/验证事实、环境与初始化 Skills、环境门禁脚本/回归、Harness validator、技术债和当日 Changelog；不创建 Verification 证据卷。
- 理由：最低门禁已经表达工程能够被验证的最小环境；升级宿主比改写项目去适配旧工具更直接，也能让初始化和真实错误恢复自动闭环。把“低于下界”与“高于上界/预发布/不可解析/损坏”分开，保留了可证明安全的自动恢复，同时避免把任意异常安装当成可覆盖对象。
- 风险与验证：受管升级可能受权限、PATH 缓存、正在使用的可执行文件、包管理器仓库或网络影响；Node.js 分段范围和 cargo-xwin 上界若分类错误，还可能误升级本应阻断的版本。隔离测试必须覆盖 Git/Rust/Node.js/pnpm 与 xwin 的缺失安装、低于下界升级、范围内复用、check-only 零写入/`upgrade-required`，以及上界、预发布、无法解析和损坏拒绝；Shell/PowerShell 实现、Harness validator 与 Skill 结构检查共同锁定契约。真实三平台升级仍按 LIM-009 标记为待前向验证。
- 恢复或调整标准：若真实宿主证明某个受管升级路线不具备原子性或可恢复性，可让该具体平台在写入前报告阻断并保留升级需求；不得通过降低门禁、回退项目依赖、shim 或替代工具链恢复。若官方支持范围改变，先以真实兼容证据更新最低/最高范围和回归，再调整分类；任何扩大自动覆盖到上界以上、预发布、不可解析或损坏安装的方案都需要新的安全决定。

## ADR-20260907-001：GUI Release 性能允许上限统一放宽 20% 并启用 v2 阈值档案

- 日期：2026-09-07；状态：Accepted。
- 关联需求：项目负责人明确要求放开性能验收标准，各项放宽 20%。
- 变更标识与所需版本：`change_id = HARNESS-CHANGE-GUI-RELEASE-PERFORMANCE-BUDGET-V2`；`required_version` 为下一次高于 `202609020957` 的 Harness 时间版本，由发布流程决定物化。本决定不自动改变 `Version.md` 或发布状态。
- 背景：ADR-20260831-006 建立的 `gui-release-v1` 为冷启动、交互、Long Task、CPU、RSS 和循环后 RSS 增长设置了固定允许上限。该档案已进入性能 helper、证据、构建、收集和验收契约；直接原地改写 v1 会让历史证据与新判定使用同一名称却表达不同语义。
- 决定：所有用于判定“不得超过”或“必须低于”的性能允许上限统一乘以 1.2：冷启动中位数 `2000 → 2400 ms`、最大值 `3000 → 3600 ms`；交互 p95 `100 → 120 ms`、单次严格上限 `200 → 240 ms`；Long Task 严格上限 `200 → 240 ms`；空闲 CPU p95 `5% → 6%`、隐藏/托盘 CPU p95 `2% → 2.4%`；稳态 RSS `300 → 360 MiB`、峰值 RSS `500 → 600 MiB`；循环增长比例 `15% → 18%`、最小允许增量 `32 → 38.4 MiB`。
- 测量边界：至少 1 次预热、恰好 5 次冷启动、至少 20 次交互、至少 30 秒且至少 5 个 CPU 样本、至少 20 轮导航/交互，以及 50 ms Long Task 记录下限继续保持。这些值决定证据可信度或浏览器观测口径，不是“允许更慢或占用更多”的性能上限，因此不随本次 20% 放宽而减少或提高。
- 证据兼容：新判定固定使用 `performanceThresholdProfile: gui-release-v2`，helper 输出同名 `thresholdProfile` 并内嵌全部 v2 数值。既有 `gui-release-v1` 证据继续按原阈值解释，不得只改 profile 名称、复用旧观测或把历史 `failed` 重判为 `passed`；新候选必须从对应 clean HEAD 的原生 Release 探针重新测量。
- 取代范围：本决定只取代 ADR-20260831-006 的固定预算数值及“永久放宽只能走单候选 waiver”的部分；真实 release-profile no-bundle 探针、整进程树、window-state 隔离与恢复、失败回流、不可豁免完整性阻断、显式 waiver 和 runtime 字节绑定继续有效。ADR-20260901-002 的逐次性能选择也不变。
- 影响范围：最新 Product Spec、README、`docs/RELEASE.md`、GUI 性能/构建/制品收集 Skills、性能证据契约与 helper、Harness validator、专项回归和当日 Changelog。CLI/TUI/MCP、GUI 初始化 E2E、性能选择方式、样本方法和 `Version.md` 不变。
- 理由：精确比例调整直接落实项目负责人的新容差，同时用 v2 档案隔离旧证据，避免同名阈值漂移。保留采样方法与完整性门禁，确保标准变宽不会削弱测量可信度或允许不完整证据通过。
- 风险与验证：更宽松的预算会允许启动、交互和资源占用比此前高 20%，可能降低对低配置设备回归的敏感度。专项测试必须覆盖 v2 边界通过、刚越界失败、独占上限、RSS 增长、profile 输出和 v1 字符串清除；完整 Harness Python 回归与 validator 负责证明所有当前契约同步，真实目标性能仍只由下游原生候选测量。
- 恢复或调整标准：若跨平台真实数据或渠道要求证明 v2 过宽，可用新 ADR 与新 profile 收紧；若继续调整，也必须显式版本化 profile、同步 helper/证据/构建/验收契约，并保留历史证据原语义。不得原地重定义已使用的 profile，也不得降低整进程树、恢复、样本或失败关闭要求来制造通过。

## ADR-20260902-002：左侧 Task 绑定保存项目，setup 排队态有界返回，内部并行使用独立单元命名空间

- 日期：2026-09-02；状态：Accepted。
- 关联需求：项目负责人反馈多 Task + Worktree/Subagent 工作流会停在只返回 `clientThreadId` 的 setup 状态，且左侧 Task 可能建立为 projectless 或落在实际项目之外；同时要求复核一套把阶段变化强制拆 Task、禁止协调 Task 实施、要求标题携带状态的全局提示词。
- 变更标识与所需版本：`change_id = HARNESS-FIX-PROJECT-BOUND-TASK-AND-SUBAGENT-WORKTREE`；`required_version` 为下一次高于 `202609020957` 的 Harness 时间版本，由发布流程决定物化。本决定不自动改变 `Version.md` 或发布状态。
- 事实与根因：本次修复检查到当前 Codex Task 的 cwd 指向目标仓库但 `projectId` 为空，证明 cwd 恰好正确不能替代保存项目绑定。Codex 创建 Worktree Task 可能先只返回不可传给 thread 工具的 `clientThreadId`；原规则又同时要求真实 `threadId`、禁止重复创建并禁止当前 Task 实施，因此没有可用的晋升查询桥时会形成无限等待。官方文档说明 Projects 用于组织相关 chats，Subagents 是主 agent 管理的内部 agent threads，Worktrees 通常位于 `$CODEX_HOME/worktrees` 等保存项目目录之外但仍共享同一 Git repository（[Projects and chats](https://learn.chatgpt.com/docs/projects)、[Subagents](https://learn.chatgpt.com/docs/agent-configuration/subagents)、[Git worktrees](https://learn.chatgpt.com/docs/environments/git-worktrees)，核对日期：2026-09-02）。
- Task 粒度决定：左侧 Task 只指用户可独立进入的 user-owned Task/thread；plan、Todo、review、Subagent、agent thread 和内部单元 Worktree 都属于当前 Task。一个 Task 以单一可验收结果而非生命周期阶段划界；同一结果的诊断、实现、相关测试/review 和同范围缺陷修复不自动拆分。只有用户明确要求新建左侧 Task，或已明确把当前 Task 设为多 Task 协调器并指定独立结果时才创建；普通单结果请求不强制先建 Task0。原“动作 + 单一结果 + 可选编号”标题子句已由 ADR-20260908-003 取代；状态不入标题的边界继续有效。
- 项目绑定决定：创建前以 `list_projects` 按规范化完整路径精确选择保存项目，并把真实 `projectId` 作为 `create_thread` 的项目目标；Git 项目只使用该项目的 Worktree 环境，非 Git 项目使用该项目的 Local 环境，禁止 projectless、默认兜底或其他项目。进入 Ready Task 后再次核对 `projectId`；Git Worktree 的路径可以位于保存项目之外，但其规范化 Git common-dir 必须与保存项目相同，并且必须出现在保存项目的 `git worktree list --porcelain` 中。非 Git Local Task 才要求 cwd 精确等于保存项目路径。
- setup 状态决定：一个独立结果只派发一次。返回 `threadId` 才进入 Ready；只返回 `clientThreadId` 时记为 `SETUP_PENDING`，立即交付 queued UI 引用并有界结束，不把该 ID 传给 thread 工具，不无限轮询、不重复创建，也不在当前 Task 或后台目录替代实施。只有已得到真实 `threadId`，或用户后来明确要求检查 queued Task 时才用 `list_threads` 对账；唯一候选未出现继续保持 pending，候选不唯一报告 ambiguous，只有工具明确失败才记为 `SETUP_FAILED`。
- 基线与分支决定：用户指定起始 branch/ref 时使用该事实，否则使用保存项目默认分支的已提交 HEAD；不硬编码 `main`/`master`，不自动 fetch/pull，也不隐式复制未批准的 working-tree 修改。左侧 Task 使用 `codex/task-<task-slug>`；当前 Task 内部并行使用 `codex/unit-<task>-<unit>`，避免 Git ref 的父子命名冲突。
- 内部并行决定：`$desktop-run-parallel-worktrees` 只拆当前左侧 Task 的内部写入单元，不调用 `create_thread`。helper 必须同时接收保存项目根和当前 Task source worktree，证明二者属于同一 common-dir且 source 已登记，再从 source HEAD 创建单元。每个单元登记非空、规范化、repo-relative 且互不重叠的所有权；guard 只接受登记范围，postflight 检查 committed、uncommitted 和 untracked 实际路径，remove 固定以创建时记录的 source 分支判断整合，不能用单元自己的分支自证通过。外部容器或路径组件是符号链接时失败关闭。
- 取代范围：本决定取代 ADR-20260828-001 中硬编码最新本地 `main`、`codex/<task-slug>`/`codex/<task>/<unit>` 父子分支、主任务自动为每个结果建 Task，以及只按路径祖先关系判断 Worktree 的部分；其单一结果、隔离修改、逻辑提交、干净交付、无未授权副作用和整合后清理原则继续有效。
- 风险与剩余限制：仓库规则不能让已存在的 projectless Task 改挂项目，也不能修复 Codex 宿主永不把 `clientThreadId` 晋升为 `threadId` 的后台故障；它只能阻止新任务错误绑定、无限等待、重复孤儿 Task 和越界内部单元。绕过 helper 的宿主写入仍不能由仓库脚本强制阻断，必须继续在宿主/沙箱层解决。
- 验证：Harness validator 和 Python 回归必须锁定 Task/内部 Subagent 分层、精确 `projectId`、`SETUP_PENDING` 有界返回、common-dir/Worktree 登记、默认分支语义、`codex/task-*`/`codex/unit-*` 命名、所有权注册、postflight 和保守 remove；Skill Creator 校验受影响 Skills。Harness 源没有可运行的下游产品，本决定不自动增加构建、GUI/Computer Use、E2E 或发布。
- 恢复或调整标准：若 Codex 后续提供稳定的 `clientThreadId → threadId` 查询/取消接口，可用新 ADR 增加有界对账；若项目工具直接保证不可伪造的 repository identity，也可替换 common-dir/登记复核。任何调整仍必须保持一次创建、精确项目绑定、内部 agent 不冒充左侧 Task、越界失败关闭和无隐式默认分支。

## ADR-20260902-001：所有 GUI 固定安装 dialog，并把 WebView 权限收敛到官方默认对话框

- 日期：2026-09-02；状态：Accepted。
- 关联需求：项目负责人要求为所有 GUI 增加固定 dialog WebView 基线，精确锁定 Rust/GUI/前端依赖、Builder 顺序与主窗口默认权限，同时明确禁止 wildcard、废弃 ask/confirm alias、deny 项和任何 filesystem 授权。
- 变更标识与所需版本：`change_id = HARNESS-FEAT-GUI-DIALOG-DEFAULT-PERMISSIONS`；`required_version = 202608281139`。本决定不自动改变 `Version.md`。
- 背景：ADR-20260901-003 已把 `os`（system-locale）、updater、window-state 固定为不进入 profile 的 Rust-only 基线，但尚未给需要从 WebView 调用原生 message/save/open 的 GUI 建立统一 dialog 依赖与 ACL。让下游自行选择 permission 容易出现 wildcard、废弃 alias、过宽 filesystem 权限或把插件 API 错误接入 Rust command handler；把 dialog 误称为第四项 Rust-only 基线又会掩盖它确实向 WebView 开放 ACL 的事实。
- 固定依赖与注册决定：所有 GUI 无条件安装官方 dialog 插件，不增加初始化问询或 profile 字段。根 `[workspace.dependencies]` 精确声明 `tauri-plugin-dialog = "2.7.3"`，GUI member 精确声明 `tauri-plugin-dialog = { workspace = true }`，前端精确声明 `@tauri-apps/plugin-dialog = "^2.7.3"`。GUI adapter 的稳定 Builder 顺序固定为 `single-instance → deep-link → os → updater → window-state → dialog → notification → autostart → global-shortcut`；条件插件缺席时保持其余插件的相对顺序。dialog 使用官方 plugin API，不增加 Rust command，也不进入唯一 `invoke_handler`。
- WebView 安全边界：只有主窗口 capability 对 dialog 的授权精确为 `dialog:default`，允许全部官方默认的 message、save、open 对话框类型；不得使用 permission wildcard、deprecated `ask`/`confirm` alias、额外 dialog permission 或任何 `dialog:deny-*` 项。主窗口和其他窗口均不获得 filesystem 权限；open/save 只返回用户在原生对话框中选择的路径，不授权读取、写入、创建、覆盖或删除对应文件。`os`、updater、window-state 继续保持 Rust-only，不安装相应前端包、不开放 WebView ACL，也不因 dialog 基线改变既有 IPC、零出站或安全恢复边界。
- 影响范围：最新 Product Spec、GUI 初始化/adapter/初始化 E2E 与升级工程层、Rust/Tauri/React 基线、capability 模板、插件契约检查器、Harness validator、回归测试和当日 Changelog。九字段 GUI profile、条件能力选择、业务 core、真实 updater 配置、文件读写能力、签名、构建和发布授权不变。
- 理由：固定官方依赖和注册顺序消除每个下游重复装配；只给主窗口一个官方审阅过的默认权限集合即可覆盖通用 message/save/open，同时让文件对话框选择与后续文件 I/O 授权保持分离。dialog 明确归类为固定 WebView 基线，保留三项 Rust-only 基线“无前端包、无 ACL”的安全语义。
- 风险：`dialog:default` 的上游内容或前端 `^2.7.3` 解析结果可能在兼容更新中变化，受损 WebView 也可能滥用原生对话框骚扰用户；open/save 返回的路径可能泄露用户选择的位置。通过 Rust 根版本事实、锁文件、主窗口精确 capability、无 wildcard/deny/filesystem 权限、CSP、依赖与 permission 契约回归降低风险；真实下游仍须观察 message/save/open 的打开、取消与路径返回，不得把静态 fixture 当作候选验收。
- 验证：机械门禁必须同时证明三处依赖声明、Builder 相对顺序、dialog 不进入 `invoke_handler`、主窗口只有精确 `dialog:default`、全部官方默认 message/save/open 可达，以及 wildcard、ask/confirm alias、deny、额外 dialog permission 与所有 filesystem 权限均缺席；负向回归分别注入每类越权或错误接线并要求失败关闭。Harness 源只验证规则、Skills、模板与契约检查器，真实 GUI/Computer Use、初始化 E2E、构建、签名、公证和发布仍由适用下游执行。
- 恢复或调整标准：若官方 dialog API、默认 permission 集、Tauri/WebView 兼容性或真实宿主证据变化，必须用新 ADR 记录经过审阅的版本、最小权限、迁移和真实验证后调整；若产品需要读取或写入用户所选文件，必须作为独立 filesystem/业务能力取得产品与安全批准，不能扩大本基线、改用 wildcard 或把文件 I/O 偷渡进 dialog command。

## ADR-20260901-004：全局快捷键能力与绑定分离，默认零绑定并由产品需求声明动作

- 日期：2026-09-01；状态：Accepted。
- 关联需求：项目负责人要求参考真实终端下游已经形成的快捷键设计，完善 Harness 的全局快捷键模块，并明确禁止 Harness 强制绑定默认快捷键，所有 chord 与动作完全由需求决定。
- 变更标识与所需版本：`change_id = HARNESS-FEAT-DEMAND-DRIVEN-GLOBAL-SHORTCUT-BINDINGS`；`required_version = 202608281139`。本决定不自动改变 `Version.md`。
- 背景：ADR-20260901-003 把全局快捷键拆为独立 Skill，但同时把 `global_shortcut = enabled` 等同于注册 `CommandOrControl+Shift+Space → restore_main_window`。这会在没有产品需求时占用系统键位，把能力开关、期望绑定与 OS 实际注册压成一个状态，也迫使需要其他动作、可编辑绑定或默认未绑定的下游记录例外。参考下游已经证明 `Option<String>/null`、逐项真实注册、原子替换/回滚和录制态抑制可以在零默认绑定下完整工作。
- 决定：保留九字段 profile 中的 `global_shortcut = enabled|disabled`。启用时另要求唯一 JSON 围栏 `gui-global-shortcut-contract`；中性初始化固定为 `schemaVersion = 1` 与 `actions = []`，只安装 Rust-only 插件能力，不预置产品动作、chord、OS 注册、快捷键页面或占位状态。禁用时 contract、依赖、插件、注册器、命令、配置、UI/i18n 与专属测试全部缺席。
- 产品 contract：产品范围批准后，每项动作以唯一 ASCII `snake_case` ID 声明 `fixed|user-configurable`、`defaultChord: string|null`、`host-action|core-use-case` typed dispatch 及 `e2eSafe`；dispatch target 只接受稳定 ASCII snake_case/点分 snake_case 标识。Rust action 表必须与 profile 逐字段等价物化且每个 target 有明确 typed dispatcher 分支，禁止缺失、额外、漂移或 wildcard/no-op。`fixed` 只有需求明确给出非空 chord 才成立且运行时不可修改；`user-configurable` 允许 `null`，不得用推荐、fallback 或旧 Harness 值填补。宿主动作留在 GUI adapter，业务动作调用单个 core 用例；脚本、Shell、动态 URL、文件、网络、支付、删除和未批准命令不得成为快捷键直接分派目标。
- 状态与事务：能力开关、期望绑定和 active OS registry 分为三层。逐项状态返回配置 chord、真实 `registered` 与稳定脱敏错误码。非空 chord 使用官方解析器规范化、最多 128 UTF-8 字节、至少一个修饰键并拒绝规范化重复。更新完整快照时先保留旧文件/注册，注册全部新项，任何注册或持久化失败都清理本轮新项并恢复旧状态；初始化失败、退出和 E2E 只注销模块实际拥有的 chord，不无差别调用 `unregister_all()`。
- 可编辑边界：只有 contract 声明 `user-configurable` 才建立录制、取消与清空。录制使用 `KeyboardEvent.code`，保留 Tab/Escape/清空语义，忽略重复键与 IME；开始前先从 Rust 取得唯一 token，失败即关闭录制。录制期间保留旧注册但暂停本模块分派，完整快照串行保存，失败恢复服务端旧值。WebView 不使用 localStorage、sessionStorage、Jotai 或 TanStack Query 保存权威绑定。
- 替代关系：本决定只取代 ADR-20260901-003 中“启用即固定注册 `CommandOrControl+Shift+Space`、动作固定恢复窗口、只安装插件不得视为启用”的子决定，以及由该子决定派生的固定只读设置/E2E。ADR-20260901-003 的独立 Skill 所有权、Rust-only、plugin 顺序、Pressed-only、安全最小权限、冲突可见和退出回收继续有效，但改为按 contract 与 owned registry 执行。
- 风险与验证：允许零绑定可能被误解为能力缺失，或可编辑事务可能留下部分注册/陈旧文件。以唯一 machine-readable contract、空 contract 零注册正向回归、固定/可编辑 schema 负向回归、Pressed-only typed dispatch、逐项 `is_registered`、注册/持久化双回滚、录制 token 和 owned cleanup 门禁降低风险。中性初始化 E2E 证明零默认键位；只有明确安全非空动作才触发真实 chord，并在恢复原配置后确认释放。本轮 Harness 源只验证规则、Skills 和契约检查器，不把 fixture 当作真实 Tauri 候选。
- 恢复或调整标准：只有真实产品需求可在自身 Product Spec/GUI profile 中增加固定或可编辑动作及初始 chord；不得恢复 Harness 通用默认。若官方插件或平台证据证明当前事务/录制模型不可维护，可用新 ADR 调整数据模型和回滚机制，但仍必须保持“无明确需求即零绑定”和 core-first typed dispatch。

## ADR-20260901-003：GUI 官方插件按独立 Skill 管理，系统语言、更新器与窗口状态成为固定基线

- 日期：2026-09-01；状态：Accepted；其中全局快捷键固定 chord/action 子决定已由 ADR-20260901-004 取代。
- 关联需求：项目负责人要求把已调研的 Tauri v2 完善插件分别沉淀为独立能力 Skill，在选择 GUI 时询问可选能力，并把 updater、window-state 等基础能力设为无需额外询问的基线；同时验证和修复既有固化 GUI Skills。
- 变更标识与所需版本：`change_id = HARNESS-FEAT-GUI-PLUGIN-CAPABILITY-MODULES`；`required_version = 202608281139`。本决定不自动改变 `Version.md`。
- 决定：所有 GUI 无条件调用 `$desktop-add-gui-system-locale`、`$desktop-add-gui-updater`、`$desktop-add-gui-window-state`。三者不进入 `gui-initialization-config`，不单独询问，并统一采用 Rust-only 接线，不安装相应 JavaScript 包或向 WebView 开放默认 ACL。system-locale 以官方 OS plugin 的 `locale()` 为唯一系统探测源；updater 在缺少 endpoint、公钥、channel/target 产品事实时固定为 `NotConfigured` 且零出站；window-state 只持久化 `SIZE | POSITION | MAXIMIZED`，拒绝恢复隐藏、装饰、全屏或最小化状态，并为首次、损坏和越界状态保留 1440×900 居中、最小 960×640 的安全回退。
- 可选能力：GUI 条件问询固定为系统托盘、系统通知、开机自启、关于页、赞助页、单实例、深链接、全局快捷键八项 `enabled|disabled`，再加侧栏 `compact|detailed`，按此顺序写入唯一九字段 profile。系统托盘、单实例、深链接、全局快捷键分别由新独立 Skill 拥有；通知与自启继续由既有独立 Skill 拥有。禁用时必须同时移除专属依赖、feature、插件注册、命令、ACL、UI/i18n 与测试场景。
- 组合与真实行为：`deep_link = enabled` 强制要求 `single_instance = enabled`，single-instance 仍是首个 plugin 并启用官方 `deep-link` feature，deep-link 紧随其后。初始化期深链接只接受从已确认项目标识派生的 `app-<kebab-project-id>://restore`，只恢复并聚焦既有主窗口，且静态中性路线不预开运行时注册 API。原决定曾要求全局快捷键固定为 `CommandOrControl+Shift+Space` 并恢复窗口；该子决定现已由 ADR-20260901-004 取代，不再是活动契约。Pressed-only、安全最小权限、冲突可见与 owned 注销边界仍按新动作 contract 继续有效。
- 注册与发布边界：GUI adapter 统一拥有稳定 plugin 顺序与唯一 command handler，能力 Skill 只拥有自身依赖、运行时、回归和禁用无残留契约。固定 updater 仅表示本地生命周期基线，不授权网络 endpoint、签名密钥或更新制品；正式构建仍由独立 `updaterEnabled` 产品事实决定是否生成并验证官方 archive 与 `.sig`。macOS 深链接的最终协议注册只接受已打包应用证据，debug no-bundle 不得冒充。
- 取代范围：本决定把 ADR-20260831-005 与 ADR-20260827-001 所述七字段 GUI profile 扩为九字段；取代 ADR-20260806-001 中“中性初始化允许暂不接入 `tauri-plugin-os`”的 GUI 初始化时点，但保留其统一 locale、前端/原生语言状态和 core 语言无关边界；既有通知、自启、页面和侧栏语义继续有效。
- 风险与验证：固定插件若被误解为默认联网或默认恢复隐藏状态，会引入隐私、启动与可找回性风险；深链接/单实例顺序错误会丢失热启动 URL，全局快捷键冲突可能让能力静默失效。以独立 Skill 契约、唯一九字段解析、依赖下界/注册顺序/Rust-only/零出站/无残留检查、非常量单元回归和真实初始化 E2E 失败关闭；window-state 性能测量必须隔离并恢复状态目录。本轮在 Harness 源只验证规则、Skill 和标准库校验器，不把中性 fixture 声称为真实 Tauri 候选。
- 恢复或调整标准：只有官方插件 API、平台限制或真实下游证据证明当前基线不安全或不可维护时，才可用新 ADR 调整版本、顺序、持久化 flags、URL/chord 或是否固定；新增网络配置、前端 ACL、产品动作或协议载荷必须另获产品范围批准，不能由本基线推断。

## ADR-20260901-002：GUI 发布性能改为每次发布单独选择

- 日期：2026-09-01；状态：Accepted。
- 关联需求：项目负责人认为每次发布固定采集启动、交互、CPU 和内存指标耗时过长，要求把性能指标改为发布时可选，并在每次发布前询问。
- 变更标识与所需版本：`change_id = HARNESS-FEAT-GUI-PER-RELEASE-PERFORMANCE-SELECTION`；`required_version = 202608281139`。该值是当前 Harness 未发布时间版本，本决定不自动改变 `Version.md`。
- 背景：ADR-20260831-006 建立了可追溯的原生 Release no-bundle 探针、固定预算、失败回流和 runtime 绑定，但同时把它设为每个 GUI 发布都必须运行。性能采样需要多次冷启动、至少 20 次交互和两段 30 秒资源观察，会显著拉长不需要性能结论的发布。
- 决定：取代 ADR-20260831-006 中“每个 GUI 发布无条件运行、E2E disabled 也不能跳过”的选择部分。每次 GUI 发布在任何本地发布提交、测试或编译前解析当次 `performanceSelection: enabled | disabled`；当前请求已经明确时复用，否则询问一次。同一发布的修复重跑复用原选择，新发布重新询问。选择不写入 `docs/AGENT_POLICY.md` frontmatter、不从 `milestone_e2e` 或上次发布推断；产品/渠道硬要求优先并强制启用。选择、来源及关闭时的原因/剩余风险作为当前候选 `candidateSelections` 与链状态原子进入 closing commit，使中断重试只读复用而不重新询问。
- 启用路径：`enabled` 或硬要求时完整保留 ADR-20260831-006 的真实目标平台、整进程树、阈值、证据、失败修复、重新构建、显式 waiver 和包内 runtime 绑定。`waived` 只表示已有真实 `failed` 证据后用户批准继续，不能作为预先跳过的别名；E2E 仍不能替代已启用性能结论。
- 关闭路径：`disabled` 且无硬要求时跳过整个 `pnpm tauri build --no-bundle` 性能探针、Computer Use 采样、helper 与 runtime 绑定；候选 manifest 记录 `performanceSelection: disabled`、`performanceStatus: Not run`、非空原因和剩余风险，并要求 `performanceEvidence`、`performanceProbe*`、`performanceThresholdProfile`、`performanceWaiver` 与 `performanceRuntimeBinding` 全部缺席。同一候选已经产生真实失败后不能改写成 `Not run` 擦除证据，只能修复重建或保留失败并显式 `waived`。
- 平台与验收：`enabled` 的原生候选只接受 `passed | waived`，启用但只有 xwin/非原生目标时继续准确记录 `Unverified`；`disabled` 使用 `Not run` 而非 `Unverified`。没有产品/渠道硬要求时，带完整原因和风险的 `disabled + Not run` 可以形成和接受候选；存在硬要求时 `disabled`、`Not run` 或 `Unverified` 均阻断。收集、验收和就绪复核只读取 manifest，不在下游阶段补问或改变选择。
- 影响范围：Agent Policy 正文、工程/发布/验证/Rust 事实源、README、Product Spec、GUI 构建/性能/收集/验收/E2E/发布准备 Skills、性能证据 schema/helper、Harness validator 与专项回归；不新增持久策略字段，不改变 CLI/TUI/MCP 构建或 GUI 初始化 E2E。
- 理由：把昂贵采样放到用户真正需要的发布中，可缩短常规候选形成时间；逐次询问避免永久关闭掩盖关键版本的性能风险。`Not run`、`Unverified` 与 `waived` 的分离让主动跳过、平台未验证和已知失败继续具有不同且可审计的含义。
- 风险与验证：关闭性能会留下启动卡顿、CPU/RSS 超限或泄漏未被量化的风险，因此 manifest、最终回复和验收必须公开原因与剩余风险，渠道硬要求不能被关闭。专项回归锁定逐次询问、helper 拒绝 `disabled`、启用时仍与 E2E 独立、关闭时证据字段缺席、xwin 状态和条件验收；完整 Harness Python 回归与 validator 负责证明传播一致。
- 恢复或调整标准：若真实发布证明逐次询问造成不可接受的误操作，或特定渠道/产品必须始终量化性能，可在批准 Product Spec/渠道规则中把性能设为硬要求，仍复用启用路径；若要恢复全局强制或增加持久默认值，必须由项目负责人明确批准新的 ADR、迁移既有策略 schema，并说明时间成本与恢复方式。

## ADR-20260901-001：AGENTS 作为有预算的渐进披露启动路由器

- 日期：2026-09-01；状态：Accepted。
- 关联需求：项目负责人要求缩减过于巨大的 `AGENTS.md`，按实际任务渐进加载额外门禁、规则与 Skills。
- 变更标识与所需版本：`change_id = HARNESS-FEAT-PROGRESSIVE-AGENT-INSTRUCTION-ROUTING`；`required_version = 202608281139`。本决定不自动改变 `Version.md`。
- 决定：Harness 源与派生下游的根 `AGENTS.md` 只保存项目身份、启动/范围门禁、任务路由、跨任务硬约束摘要、非空 Skills 地图、非空约束地图、最小闭环与验证入口。初始化、GUI、依赖、构建、发布、验证等详细规则只写在既有唯一事实源或对应 Skill 中，命中任务后再完整读取；不为拆分新增第二套重复规则文档。
- 体积门禁：根入口的 UTF-8 字节数不得超过 20,000，行数不得超过 120。Harness validator 只在根入口检查预算、永久章节、范围门禁与路由语义；详细事实继续在其权威文档和 Skills 中检查，禁止通过把正文复制回根入口来满足片段断言。
- 派生与升级边界：初始化/实例化仍按所选接口裁剪不适用 Skills 和地图条目，但必须保留轻量路由结构、终端下游禁止继续派生、版本/升级入口和 Agent Policy 路由。现有 `AGENTS.md` 的 `merge-sections` 升级所有权不变；没有新增需传播或保护的规范文件。
- 理由：入口只承担任务分类和安全启动，能显著降低每轮固定上下文，同时让每条细则仍由现有事实源验证；复用既有文件避免拆出新的规则副本、升级所有权和悬空链接。
- 风险与验证：过度精简可能遗漏跨任务安全边界，因此范围门禁、高风险授权、产品边界确认、core-first 摘要、版本入口、真实产物声明和用户修改保护保持常驻；专项回归锁定预算与永久章节，完整 Harness 单测和 validator 证明路由仍覆盖现有 Skills/约束。
- 恢复或调整标准：只有真实任务证明某项门禁必须在分类前常驻时才把最短摘要加入根入口；新增正文仍优先写入唯一事实源。若预算不足，先合并重复路由或拆分事实源，不得直接移除预算。

## ADR-20260831-007：依赖清单保存兼容下界，缺失工具与新依赖优先选择最新兼容稳定版（低于下界的环境处置已由 ADR-20260907-002 取代）

- 日期：2026-08-31；状态：Partially superseded by ADR-20260907-002；依赖选择、兼容范围、锁文件和范围内工具复用继续有效。
- 关联需求：项目负责人要求把版本要求解释为最低要求，已安装开发工具高于最低要求时直接通过，缺失工具和新增依赖优先选择最新版本，并把下游 Rust MSRV 提升到 1.95。
- 变更标识与所需版本：`change_id = HARNESS-FEAT-RUST-1-95-LATEST-STABLE-SELECTION`；`required_version = 202608281139`。本决定不自动改变 `Version.md`。
- 决定：清单与 `engines` 继续保存经过真实验证的最低兼容范围，不写 `latest`、tag 或无下界范围；选择或安装阶段则查询官方 registry 的当前最新稳定候选，验证 MSRV、peer、目标平台和所需 API/feature 后采用它，并把验证通过的版本写成新的完整下界。锁文件固定该次真实解析结果。现有稳定工具只要落入受支持范围就直接复用，不因不是最新版而升级。
- 当前基线：Rust MSRV 为 1.95；中性 Rust 直接下界为 `assert_cmd 2.2.2`、`clap 4.6.6`、`jiff 0.2.35`、`serde 1.0.229`、`serde_json 1.0.151`、`tokio 1.53.1`。GUI 工具范围为 Node.js `^24.15.0 || >=26.0.0`、pnpm `>=11.24.0`；缺失 Node.js 安装当前受支持 LTS，缺失 pnpm 解析并安装 registry 当前最新稳定版。Node.js 25 等上游明确不支持的版本不因数值更高而通过。
- 兼容性决定：最新不等于不验证。TypeScript、ESLint、jsdom、Tauri 插件和其他成组依赖必须先求交集；当前 TypeScript 采用与 `typescript-eslint` peer 兼容的 6.0.3，而不是冲突的 7.x。pre-1.0 工具可保留经验证的兼容上界；`cargo-xwin` 当前范围更新为 `>=0.23.1, <0.24.0`。
- 取代范围：本决定取代 ADR-20260825-003 中“不得以最新为选择目标”、Rust 1.90、Node 20/22 与 pnpm 10 的当前规则；最低范围、最小 feature、锁文件可复现、三平台验证和禁止把 `latest` 写进清单的其余规则继续有效。
- 风险与验证：registry 最新版可能刚发布或存在不兼容，因此每次选择必须记录来源、稳定性和兼容验证；Rust 直接最低版本必须在 1.95 上通过全 workspace 测试，前端最低直接版本与当前锁定版本都要通过类型、lint 和测试。无法验证的候选不能抬高下界。
- 恢复或调整标准：只有官方支持范围、真实最低版本测试或平台证据变化时才更新范围；不得把“版本号更大”当成绕过 peer、MSRV 或平台不兼容的理由。

## ADR-20260831-006：GUI 发布在打包前执行定量性能门禁（执行选择已由 ADR-20260901-002 取代，固定预算已由 ADR-20260907-001 取代）

- 日期：2026-08-31；状态：Partially superseded by ADR-20260901-002 and ADR-20260907-001；真实探针、完整性、失败回流、waiver 与 runtime 绑定继续有效。
- 关联需求：项目负责人要求避免发布会卡顿或拖慢系统的 GUI，在发布前测量性能，超标先修复，无法解决时由用户决定是否继续构建。
- 变更标识与所需版本：`change_id = HARNESS-FEAT-GUI-RELEASE-PERFORMANCE-GATE`；`required_version = 202608281139`。
- 决定：每个 GUI 正式发布从最终干净提交先产生 release-profile、no-bundle 性能探针候选，再进入安装包、签名和公证。该门禁独立于 `milestone_e2e`，不能被 `disabled` 跳过；必须测整个应用进程树和实际 WebView，不接受 debug、源码函数、父进程单点或模拟数据。
- 默认预算：一次预热后执行 5 次冷启动，主窗口可操作时间中位数不超过 2 秒且最大值不超过 3 秒；批准的代表性交互至少 20 次，p95 不超过 100 毫秒且单次低于 200 毫秒；30 秒空闲 CPU p95 不超过单核 5%，适用的隐藏/托盘状态不超过 2%；稳定 RSS 不超过 300 MiB、峰值不超过 500 MiB；20 轮路由/设置操作后 RSS 增量不超过 `max(15%, 32 MiB)`；退出后全部所属进程回收。50–199 毫秒 long task 必须记录，200 毫秒及以上视为失败。
- 失败闭环：任一指标超标、样本不足、无法观察或采样器失败都返回 `$desktop-implement-change` 诊断、修复、回归并从新提交重建探针。只有穷尽安全且范围内的修复后，才询问用户是否继续；明确继续只能把当前候选记为 `performanceStatus: waived`，同时保存失败数值、风险、原因和确认，不得改判为 `passed`。未确认时不得继续打包。
- 平台与字节边界：每个平台分别测量；xwin 交叉编译不能冒充 Windows 运行性能。签名或重打包若改变运行字节或启动器，最终验收仍需确认启动和交互没有回归；产品或渠道可在 Product Spec 收紧预算，放宽只能走当前候选显式 waiver。
- 理由与风险：绝对预算让“卡顿”可判定，进程树采样避免只看壳进程；硬件噪声通过预热、多样本和同机重测降低。性能 waiver 保留交付选择，但不会隐藏已知风险。
- 恢复或调整标准：只有跨平台真实数据证明默认预算系统性失真时，才能用新 ADR 修改指标、样本或归一化方法；不得移除真实 release-profile 候选、整个进程树、失败回流或显式 waiver。

## ADR-20260831-005：GUI 初始化显式选择系统通知和开机自启

- 日期：2026-08-31；状态：Accepted。
- 关联需求：项目负责人要求从 `bifang_tomato_clock` 已完成实现提取系统通知 Skill，并在 GUI 初始化中新增系统通知与开机自启问询和设置开关。
- 变更标识与所需版本：`change_id = HARNESS-FEAT-GUI-NOTIFICATION-AUTOSTART-CAPABILITIES`；`required_version = 202608281139`。
- 决定：GUI 初始化 profile 从五项扩为七项，稳定顺序是 `system_tray`、`system_notification`、`autostart`、`about_page`、`sponsor_page`、`single_instance`、`sidebar_mode`。前六项必须明确为 `enabled|disabled`，侧栏仍为 `compact|detailed` 且未选择时物化 `detailed`。
- 通知决定：`system_notification = enabled` 时调用独立 `$desktop-add-gui-system-notifications`。macOS 使用现代 User Notifications 路线，Windows/Linux 使用官方 Tauri notification 插件；Rust-only command 保持最小 WebView ACL。权限请求与发送在生命周期拥有的串行 worker 中执行，开启设置必须先获授权再持久化，默认关闭，关闭只抑制应用发送而不伪造撤销 OS 权限；投递失败可见且不回滚已经完成的业务动作。中性 scaffold 只安装能力，不猜测通知触发点或产品文案。
- 自启决定：`autostart = enabled` 时调用独立 `$desktop-add-gui-autostart`，使用官方 Tauri autostart 插件并由 Rust command 暴露设置能力。加入能力不等于默认注册，设置默认关闭，OS 注册状态为权威；enable/disable 失败时 UI 回滚并显示可访问错误。未启用托盘时自启必须显示可找回的主窗口。
- 缺席与验证：任一能力为 `disabled` 时，其依赖、插件、命令、权限、设置控件、i18n、事件和测试场景必须缺席。设置动作绑定在各自 `Switch`，父容器不得代理。原“初始化 E2E 对启用通知执行真实授权/投递检查”已由 ADR-20260908-002 取代；当前只检查默认关闭、结构和失败可见性，真实权限/投递留给满足签名前提的最终安装候选。启用自启仍测试状态切换并恢复执行前 OS 登录项状态；无法恢复即失败关闭。
- 来源边界：只泛化参考项目的权限、worker、所有权、失败与设置模式，不复制 Pomodoro 状态、事件名、本地声音或产品文案。参考项目没有开机自启实现，因此自启是基于官方插件的新通用能力，不声称来自该项目。
- 恢复或调整标准：只有官方平台 API、真实宿主限制或产品选择模型变化时才调整；不得恢复静默默认启用、授权前持久化、detached task、父容器代理或禁用后残留。

## ADR-20260831-004：初始化主动准备 Git，显式发布自动本地提交后构建

- 日期：2026-08-31；状态：Accepted。
- 关联需求：项目负责人要求照顾不了解 Git 的首次开发者，在初始化时检查/安装并自动补齐作者信息；显式发布时无需二次审批，直接本地提交并构建。
- 变更标识与所需版本：`change_id = HARNESS-FEAT-INITIALIZATION-GIT-BOOTSTRAP-RELEASE-AUTOCOMMIT`；`required_version = 202608281139`。
- 初始化决定：完整表单确认后、首次脚手架写入前，开发环境门禁检查 Git；缺失时使用受管平台安装路径安装并复探，失败则在写入前停止。最终独立仓库建立后，已有有效作者字段保持不变；缺失字段由 Agent 把设备账户名翻译/转写为可读 ASCII 英文名称，邮箱为 `<ascii-device-username>@gmail.com`，只写目标仓库 local 配置。不得修改 global/system、配置 remote、push/tag 或把派生 Gmail 地址声称为真实邮箱。
- 报告决定：初始化完成必须返回 Git 版本、是否安装、有效 `user.name`/`user.email`、每项来源、`local` 作用域、仓库根、模板状态和基线提交。派生身份静默设置但在完成报告中明确标注，用户以后可在该仓库修改。
- 发布决定：明确“发布/准备并构建发布”请求本身授权当前流程把已完成且归属明确的本地变化形成逻辑提交并继续候选构建，不再为本地提交或构建要求第二次审批。先复核工作树、秘密与范围并提交已完成源码，再从该新 HEAD 生成双语发布日志并在有变化时形成发布元数据提交；最后要求工作树干净，以最终 40 位 HEAD 构建。普通“构建”请求不自动提交。
- 失败边界：无关或归属不明修改、疑似秘密、空或无法解释的提交、hook/签名交互、提交失败、非独立仓库或 dirty 最终状态都停止，禁止 `--no-verify`、静默扩大提交范围或继续构建。构建 Skill 必须拒绝 dirty worktree，manifest 的 `sourceCommit` 精确等于实际编译 HEAD。
- 取代范围：本决定取代 ADR-20260831-002 的 Git 检查时序和“缺身份即阻断”部分；仓库本地模板、独立 `main`、无 remote、单一初始化基线、禁止全局配置及无 push/tag 的安全边界继续有效。
- 恢复或调整标准：若受管安装路径或账户名来源在某平台不可可靠使用，应失败并报告，不得回退到全局配置、伪造远端身份或不绑定提交的构建。

## ADR-20260831-003：Harness 模板源只接受自身工程与下游初始化所需信息

- 日期：2026-08-31；状态：Accepted。
- 关联需求：项目负责人明确要求当前模板项目禁止接受 Harness 工程所需信息之外的任何需求，避免具体产品事实污染可复用源仓库。
- 变更标识与所需版本：`change_id = HARNESS-FEAT-HARNESS-SOURCE-SCOPE-GATE`；`required_version = 202608281139`。该值是当前 Harness 未发布时间版本，本决定不自动改变 `Version.md`。
- 背景：Harness 源同时承担自身工程维护与创建终端下游的入口；若在源根整理、记录或实现产品需求，这些事实会被误当模板规范或传播到其他下游。仅以“产品可稍后定义”描述中性脚手架不足以阻止混合请求提前污染源仓库。
- 决定：同时存在 Harness 专用根 `Version.md` 与活动 `.agents/skills/desktop-instantiate-project/SKILL.md` 时，确定性识别为模板源。允许范围只有 Harness 规则、Skills、中性资产、脚本、验证器、文档、许可证、发布工程等自身维护，以及创建终端下游所需的固定初始化表单与所选初始化路径身份信息。
- 拒绝范围：产品目的、用户场景、业务输入输出、业务规则、成功标准、产品专属 UI/文案/数据、远程地址、凭据、产品构建、签名或发布需求不得在源根被接收、分析、记录、规划、实现或暂存。Agent 必须保留当前文件并要求用户先实例化或切换到已存在的唯一终端下游根目录后重新提出。混合请求只解析允许的初始化字段，其余部分明确拒绝和延后。
- 影响范围：AGENTS、README、工程规则、Rust 基线、实例化表单及实例化/初始化/定义产品/日常实施 Skills、Product Spec、Changelog、Harness validator 与专项回归。
- 理由：把模板源严格保持为产品无关工程资产，能够让拒绝发生在任何写入前，并消除“先记录、后搬运”的隐式数据流；固定初始化字段仍是 Harness 创建下游不可缺少的工程输入，因此保留。
- 风险：过宽拒绝可能阻止 Harness 自身必要维护；通过明确允许规则、Skills、中性资产、脚本、验证器、文档、许可证和发布工程降低风险。路径识别若漂移可能误判；由两个 Harness 专用锚点和跨文件机械门禁锁定。
- 验证：静态回归检查源识别锚点、允许/拒绝类别、混合请求处理和下游重提要求；完整 Harness validator 同步检查 AGENTS、README、Product Spec、表单及三个执行入口。本轮不创建具体产品、下游或发布候选。
- 恢复或调整标准：只有项目负责人明确改变 Harness 源职责，且新决定说明允许的数据类别、传播边界、清理与迁移方式时才能调整；不得以单次产品请求、未来复制或临时计划作为例外。

## ADR-20260831-002：Git 只在下一步实际提交时即时检查和设置（已由 ADR-20260831-004 取代）

- 日期：2026-08-31；状态：Superseded by ADR-20260831-004。
- 关联需求：项目负责人明确要求 Git 应在用户实际需要提交代码前进行检查和设置，而不是在打开仓库、表单或脚手架早期预先配置。
- 变更标识与所需版本：`change_id = HARNESS-FEAT-JUST-IN-TIME-GIT-COMMIT-SETUP`；`required_version = 202608281139`。该值是当前 Harness 未发布时间版本，本决定不自动改变 `Version.md`。
- 背景：早期运行 Git 可用性、作者身份或提交模板检查，会在尚未形成提交意图时增加环境要求并修改仓库本地元数据。新下游仍需要独立仓库和唯一基线提交，但该事实不要求在表单、复制、测试或裁剪阶段提前准备 Git。
- 决定：只有用户明确请求配置 Git，或已授权工作流的下一项真实动作就是运行 `git commit` 时，才触发 `$desktop-configure-git-commits` 与相关 Git 可用性、版本、身份、仓库边界检查。打开/读取仓库、只读状态检查、诊断、编辑、测试、生成脚手架或未来可能提交都不触发配置脚本。为保护已有修改而进行的只读根、分支和状态检查继续允许，并与提交配置严格区分。
- 初始化时序：新下游完成全部脚手架检查、GUI 适用门禁和一次性裁剪后，下一步实际创建唯一基线提交时，才检查 `git --version` 与用户身份、建立或确认独立 `main` 仓库、验证无远端/无既有 HEAD、运行仓库本地 `install`/`check` 并提交。任何失败在提交前阻断，不得修改全局配置或伪造身份。
- 影响范围：AGENTS、README、工程规则、Rust 基线、实例化与初始化 Skills、Git 提交 Skill、初始化 validator、专项回归、Product Spec 和 Changelog。
- 理由：把副作用和依赖门禁紧邻其唯一消费者 `git commit`，可避免无提交任务被迫配置 Git，同时不削弱真正提交前的模板、身份和独立仓库保障。
- 风险：过度延迟可能在所有脚手架工作完成后才暴露 Git 缺失或身份不可用；这是有意的即时失败边界，保留全部目标文件并给出真实诊断即可恢复。不得以降低晚期失败概率为由重新提前设置。
- 验证：回归把初始化 Skill 按最终收尾步骤切分，证明此前不含 `git --version`、`git init` 或提交模板脚本，收尾段同时包含这些命令和实际基线提交条件；Git Skill 契约检查明确触发与非触发场景。规则实施阶段没有提前运行 Git 配置；项目负责人随后明确要求提交并推送全部变更，此时才按本决定即时运行仓库本地 `install`/`check` 并创建提交。
- 恢复或调整标准：只有真实工作流证明某个更早阶段必须消费具体 Git 能力，且项目负责人批准精确触发点、副作用和失败恢复方式时才能调整；只读安全检查不构成把提交配置提前的理由。

## ADR-20260831-001：Logo 候选保持原始请求顺序并在选择后才处理

- 日期：2026-08-31；状态：Accepted。
- 关联需求：项目负责人明确要求用户选择图标前不做任何验证或标准化，并要求每次预览顺序保持一致。
- 变更标识与所需版本：`change_id = HARNESS-FEAT-DEFERRED-LOGO-VALIDATION-STABLE-PREVIEW`；`required_version = 202608281139`。该值是当前 Harness 未发布时间版本，本决定不自动改变 `Version.md`。
- 背景：若生成结果按完成时间、文件名、摘要或质量重排，用户后续指代会漂移；若在选择前检查、转码或标准化，预览就不再是生成工具交付的原始候选，也会对最终未采用项执行无必要处理。
- 顺序决定：三个生成请求在发起前按请求顺序绑定 `candidate-1`、`candidate-2`、`candidate-3`，返回结果始终绑定原请求标识。首次预览、重显、说明和选择问题固定使用该顺序；单项失败只重试同一标识，禁止移动、补位或重新编号。
- 处理时序决定：选择前候选只保存在任务精确拥有的临时目录，并以原始字节预览。除预览工具显示所必需的读取外，不检查文件类型、尺寸、色彩空间、透明通道、像素可见性、安全区、小尺寸辨识度、摘要或质量；不转码、缩放、裁剪、补边、改色、压缩、去元数据、覆盖重命名或执行其他标准化。
- 选择后决定：用户明确选择后只验证并按需标准化所选项，拒绝项不补做检查或处理。若标准化可见地改变构图、颜色或边缘，必须重新预览最终母版并确认；随后才写入母版和运行时副本、生成平台图标，并只记录所选项的后置验证与摘要证据。
- 影响范围：GUI 身份/初始化/adapter Skills 与基线、AGENTS、工程规则、Rust 基线、Product Spec、Changelog、初始化 validator 和专项回归。
- 理由：稳定逻辑标识让用户指代跨异步返回与重显保持不变；把处理推迟到选择后，既忠实展示原始输出，又把验证成本和数据读取收敛到实际使用项。
- 风险：原始候选可能无法被预览工具读取；此时只能把该请求标记为预览失败并重试原标识，不能通过预处理偷偷修复。所选项可能在选择后验证失败；保留选择和原 ID，报告失败并在用户允许下重试该项。临时文件清理必须在最终母版与证据确认后执行，且不得删除用户上传原件。
- 验证：静态顺序回归检查三个 ID 的首次出现顺序、失败重试与所有重显顺序；时序回归证明选择前段禁止验证/摘要/标准化，选择后段只处理所选项。最终平台图标的既有结构验证保持不变，但只发生在选择之后。
- 恢复或调整标准：只有项目负责人明确改变候选数量、用户选择交互或允许的选择前读取范围时才能调整；不得以质量保证、工具便利或异步完成时间为由恢复预验证、预标准化或重排。

## ADR-20260828-003：初始化确认中英文名称，发布日志以双语翻译对跟随 i18n

- 日期：2026-08-28；状态：Accepted。
- 关联需求：项目负责人明确要求初始化必须取得中英文名称，只提供其中一个时自动翻译另一个；正式发布的更新日志也必须提供中英文版本并遵循现有 i18n。
- 变更标识与所需版本：`change_id = HARNESS-FEAT-BILINGUAL-INITIALIZATION-AND-RELEASE-NOTES`；`required_version = 202608281139`。该值是当前 Harness 未发布时间版本，本决定不自动改变 `Version.md`。
- 初始化名称决定：ADR-20260828-002 的单一“项目展示名”基础字段拆为“中文项目展示名称”和“英文项目展示名称”，其余基础字段、首轮集中问询和条件字段顺序不变。中英文至少一个必须由用户直接提供；只提供一种语言时，Agent 在基础字段解析阶段自动翻译另一种，不增加独立问询，并记录哪个名称来自用户、哪个来自自动翻译。两个名称都由用户提供时保持原值，不自行改译；两个都缺失时在首轮同时询问。首次写入前的完整汇总必须并列显示两个名称及来源，用户对汇总的确认同时确认译名。
- 身份传播决定：`$desktop-rename-project-identity` 接受独立的中英文旧/新名称映射；`LICENSE.zh-CN.md` 只替换为确认后的中文名称，`LICENSE.en.md` 只替换为确认后的英文名称，README 同时列出两者，双语 GUI 资源按 locale 使用对应名称。许可证除精确项目名称词元外的法律文本不得改变；ASCII `snake_case` 标识和 kebab 前缀仍独立确认/派生，不能用机器标识代替译名。
- 发布日志 schema 决定：ADR-20260826-003 的单语 schema v1 与固定中文渲染被本决定部分取代。根 `release-notes.json` 升级为 `schemaVersion: 2`；`featureOptimizations` 和 `bugFixes` 中每个逻辑条目是键恰好为 `zh-CN` 与 `en-US` 的对象，两种文案都必须非空、无首尾空白，并在同分类内逐语言去重。翻译对让两种语言共享同一条目数和顺序，避免两份独立列表漂移；任一翻译缺失或发布 helper 的配对参数数量不等都失败关闭。
- 发布准备与渲染决定：发布准备仍从同一真实版本/提交边界筛选至多 10 个功能优化和至多 10 个问题修复逻辑条目。若先整理一种语言，Agent 自动翻译另一种，但写入前必须并排复核事实与语义对应，并分别运行 `render --locale zh-CN` 和 `render --locale en-US`。中文结构使用“更新日志/功能优化/问题修复/无”，英文结构使用“Release notes/Feature optimizations/Bug fixes/None”；所有展示版本仍只带一个小写 `v`。
- GUI i18n 决定：Rust `load_release_notes` 与 React IPC 解码器都拒绝非 schema v2、未知字段、缺失翻译对和越界数据。关于页按 i18next 当前语言选择正文和标题：语言以 `zh` 开头时使用 `zh-CN`，其他或未知语言回退 `en-US`。候选仍只打包一份逐字节一致的 JSON，不生成两份可能漂移的制品；关于页未选择时运行时组件继续缺席。
- 保留范围：ADR-20260828-002 的首轮集中问询、路径 helper、完整汇总写入闸门、策略与 GUI 条件问询继续有效，只把基础字段从七项扩为八项并加入单语自动补译。ADR-20260826-003 的交互所有权、近五版、两类十条上限、候选摘要/包内字节绑定、保护属性和单 `v` 规则全部保留。
- 风险与缓解：自动译名可能不符合品牌偏好，因此译名必须标记来源并由写入前同一汇总确认；两个独立语言数组可能错位，因此 schema 以逐条翻译对绑定；未知 locale 可能显示原始键，因此固定回退英文；旧 schema v1 文件不得被脚本静默解释为已满足双语要求，发布准备必须先显式重建为 v2 并重新形成候选。
- 影响范围：初始化表单、实例化/直接初始化/身份改名 Skills 与脚本、两份许可证中的 Harness 项目名称词元、README/AGENTS/工程与发布规则、GUI 支持资产和 i18n、发布 helper、Rust/React 解码与回归、最新 Product Spec、当日 Changelog 和 Harness validator。
- 验证：初始化回归解析八个基础字段并检查单语自动翻译、来源标记与汇总确认语义；身份脚本回归证明中文/英文许可证分别替换；发布 helper 回归覆盖 schema v2、双语渲染、缺失翻译/未知 locale 失败、五版/十条/符号链接；GUI 结构和模板回归覆盖 Rust/React 缺译拒绝、中文与英文正文选择和英文回退。本轮不构建发布候选或运行真实下游 GUI E2E。
- 恢复或调整标准：只有项目负责人明确改变支持语言、译名确认方式、发布日志 locale/schema 或渠道格式时，才能以新 ADR 调整；不得恢复单一项目名称、单语发布日志、两份不配对列表、未确认自动译名或候选后补译。

## ADR-20260828-002：初始化首轮集中收集基础字段，条件配置随后按需逐项补全（GUI 字段集合已由 ADR-20260831-005 扩展）

- 日期：2026-08-28；状态：Accepted。
- 关联需求：项目负责人明确要求“初始化时，一次问出全部基础问题，之后再逐步按需补全其他问题”，以减少固定信息被拆成多轮问答造成的等待，同时保留条件配置的可理解决策顺序。
- 变更标识与所需版本：`change_id = HARNESS-FEAT-BATCHED-BASE-INITIALIZATION-FORM`；`required_version = 202608281139`。该值是当前 Harness 未发布时间版本，本决定不自动改变 `Version.md`。
- 基础字段决定：新下游实例化的固定基础字段恰好是项目展示名、ASCII `snake_case` 标识、项目路径、负责人、目标平台、接口组合和 Agent 策略模式。Agent 必须先复用当前请求中已合法提供的值，再在首轮用清晰编号一次列出其余全部基础字段；不得拆成逐字段多轮，也不得用不列字段的笼统问询替代。首轮回复有缺失或非法值时，集中说明仍需修正的基础字段与各自约束，合法字段不重问；基础字段全部解析前不进入条件阶段。
- 条件字段决定：选择自定义策略后，`superpowers`、`parallel_worktree_subagents`、`milestone_smoke`、`milestone_e2e` 每轮补全一个；选择 GUI 后，`system_tray`、`about_page`、`sponsor_page`、`single_instance`、`sidebar_mode` 每轮补全一个。不适用字段直接标记为不适用，不制造问询。GUI 四项能力仍须明确为 `enabled|disabled`，侧栏明确跳过时仍归一化为 `detailed`，显式非法值仍须重问。
- 直接初始化决定：`$desktop-initialize-rust-project` 不经实例化表单直接调用时，把尚未解析的接口组合与 Agent 策略模式放在同一首轮；只有基础决定完成后，才逐项询问自定义策略或 GUI 条件字段。由 `$desktop-instantiate-project` 发起时继续复用写入前完整表单，复制后不重复询问。
- 确认与写入边界：条件字段全部解析后，仍须运行只读路径 helper，并展示包含输入路径、唯一目标根、派生前缀、接口、策略和适用 GUI 五项配置的完整汇总；用户确认前继续禁止目录创建、复制、环境安装、Git 初始化或文件修改。
- 取代与保留范围：本决定取代同日未发布中间状态 `HARNESS-FEAT-STEPWISE-INSTANTIATION-FORM-PATH-RESOLUTION` 中“全部字段每轮只问一个”的交互方式，并以新的稳定变更标识描述当前行为。项目路径的父目录/最终根确定性解析、安全门禁、完整汇总确认、推荐策略配方、GUI 显式能力选择和 `pending` 禁止规则全部保留。
- 理由：展示名、标识、路径、负责人、平台、接口和策略模式彼此都是创建意图的固定轮廓，集中展示能让用户一次校对全貌；自定义策略和 GUI 能力是否适用取决于首轮选择，保留逐项条件问询可避免一次展示无关配置，并给每项有副作用的生命周期决定足够上下文。
- 影响范围：初始化表单参考、`$desktop-instantiate-project`、直接调用的 `$desktop-initialize-rust-project`、两项 Skill UI 元数据、AGENTS/README、Agent Policy、Rust 初始化基线、最新 Product Spec、当日 Changelog、Harness 契约片段和初始化专项回归。
- 风险与缓解：首轮字段过多可能导致漏答或格式混乱；通过固定七项、清晰编号、合法值预填复用和只集中补齐无效基础字段降低风险。若条件字段提前出现，可能把不适用配置误当必填；通过基础阶段完成门禁、显式阶段标签和结构回归阻止。集中首轮不授权写入，完整汇总确认仍是唯一写入闸门。
- 验证：Harness 回归必须解析初始化表的阶段列，证明前七项均为“首轮基础”、其余均为“条件补全”，并检查实例化与直接初始化入口保留相同阶段顺序；完整 validator 同步拒绝缺失的首轮集中问询或条件逐项语义。两个修改 Skill 另通过 Skill Creator quick validator。
- 恢复或调整标准：若真实初始化证明七项首轮表单在目标界面不可可靠提交，项目负责人可以明确调整基础字段集合、分组或交互载体；任何调整都必须继续复用合法输入、区分固定与条件字段、保留完整汇总写入闸门，并同步表单、Skills、Product Spec、ADR 与回归，不能静默退回所有字段逐轮询问。

## ADR-20260828-001：左侧 Task 以独立 Worktree、codex 分支和可审查提交交付单一结果

- 日期：2026-08-28
- 状态：Accepted
- 关联需求：项目负责人明确要求把“一个左侧 Task = 一个明确目标 + 一个独立 Worktree + 一个 `codex/*` 分支 + 一组可审查提交”设为核心约定，并统一 Task 的动作结果标题、描述内容、基线、提交、验收、主任务整合与清理规则。
- 变更标识与所需版本：`change_id = HARNESS-FEAT-INDEPENDENT-TASK-WORKTREE-DELIVERY`；`required_version = 202608051301`。该值是当前 Harness 未发布时间版本，本决定不自动改变 `Version.md`。
- 事实依据：OpenAI Docs 要求为每个 distinct outcome 建立独立 chat，并建议用描述 outcome 的短标题；Codex Worktree 从所选分支的当前 HEAD 建立，默认处于 detached HEAD，之后可用 `codex/` 前缀创建分支，Codex 管理 Worktree 通常专用于一个 chat（[Projects and chats](https://learn.chatgpt.com/docs/projects)、[Git worktrees](https://learn.chatgpt.com/docs/environments/git-worktrees)，核对日期：2026-08-28）。仓库规则不能修改 Codex 桌面应用的全局 UI 默认值，因此本决定约束 Agent 创建左侧 Task 时必须显式选择项目 Worktree；人工手动新建仍需在界面中选择 Worktree。
- 核心决定：每个用户可见左侧 Task 只承载一个可独立验收结果；原“动作 + 结果”标题子句已由 ADR-20260908-003 取代。Task 描述固定包含目标、工作方式、当前事实、必须阅读的项目文档、实施范围、禁止事项、验收标准和交付要求；各节写入当前真实值，不适用项明确标记，不得删除必填节后留下隐含边界。
- 基线与分支决定：新 Task 默认从主任务已同步并确认的最新本地 `main` HEAD 创建。存在远端时先获取引用并完成安全快进；Local 主工作目录有修改时，主任务先审查差异、排除秘密/生成物并提交已确认基线。没有 `main`、存在分叉、修改无法安全归属或无法提交时停止，不回退到当前分支、`master` 或 Codex 的未提交修改复制能力。Worktree 创建后在首次编辑前建立唯一 `codex/<task-slug>` 分支并核验实际 Git 根。
- 执行与提交决定：Task 只修改自己的 Worktree，不进入 Local 主目录或其他 Task Worktree；每个逻辑闭环形成结果导向提交。交付前必须完成任务要求的测试与权威文档同步，提交全部修改并保持 `git status --porcelain=v1 --untracked-files=all` 为空；`target/`、`node_modules/`、`dist/`、`__pycache__/` 和其他生成缓存不得提交。适用目标必须编译并验证真实目标行为；没有可编译目标或最终候选不在范围内时明确报告 `Not applicable`/`Not run`，不自动扩张构建、E2E 或完整验收。
- 权限与整合决定：Task 不覆盖 `/Applications` 中的最终应用，不删除其他 Worktree，不合并 `main`，也不执行未授权推送、发布、签名或外部副作用。Task 交付分支、提交哈希、实际验证、未执行项和剩余风险；主任务复核后整合到 `main`，只有状态干净且提交已成为 `main` 祖先后，才由主任务依次移除 Worktree 和安全删除分支。
- 层级决定：本条旧表述已由 ADR-20260902-002 取代。当前规则仍是 `parallel_worktree_subagents` 只控制单个左侧 Task 内部是否允许显式并行 Subagent，不控制左侧 Task 是否使用 Worktree；Task 内并行使用 `$desktop-run-parallel-worktrees` 的 `codex/unit-<task>-<unit>` 单元分支和边界 guard，不得让两个左侧 Task 共用工作树，也不得替代 Task 自身的 `codex/task-*` 交付分支。
- 影响范围：最新 Product Spec、`docs/AGENT_POLICY.md`、AGENTS/README、日常实施与并行 Worktree Skills、实例化/初始化的保留规则、Harness validator、专项回归和当日 Changelog。
- 不包含：不修改 Codex 桌面应用二进制或用户全局设置，不自动 fetch/pull/push，不重命名当前 Harness 源仓库仍在使用的 `master` 分支，不授权 Task 自行合并/删除/发布，也不因 Task 模板存在而自动启动 Subagent、构建或完整验收。
- 风险：错误地把“最新 main”理解为任意当前分支会产生过时或分叉基线；自动提交未审查修改可能混入秘密；Task 自行清理会丢失未整合提交；把 Task 级 Worktree 与 Subagent 单元 Worktree 混为一层会造成分支和所有权冲突。通过 main 缺失/分叉失败关闭、基线审查、独立分支、干净交付、主任务复核和整合后清理顺序降低风险。
- 验证：Harness validator 必须机械检查 Agent Policy 模板、AGENTS/README 入口、实施 Skill 收口、并行 Skill 分层及初始化保留要求；Python 回归覆盖必填描述节、`codex/*`、干净状态、禁止自行合并和主任务清理语义。文档链接和 `.gitignore` 继续由完整 Harness validator 检查。
- 恢复或调整标准：若 Codex 官方 Worktree/branch/Handoff 行为变化，或真实多仓项目证明 `main`、一 Task 一 Worktree、提交粒度或清理顺序不再安全，项目负责人须用新的明确规则说明替代基线、所有权、迁移和恢复条件；不得通过共用 Worktree、复制未提交状态或提前强制删除形成隐式例外。

## ADR-20260827-004：固定详细侧栏 248/76 两态并由 AppShell 同步主内容偏移

- 日期：2026-08-27；状态：Accepted。
- 关联需求：项目负责人明确批准 `sidebar_mode = detailed` 的运行时标准，要求保持 `248px` 展开、`76px` 收起与 `72px`/`44px` Logo，统一使用 `22px`/`1.75` Tabler 菜单图标，禁止套用 compact 运行时或任何旧精简密度，并修复 fixed 侧栏变化后 Mantine AppShell 主内容偏移未同步的缺陷风险。
- 变更标识与所需版本：`change_id = HARNESS-FEAT-DETAILED-SIDEBAR-RUNTIME-SYNC`；`required_version = 202608051301`。这是当前 Harness 未发布时间版本，本决定不自动改变 `Version.md`。
- 设计事实：`sidebar_mode = detailed` 命中 `tauri-gui-sidebar-detailed-v1`。首次默认展开，展开为 `248px`、`72px` Logo、图标左/名称右；收起为 `76px`、`44px` Logo、icon-only，名称只通过 `position=right`、`openDelay=0` 的 Mantine Tooltip 揭示。两态菜单图标固定 `22px`/`1.75`；展开菜单项 `44px` 最小高度和 `sm` 横向 padding，收起左右 padding 与 section margin-inline 为零；身份区使用 Mantine `xs` padding，折叠图标为 `18px` 的 `IconChevronLeft`/`IconChevronRight`。
- 状态与壳层决定：折叠偏好只使用 `app.sidebar.detailed.collapsed`；读取时只有字符串 `"true"` 表示收起，其余状态一律展开。AppShell 用 `readDetailedSidebarCollapsed()` 初始化并拥有状态，以 `detailedSidebarNavbarWidth()` 同时驱动侧栏宽度、Mantine `navbar.width` 与 `data-navbar-width`；`AppSidebarTemplate` 只接收受控状态，并从自身 `ActionIcon` 调用 `onCollapsedChange(nextCollapsed)`。该偏好不得进入 Jotai 页面会话 store、URL、TanStack Query、shared core 或业务持久化。
- 交互决定：折叠事件只属于 `data-testid="app-sidebar-collapse-toggle"` 的 ActionIcon；Logo、版本和身份区父级不代理。展开菜单项标记 `icon-with-label` 并渲染 i18n 名称且不使用 Tooltip；收起菜单项标记 `icon-only`、不渲染可见名称节点并保留完整 `aria-label`。导航仍由实际 NavLink/button 自身拥有点击和 active。Logo 只能使用 `/app-identity/logo.png`，展示版本统一规范为一个小写 `v`。
- 模板与门禁：新增受管 `AppShellTemplate.tsx` 作为 detailed 运行时接线参考，把 detailed 状态从 `AppSidebarTemplate` 内部提升到 AppShell；React 回归覆盖默认 248、身份父级无动作、ActionIcon 后 76 与主区偏移同步、Tooltip 和重挂载恢复；Node GUI 生命周期检查器与 Python Harness 合同拒绝固定 248 偏移、30px detailed 图标、父级代理或缺少回调接线。
- Profile 边界：Harness 根不是已实例化 GUI 产品，因此不创建虚假的 `docs/GUI_APP_PROFILE.md`。下游初始化仍由 `$desktop-prepare-gui-app-identity` 把 `sidebar_mode = detailed` 和 `tauri-gui-sidebar-detailed-v1` 写入真实产品 profile；任何像素或模式偏离必须由用户另行批准，并同步该产品 profile 与 ADR。
- 取代范围：本决定只取代 ADR-20260827-001 对 detailed 实现细节的较粗描述，以及 ADR-20260827-003 中“detailed 保持不变”的保留说明；compact 的 `tauri-gui-sidebar-compact-80-v1`、未选择模式时归一化为 detailed、支持页条件与顺序均不变。
- 理由与风险：fixed 侧栏自行折叠而 AppShell 仍保留旧宽度会留下空白或遮挡内容；双份内部状态也会造成刷新与渲染漂移。单一 AppShell 状态和共享宽度 helper 消除该分叉。风险主要是模板被 compact 产品误接或存储异常；通过固定 detailed wrapper、profile 模式门禁、严格 `"true"` 读取和不可用时展开缓解。
- 验证边界：Harness 仓库没有可启动的下游 Tauri 应用或前端包清单，本次只能运行受管 React 测试源码的结构验证、Python 合同和 Node 生命周期回归，不能把它们声称为真实窗口证据。首次命中该标准的下游初始化仍必须运行相关 Vitest，并在真实本机窗口观察展开横排、收起后主内容扩展和 Tooltip。
- 恢复或调整标准：模式、像素、图标尺寸、存储键、状态所有权或 AppShell 同步方式的任何变化都必须先取得项目负责人明确批准，再同步设计标准、下游 GUI profile、ADR、模板和回归；不得静默改回 compact、80/108/136px detailed 宽度或内部双份折叠状态。

## ADR-20260827-003：建立 UI 设计标准目录并以 80px 精简侧栏取代旧密度

- 日期：2026-08-27；状态：Accepted。
- 关联需求：项目负责人明确批准 Tauri GUI 精简侧栏的 `80/6/36/22/11/1.25/56/4/8` 像素与节奏，要求禁止固定 `em/ch` 名称盒、保持 compact 不折叠，并让 Harness 初始化优先匹配已批准设计标准而非套用旧 `136/56/30/10em` 缺省。
- 变更标识与所需版本：`change_id = HARNESS-FEAT-UI-DESIGN-STANDARDS-CATALOG`；`required_version = 202608051301`。这是当前 Harness 未发布时间版本，本决定不自动改变 `Version.md`。
- 决定：新增 `docs/design_standards/` 作为 UI 标准唯一索引，按 profile、当前请求与批准 ADR 精确匹配；产品已批准规则优先于 Harness 通用缺省。无精确匹配或特殊需求先设计并取得批准，像素/信息架构偏离更新下游 `docs/GUI_APP_PROFILE.md` 与 ADR，不得自行发明密度；规则只进入 GUI adapter 展示/纯交互层。
- compact 决定：`sidebar_mode = compact` 命中 `tauri-gui-sidebar-compact-80-v1`，固定 `80px` 栏宽、`6px` 内容内边距、`36px` Logo、`22px`/`1.75` Tabler 图标、`11px`/`1.25` 全宽居中名称、`56px` 菜单项、`4px` 项内节奏与 `8px` 区间距；NavLink 自身拥有点击/active/aria-label，AppShell navbar 复用宽度常量且 Navbar padding 为 `0`。中性功能菜单只有 i18n“首页”，支持区保持已选赞助→设置→已选关于。
- 保留范围：detailed 的默认 `248px` 展开、`76px` 收起与 `72px`/`44px` Logo、ActionIcon、Tooltip 和独立持久化不变；`sidebar_mode` 未选择仍归一化为 detailed。本决定只取代 ADR-20260827-001 的 compact 像素/排版部分，以及更早 ADR 中仍提及的 `136/56/30/10em` 当前效力。
- 理由：集中路由能阻止 Skills、模板和检查器各自持有不同像素事实；全宽名称消除两字标签在固定短盒内相对图标偏左的已知回归，同时 80px 密度符合当前批准产品标准。
- 风险与缓解：更窄栏可能放大长翻译、文本缩放和 AppShell 偏移问题；通过最多两行、完整 aria-label、无固定盒、结构门禁、组件回归和真实初始化 E2E 复核中心线/裁切降低风险。受管标准目录可升级，但产品 profile/ADR 保持受保护，避免升级覆盖专属规则。
- 验证：React 模板、Python Harness 合同和 Node.js GUI 生命周期检查器同时锁定新数值、全宽居中、无固定 `em/ch` 盒、fixed/`100dvh`/边框、AppShell 宽度/padding 与 detailed 不漂移；本轮只运行相关单元/结构检查，不生成 Tauri 候选或真实 GUI E2E 证据。
- 恢复或调整标准：任何像素、密度、结构或匹配优先级变化都需项目负责人明确批准，并同步新 ADR、设计标准、下游 GUI profile、模板和回归；不得静默恢复旧 compact 密度或把 compact 数值传播到 detailed。

## ADR-20260827-002：新增 Harness 自身记忆历史治理机制，`_history.md` 补充 ADR-20260805-004

- 日期：2026-08-27
- 状态：Accepted
- 关联需求：项目负责人要求为更好地让本项目自举，增加一个不派生到下游的技能，把 ADR 等按日记忆文件中此前仍在沿用的设定和思路整理进一份 `{ADR|Changelog|...}_history.md`，并清理过期部分，便于人类追溯和阅读。
- 变更标识与所需版本：`change_id = HARNESS-FEAT-CURATE-MEMORY-HISTORY-SKILL`；`required_version = 202608051301`。该值是当前 Harness 未发布时间版本，本决定不自动改变 `Version.md`。
- 背景：ADR-20260805-004 把 ADR/Changelog/Work Plan/Product Status/Product Spec 五类按日记忆收敛为只保留日期最新文件、历史交给 Git，理由是同一批决定被跨多个历史日期文件反复复述、检索噪音上升。但 ADR、Changelog 自身的写入规则要求当前最新文件综合携带此前仍然有效的条目（例如当前 `docs/adr/20260827_ADR.md` 累积了 27 条 ADR-YYYYMMDD-NNN，多数标题已标注被更晚条目部分取代），只保留最新文件并不能阻止这份文件本身随时间单调增长；Work Plan、Product Status、Product Spec 三类按各自规则整篇重写为当日完整快照，不逐条累积，没有同类问题。
- 决定：新增 `$desktop-curate-harness-memory` 技能，只治理 Harness 根目录自身的 `docs/adr/`、`docs/changelog/`：当当前最新文件行数超过人工维护文本 500 行建议重构阈值，或项目负责人明确要求时，把已被后续决定完全取代、且在全仓检索确认不再被任何当前规范文档（`AGENTS.md`、`docs/ENGINEERING_RULES.md`、`docs/RUST_CLI_TEMPLATE.md`、`docs/RELEASE.md`、`docs/VERIFICATION.md`、任何 Skill/参考文档）引用的过期条目原文，一字不改追加到同目录 `ADR_history.md`/`CHANGELOG_history.md` 末尾，随后从当前最新文件删除该条目；`_history.md` 只追加、不重写、不删除，是从当前最新文件迁出的完整过期条目原文的永久存档，用于降低当前文件的阅读噪音，同时不依赖 `git log` 即可读到仍有意义的历史叙事。只标注“部分取代”而未被完整取代声明覆盖、或存在任何未判定清楚的条目，一律保守保留在当前文件，不得整条迁移；无法判定的条目在完成报告中列为候选交人工复核，不自动迁移。
- 不适用范围：不改变 ADR-20260805-004 对 Work Plan、Product Status、Product Spec 三类的“只保留最新文件、历史交给 Git”规则；三类文件不产生 `_history.md`。不改变 `docs/verification/` 与 `docs/verification/human_review.md` 的独立保留规则。不追溯要求本次立即对现有 27 条 ADR 条目执行迁移；当前最新 ADR 文件 461 行仍低于 500 行建议阈值，本决定只建立机制，实际迁移留待阈值触发或项目负责人下次要求时按 Skill 规则执行。
- 派生边界决定：`$desktop-curate-harness-memory` 与其可能产生的 `ADR_history.md`/`CHANGELOG_history.md` 一律不进入 `$desktop-instantiate-project` 复制清单，`docs/adr/`、`docs/changelog/` 本身在实例化时已被排除复制；下游从零积累自己的记忆，没有可迁移的历史条目，也不得保留、复制或独立重建该 Skill。
- 理由：一份不断膨胀、包含大量已被部分取代条目的“最新” ADR 文件既不符合 ADR-20260805-004 降低检索噪音的初衷，也让人类维护者难以分辨哪些规则仍在直接约束当前行为；引入只追加的 `_history.md` 作为当前文件的过滤视图，能在不违反“只保留最新文件、历史交给 Git”硬规则的前提下，把降噪目标同时应用到条目级别，且完全不损失可追溯性。
- 风险：机械按日期或标题关键词判断“过期”容易误删仍在依据的字段，尤其是本仓库大量条目采用“部分取代、部分继续有效”的写法；`_history.md` 本身也可能随时间变得很长。通过要求逐字段核对取代声明、无法判定时保守保留并列为候选、只追加不改写、超过 2000 行硬上限时按既有拆分惯例续卷等约束降低风险。
- 验证：新增 Skill 结构与 `agents/openai.yaml` 通过 `python3 scripts/validate_harness.py` 的 Skill/UI 元数据/Markdown 链接检查；`$desktop-instantiate-project` 复制排除清单同步更新并人工核对措辞一致；`docs/adr/README.md`、`docs/changelog/README.md`、`docs/ENGINEERING_RULES.md`、`AGENTS.md` 交叉引用一致，无需新增机械回归即可通过既有 158 项必需文件与链接完整性检查。本轮不产生真实 `_history.md` 文件，机制验证以静态一致性核对与既有 validator 通过为准。
- 恢复或调整标准：若项目负责人认定该机制不必要或产生了新的检索噪音，可通过新 ADR 废止 `$desktop-curate-harness-memory` 并要求已产生的 `_history.md` 内容通过新 ADR 明确处理方式（不得静默删除）；若需要扩展到下游或 Work Plan/Product Status/Product Spec，必须由项目负责人明确批准并记录新的适用范围。除非被新的明确决定替代，本决定持续有效。

## ADR-20260827-001：GUI 初始化改为显式能力选择并以详细侧栏作为缺省模式（五字段 schema 已由 ADR-20260831-005 取代）

- 日期：2026-08-27
- 状态：Partially superseded by ADR-20260831-005；侧栏、托盘、单实例与支持页启用分支继续有效
- 关联需求：项目负责人要求选择 GUI 后再单独询问是否需要系统托盘、关于页、赞助页和单实例，并允许侧栏选择精简或详细模式；用户未选择侧栏时默认采用详细模式。托盘启用时关闭窗口不退出，托盘禁用时关闭最后一个窗口退出；详细侧栏默认展开、可折叠并持久化，折叠后通过 Tooltip 显示名称。
- 变更标识与所需版本：`change_id = HARNESS-FEAT-GUI-INITIALIZATION-CAPABILITY-SELECTION`；`required_version = 202608051301`。该值是当前 Harness 未发布时间版本，本决定不自动改变 `Version.md`。
- 问询与事实源决定：用户选择 GUI 后，初始化器必须进入独立问询轮，逐项解析 `system_tray`、`about_page`、`sponsor_page`、`single_instance` 为 `enabled|disabled`，并提供 `sidebar_mode` 的 `compact|detailed` 选择。四项能力不得根据旧默认、推荐策略预设、页面数量或 Agent 偏好推断；用户省略、留空或跳过侧栏选择时，初始化器必须在写 profile 前确定性归一化为 `sidebar_mode = detailed`。明确选择 `compact` 或 `detailed` 时保持原值；显式非法值不是“未选择”，必须重新确认。归一化后的五项原子写入 `docs/GUI_APP_PROFILE.md` 唯一 `gui-initialization-config` 代码块，初始化基线提交前不得缺失或残留 `pending`；后续 GUI Skill 与检查器只消费完整 profile，不重复推断。
- 托盘与关闭决定：`system_tray = enabled` 时，既有非透明应用图标、稳定双项菜单、运行时 i18n、关闭隐藏、托盘左键/显示项恢复和退出项结束应用的完整契约继续作为硬门禁。`disabled` 时不得启用 `tray-icon` feature、安装托盘、保留托盘菜单/locale 资源或注册关闭隐藏处理；改由 `.on_window_event(...)` 接入主窗口 `CloseRequested` 并显式调用 `AppHandle::exit(0)`，以 `close_last_window_exits_application` 与真实本机关闭场景锁定跨平台关闭退出。
- 单实例决定：`single_instance = enabled` 时，官方 `tauri-plugin-single-instance` 首插件、只恢复既有主窗口的中性回调、两个固定命名回归和真实双启动唯一性继续作为硬门禁。`disabled` 时不得声明依赖、注册插件、保留回调或执行双启动场景。文件关联、深链和启动参数转发仍需以后独立批准并交给 core 判定业务语义。
- 页面与资源决定：动态标题、`/settings`、语言与三态主题、亮暗语义主题是所有 GUI 的固定基线。`about_page`、`sponsor_page` 各自决定对应路由、导航入口、React 组件和运行时资源；禁用时不得保留隐藏路由、不可达组件或无入口媒体。选择关于页时保留检查更新、近五版更新日志、作者/联系人/免责声明及既有零出站边界；选择赞助页时完整 sponsor 媒体进入 bundle，禁用时这些媒体只保留在 Harness Skill 源资产而不进入下游运行时 bundle。
- 侧栏决定（compact 像素/排版已由 ADR-20260827-003 取代）：`compact` 原固定为 `136px`、`56px` Logo、`30px` 图标与 `11px`/`10em` 名称；该旧密度不再具有当前效力。`detailed` 首次默认 `248px` 展开，使用 `72px` Logo 与图标+名称横排；拥有该动作的 ActionIcon 可收起为 `76px`，收起后使用 `44px` Logo、只显示图标并通过 Mantine Tooltip 显示名称。详细模式折叠偏好使用独立 local-storage 键跨重挂载和下次启动恢复，不进入页面会话 Jotai store；菜单仍具有完整可访问名称、选中态和焦点。
- 导航决定：产品功能项从顶部向下增长，底部按“已选赞助、固定设置、已选关于”的视觉顺序生成。未选页面既不占空位也不保留不可达入口。两种侧栏均直接使用 `@tabler/icons-react` 命名组件，Logo、图标和文字沿同一中心线且无裁切。
- 验证决定：`verify-gui-lifecycle-contract.mjs` 必须解析五项 profile，对启用能力验证原有完整结构与有断言命名回归，对禁用能力验证依赖、feature、插件、运行时、路由与资源缺席。`$desktop-test-gui-initialization-e2e` 仍在唯一基线提交前固定运行，但只在单实例启用时双启动、只在托盘启用时操作真实托盘；托盘禁用时实测关闭最后窗口退出。所有组合都验证所选侧栏、默认设置页、实际菜单页面可达和未选页面缺席。无法判定或观察某个适用场景仍失败关闭。
- 取代范围：本决定取代 ADR-20260825-008 的“固定 `136px` 单态侧栏”、ADR-20260825-007 的“所有 GUI 强制单实例”、ADR-20260825-006 与 ADR-20260824-004 的“所有 GUI 强制托盘/关闭隐藏”、ADR-20260825-005 的无条件双启动/托盘 E2E、ADR-20260825-002 的默认收起侧栏、ADR-20260825-001 的统一可折叠侧栏与固定支持路由，以及 ADR-20260820-002/ADR-20260824-004 的赞助媒体默认进入全部 GUI bundle。上述 ADR 中关于能力启用后的实现质量、安全、i18n、品牌事实、更新/统计零出站、主题、图标、真实宿主证据和 core-first 的约束继续有效。
- 理由：托盘、单实例和支持页会改变进程生命周期、依赖、包体与信息架构，不应由 Harness 替所有产品静默决定，因此四项能力继续要求显式选择。侧栏未选择不应阻断中性初始化；详细模式保留可见名称，同时仍可折叠获得空间效率，适合作为确定性缺省值，精简模式继续供低菜单数量工具显式选择。启用分支的严格门禁和禁用分支的反向无残留校验保持不变，避免“可选”被误读为弱实现。
- 风险与缓解：缺省规则若散落在多个消费者中可能造成行为漂移，显式非法输入若被误当作空值会掩盖配置错误；能力组合增多会扩大测试矩阵，localStorage 可能不可用或损坏，托盘禁用会改变用户习惯的退出语义。通过只在初始化器归一化、最终固定五字段 schema、非法值重新确认、组合静态检查、只执行适用真实场景、存储失败回到默认展开、独立 `aria-label`、关闭语义命名回归和禁用能力残留扫描降低风险。首次真实 GUI 下游的各组合仍保持 `Unverified`，不得以 Harness 模板测试冒充前向证据。
- 验证：Harness 模板增加条件导航与两种侧栏回归；Node.js 生命周期检查器增加 profile 解析、托盘/单实例启用与禁用分支、缺失/非法配置及关闭语义负向测试；Python Harness validator 与初始化主契约测试同步锁定问询、profile、模板、文档和 Skill。当前 Harness 不包含已实例化下游 GUI，因此本轮不运行真实 Tauri 组合 E2E；首次下游初始化仍必须按 profile 完成真实门禁。
- 恢复或调整标准：若真实下游证明五项粒度不足、某组合在目标平台不可支持或侧栏尺寸/持久化不满足可用性，项目负责人可通过新 ADR 调整字段、兼容矩阵、缺省模式、尺寸或存储实现；不得恢复静默默认任何一项能力、侧栏未选后仍缺失/`pending`、显式非法值被静默接受、禁用能力残留、托盘禁用仍关闭隐藏、详细模式默认收起或折叠后没有可发现/可访问名称。

## ADR-20260826-004：GUI 页面工作状态只在应用进程内跨路由保留

- 日期：2026-08-26
- 状态：Accepted
- 关联需求：项目负责人要求 GUI 中可交互选择的选项卡、查询项、分页等状态在路由和页面切换后仍可继续使用；返回后分页状态若查询不到数据则回到第一页；这些状态只在程序本次打开期间有效。
- 变更标识与所需版本：`change_id = HARNESS-FEAT-GUI-PROCESS-SESSION-STATE`；`required_version = 202608051301`。该值是当前 Harness 未发布时间版本，本决定不自动改变 `Version.md`。
- 状态所有权决定：活动选项卡、已应用查询/筛选、排序、页码/每页数量及同类页面工作状态属于 GUI adapter 的纯交互状态，由应用根 Jotai store 的页面级模块 atom 持有。atom 在页面模块顶层稳定创建，Provider/store 位于不会随 route unmount 重建的应用壳；切换路由以及已启用的关闭隐藏/单实例唤醒沿用同一 store，真正退出后下一次程序运行使用新 store 和默认值。
- 生命周期决定：页面会话 atom 不使用 `atomWithStorage`、localStorage、sessionStorage、IndexedDB、Tauri Store、配置文件、数据库或 URL，不形成跨进程恢复。语言、主题与 ADR-20260827-001 批准的详细侧栏折叠偏好按各自独立契约持久化，侧栏偏好不进入页面会话 atom；TanStack Query 继续拥有异步数据/缓存，shared core 和持久层继续拥有权威业务状态，atom 不复制这些数据。
- 分页决定：查询条件、活动数据范围或每页数量变化时立即把页码设为 1。路由返回后按保存的控件状态查询；只在查询成功、当前页大于 1 且结果为空时把页码原子改为 1，由变更后的 query key 重查一次。loading、取消、超时或 error 不视为空结果；第 1 页为空时保持空态，不重复触发。恢复时失效的枚举选择回到当前安全默认。
- 模板决定：共享 GUI React 资产增加 `pageSessionState.ts` 和独立 `PageSessionState.test.ts`，作为下游页面状态 atom 与空页协调的基线；具体产品页面仍按真实字段定义自己的 tab/query 类型，不把 helper 扩张成业务状态容器。
- 验证决定：同一应用根 store 必须证明 tabs、query/filter、sort、page/pageSize 在 route unmount/remount 后保持；新 store 必须从默认值开始；另覆盖成功空页回退、loading/error 不回退和第 1 页空数据不循环。validator 同时拒绝删除 helper 的进程内约束或加入页面会话持久化 API。
- 影响范围：AGENTS/README、工程规则、Rust/GUI/React/Mantine 基线、GUI adapter/支持界面/日常实施 Skills，共享 React 资产与测试、Harness validator、最新 Product Spec 和当日 Changelog。当前不创建真实下游产品页面、不运行生产构建/E2E，也不改变设备级语言/主题偏好。
- 风险与缓解：过度持久化会在下次启动泄漏陈旧工作上下文，因此只使用内存 store；把失败误判为空会隐藏错误，因此只响应 success；陈旧页码可能产生二次查询，因此回退仅从大于 1 到 1 且第 1 页终止。页面 atom 若在组件内创建仍会丢失，所以模板和回归固定模块顶层与应用根 store 所有权。
- 恢复或调整标准：只有项目负责人明确要求某类页面状态跨进程、可分享 URL 或由 core 持久化，并给出数据生命周期、隐私、迁移与失效规则时，才以新 ADR 调整该类状态；不得通过局部 localStorage、URL search 或 Query 数据镜像形成隐式例外。

## ADR-20260826-003：控件自行拥有交互，发布候选携带近五版日志并统一可见版本前缀

- 日期：2026-08-26
- 状态：Accepted
- 关联需求：项目负责人要求页面交互事件绑定到元素自身，禁止通过父级点击切换表格内 `Switch`；关于页在“检查更新”旁增加可查看近 5 次内容的“更新日志”；每次发布前汇总上一次正式发布到当前版本的功能优化与问题修复并只保留近 5 版；所有用户可见版本号统一带 `v`。
- 变更标识与所需版本：`change_id = HARNESS-FEAT-INTERACTION-RELEASE-NOTES-VERSION-DISPLAY`；`required_version = 202608051301`。该值是当前 Harness 未发布时间版本，本决定不自动改变 `Version.md`。
- 事件归属决定：按钮、链接、`Switch`、`Checkbox` 和菜单项等语义控件自行绑定 handler；Card、`Table.Tr`、`Table.Td` 等父级不得代理子控件动作。父级确有独立动作时只能执行自身语义并隔离冲突传播；测试分别点击控件与周围父级区域，表格行或单元格点击不得切换行内 `Switch`。这项规则适用于所有页面，不只关于页示例。
- 关于页决定：在 ADR-20260827-001 选择启用关于页时，“检查更新”与“更新日志”作为相邻但独立的 Button，各自拥有事件。更新区 Paper/Group 不注册这两个动作；`NotConfigured` 只禁用远程检查，本地更新日志仍可打开。弹窗按最新在前显示至多 5 版，每版功能优化与问题修复各至多 10 条。未选择关于页时不建立隐藏入口。
- 发布日志决定：根 `release-notes.json` 是候选包和 GUI 关于页共用的结构化发布日志事实。`$desktop-prepare-release` 在候选构建前从上一次真实正式发布版本及 40 位提交到当前发布源码筛选最重要的用户可见变化；首发从仓库起点计算，无法可靠确定边界时返回 `Not ready`。普通缺陷即使不触发项目 Changelog，也进入当前版本的“问题修复”。
- 格式决定：每版两类合计至少一条，两类分别最多 10 条，空分类显示“无”；固定渲染标题为 `-----------更新日志 {发布日期} {发布版本}----------`，分类标题为 `###功能优化` 和 `###问题修复`。文件使用固定 schema、规范 ISO 日期、唯一版本、最新在前、最多 5 版、拒绝符号链接/非普通文件并通过同目录暂存原子替换。
- 候选绑定决定：构建 Skill 在任何测试或编译前只读校验日志最新版与当前机器版本并计算 SHA-256，把根文件同一字节作为 `release-notes.json` 打入 CLI 归档或 GUI 应用资源；manifest 记录 `releaseNotesVersion`、`releaseNotesSha256`、`releaseNotesPath`。构建、收集、验收和发布就绪复核不得生成、补写、重排或截断日志；候选形成后任何日志变化都要求新提交、新构建和重新验收。Harness 升级把该文件视为 `protected`。
- 版本展示决定：所有面向人的版本号先移除已有 `v`/`V` 前缀，再补且只补一个小写 `v`，覆盖窗口标题、侧栏、设置/关于页、更新状态、CLI `--version` 与更新日志。Cargo、JSON/协议、状态和 manifest 的机器 `version` 保持不带展示前缀的原始值，避免破坏 SemVer 解析或既有机器契约；只有明确命名的 `releaseNotesVersion` 使用展示值。
- 实现边界：GUI 模板使用共享 `formatDisplayVersion`，发布准备使用无第三方依赖的标准库 `release_notes.py`，关于页只消费本地候选事实，不把远端自由文本更新说明当作发布日志权威。CLI Clap 版本输出使用带 `v` 的展示形式，结构化 CLI 版本字段仍保持原始值。
- 影响范围：工程规则、AGENTS/README、CLI/Rust/GUI/发布文档，CLI/GUI 初始化资产，GUI 支持模板与 i18n，发布准备/构建/跨平台/收集 Skills，候选 workflow、升级所有权、Harness validator、最新 Product Spec 与当日 Changelog。当前不构建或发布候选，也不创建根日志占位文件；下游首次真实发布准备时按事件创建。
- 风险与缓解：父级与子级都含动作时可能发生传播冲突，因此默认禁止父级代理并要求双区域回归；展示前缀可能污染机器版本，因此格式化只位于用户边界；发布日志可能与候选漂移，因此构建前锁定摘要、包内逐字节核对并在候选后禁止改写；同日多个版本只能依赖列表顺序，维护脚本始终把当前版本置顶。
- 验证：发布日志 helper 覆盖格式、单 `v`、5/10 上限、同版本替换、畸形 schema、版本不一致和符号链接拒绝；GUI 模板回归覆盖两个按钮自身事件、父容器无动作、5/10 裁剪与 `v` 归一化；候选 workflow 回归覆盖构建前校验、两平台打包、包内字节、manifest 字段和顺序；CLI 资产回归覆盖 `--version` 的 `v` 前缀；Harness validator 锁定文档、Skills、升级保护与模板资产。
- 恢复或调整标准：只有项目负责人依据真实交互可用性或发布渠道格式要求明确新的事件语义、日志保留/分类/格式或版本展示规则时，才以新 ADR 取代本决定；不得通过行级点击代理、远端自由文本、候选后补写或机器版本加 `v` 形成隐式例外。

## ADR-20260826-002：按 Rust 与前端代码类型收紧文件行数门禁并固定模块目录结构

- 日期：2026-08-26
- 状态：Accepted
- 关联需求：项目负责人明确要求把 Rust 代码治理收紧为 400 行建议重构、800 行强制拆分，把前端代码治理收紧为 500 行建议重构、1000 行强制拆分，并特别保持合理文件结构与 Rust `<module>/mod.rs` 目录模块组织。
- 变更标识与所需版本：`change_id = HARNESS-FEAT-TIERED-CODE-LINE-LIMITS`；`required_version = 202608051301`。该值是当前 Harness 未发布时间版本；下游自动 SemVer 不适用于 Harness 自身，本决定不自动改变 `Version.md`。
- Rust 决定：`.rs` 文件 400 行及以内不因行数触发专项复核；401 至 800 行列为非阻断建议重构候选，并按高内聚、职责单一和职责相近性复核是否保留；800 行通过，801 行起机械失败并强制拆分。多文件模块统一使用 `<module>/mod.rs` 作为目录入口，把职责相近的子模块置于同一目录；不得保留同级 `<module>.rs` 与同名目录，不得创建空壳转发层或压缩可读性规避门禁。
- 前端决定：现代前端源码后缀 `.astro`、`.cjs`、`.css`、`.cts`、`.html`、`.js`、`.jsx`、`.less`、`.mjs`、`.mts`、`.sass`、`.scss`、`.svelte`、`.ts`、`.tsx`、`.vue` 使用 500/1000 配置；500 行及以内不触发专项复核，501 至 1000 行是非阻断建议重构候选，1000 行通过，1001 行起机械失败。候选自动列出时点已由 ADR-20260908-006 改为当次发布审查启用时；前端按功能、页面或稳定能力职责建立高内聚目录，不强制 `index.ts` 桶文件，也不得用 `export *` 隐藏真实来源。
- 通用文本决定：其他 UTF-8 人工维护文本继续沿用 ADR-20260806-003 的 500 行建议区间与 2000 行硬上限；日常强制语义复核已由 ADR-20260908-006 取代，仅在当次发布审查启用时集中执行。工具生成且禁止手工编辑的锁文件/生成物、原样内嵌且明确隔离的第三方文件继续按封闭范围排除；生成器、模板和维护说明仍按自身文件类型受门禁。
- 自动化决定：唯一行数检查器按后缀选择配置，并为每个候选或违规输出稳定 `profile`、建议阈值和硬上限。软阈值只产生可见复核提示；任一硬超限或检查器错误返回非零状态。Rust 硬超限的人类可读诊断必须同时指出 `<module>/mod.rs` 结构，不把单纯缩行误报为合格拆分。
- 取代范围：本决定只取代 ADR-20260806-003 对 Rust 与前端代码使用统一 500/2000 的部分；该决定的通用文本 500/2000、职责语义复核、生成物封闭排除和物理行计数继续有效。ADR-20260805-002 的历史 400 行全文件硬上限继续保持被取代状态。
- 影响范围：最新 Product Spec、AGENTS/README、工程规则、Rust 基线、初始化/实施/重构/i18n Skills、Harness 方法论与升级传播、统一行数检查器、Harness bridge/validator、专项回归、TECH_DEBT 当前说明和当日 Changelog。日常开发是否自动运行全仓治理检查、core-first、中文注释、发布与验收门禁不变。
- 理由：Rust 模块通过明确的目录入口可以在较低规模时按领域职责稳定拆分；前端页面和组件通常包含相邻视图、状态与样式，保留稍高的建议阈值更符合其结构。分层硬上限能阻止“语义复核”把代码文件无限保留，同时避免把文档、规格和 Skill 一并机械压到代码阈值。
- 风险与缓解：后缀分类会把同后缀的 Node.js 工具代码纳入前端配置，这是有意的确定性代码治理范围；新增后缀必须更新事实源和边界回归，不能按任意目录临时豁免。过早拆分可能产生碎片，因此建议区间仍以职责判断为准；硬超限拆分也不得创建无职责的文件或桶层。
- 验证：覆盖 Rust 400/401/800/801、前端 500/501/1000/1001、通用文本 500/501/2000/2001，检查报告 profile/阈值/上限、Rust `mod.rs` 诊断、Git 可见范围、生成锁文件排除、CLI 0/1/2 退出码和 Harness bridge；运行相关 Python 专项回归、完整 Harness validator、现行阈值引用扫描、Skill quick validator 与差异检查。
- 恢复或调整标准：只有项目负责人依据真实审阅成本或职责拆分证据明确新的语言配置、边界值和迁移方式时才能调整；不得通过路径豁免、ADR 例外、缩行或空壳模块放宽 800/1000 硬上限。

## ADR-20260826-001：下游采用按完成事件提交、按正式发布周期去重的自动语义化版本门禁

- 日期：2026-08-26
- 状态：Accepted
- 关联需求：项目负责人要求把自动版本规则整理为 Agent 可执行门禁/Skill：新功能提升版本但同一正式发布前不重复提升，独立缺陷修复逐次提升且同一缺陷不重复；Major 由用户决定、Minor 由功能决定、Patch 由缺陷修复决定，分量范围为 `0..100`。经逐项确认，本规则只适用于下游产品，Harness 继续使用时间版本；首功能执行 `MINOR+1` 并归零 Patch；只有真实正式发布成功才开启下一周期；重复缺陷以稳定 ID 去重，发布后回归必须使用新 ID；查询、诊断、复现、重构等非需求/修复完成操作不提升。
- 变更标识与所需版本：`change_id = HARNESS-FEAT-DOWNSTREAM-AUTO-VERSIONING`；`required_version = 202608051301`。该值是当前 Harness 未发布时间版本，不表示下游 SemVer，也不触发 Harness 自动提升；下游每项变化的 `required_version` 由新门禁按当时周期状态计算。
- 决定：新增持久 `$desktop-manage-version`。下游根 `Cargo.toml` 的 `[workspace.package].version` 继续作为当前版本唯一事实源；受保护的 `.harness/version-state.json` 只保存周期基线、当前目标、首功能是否已提升、待发布变化、历史缺陷 ID 和最近发布绑定。helper 使用 Python 标准库、稳定 JSON 输出和失败关闭语义，不引入第三方或全局依赖。
- 版本决定：只接受稳定三段 `MAJOR.MINOR.PATCH`，三个分量均在闭区间 `0..100`；溢出不进位并请求用户决定。Major 必须由用户批准高于当前值的精确目标，写为 `N.0.0`，并视为本周期已包含功能提升。一个正式发布周期内首个已完成功能执行 `MINOR+1` 且 `PATCH=0`，后续功能只记录所需版本、不再次因功能提升；每个新稳定缺陷 ID 的已完成修复执行 `PATCH+1`，相同 ID 的重试、补充修改和跨发布重复都幂等，真实回归使用新的回归 ID。
- 非提升决定：缺陷查询、诊断、复现、未完成或重复修复尝试，以及行为保持重构、测试补强、文档、格式和内部清理统一分类为 `maintenance`，不改变 Cargo 或状态。`plan` 始终只读；只有实现完成且本次相关测试通过后才能 `apply`。普通构建、`pending` 候选、候选收集、验收与发布准备只执行一致性检查，绝不补升或重置。
- 发布决定：只有真实渠道正式发布成功且精确版本/40 位源码提交已有证据后才能 `finalize-release`。它清空待发布变化、允许下一周期的首功能再次升 Minor，但保留历史缺陷 ID。构建成功、候选 `accepted`、标签/上传尝试、失败、取消或超时均不得调用该动作。
- 文档决定：Product Spec、ADR、Changelog 或 Work Plan 仅在自身触发条件独立满足时记录稳定 `change_id` 与门禁返回的 `required_version`；版本提升本身不创建这些记忆。早期变化记录的是其最低所需版本，最终发布版本可因后续变化更高。普通缺陷修复和维护排除规则保持不变。
- 影响范围：最新 Product Spec、AGENTS/README、工程与发布规则、Rust 初始化基线、产品定义/计划/实施/初始化/构建/收集/验收/发布准备/升级 Skills、升级所有权、Harness validator、版本 helper/专项回归和当日 Changelog。Harness `Version.md`、时间版本规则、产品业务 core、接口契约和发布授权不变。
- 理由：把提升时点绑定到“变化已完成且测试通过”避免查询或失败尝试消耗版本；把首功能状态绑定到正式发布周期符合“下次发布前不再增加”；稳定缺陷 ID 让重复修复可机械去重，而新的回归 ID 保留独立修复的可追踪 Patch；Cargo 与周期状态分责避免出现两个当前版本来源。
- 风险与缓解：错误分类可能漏升或误升，因此分类会改变产品范围或版本且存在二义性时必须询问；状态与 Cargo 漂移、符号链接、损坏 JSON、ID 跨类别复用、未批准 Major、非稳定 SemVer和溢出全部失败关闭。两文件提交无法获得文件系统级事务时，helper 在状态写失败后恢复 Cargo 原字节，并由后续 `check` 拒绝任何残留漂移。历史缺陷 ID 会增长，但它是实现全周期去重所需的有界标识集合，未来只有真实规模证据才能引入压缩/归档方案。
- 验证：标准库专项测试覆盖首功能/后续功能、Minor 归零 Patch、独立/重复缺陷、维护/只读 plan、正式发布重置与缺陷 ID 保留、新回归 ID、Major 用户批准、分量溢出、Cargo/状态漂移与不稳定版本拒绝；Harness validator 锁定初始化、开发、两类构建、跨平台、收集、验收、发布准备、项目记忆和升级保护接线。
- 恢复或调整标准：若真实下游证明发布周期边界、ID 来源或版本上限需要改变，项目负责人必须明确新的确定性规则、已有状态迁移、重复判定和发布失败行为，并以新 ADR 取代本决定；不得通过手工编辑 Cargo/状态或在构建阶段补版本形成隐式例外。

## ADR-20260825-008：GUI 初始化固定单态侧栏、精简设置页并把托盘 i18n 升级为运行时门禁（侧栏固定性已由 ADR-20260827-001 取代）

- 日期：2026-08-25
- 状态：Accepted
- 关联需求：项目负责人依据已初始化下游的截图反馈三项问题：系统托盘直接显示 `tray.show_window`/`tray.quit` 原始键；设置页默认出现不必要的隐私/使用情况统计区块；左侧菜单不应再展开，而应保持当前紧凑形态并改为较宽、较大图标、图标在上文字在下且文字居中，约预留十个汉字宽度。截图只用于说明初始化结果，不代表 Harness 自身界面。
- 托盘 i18n 决定：`show_window` 与 `quit` 继续只作为稳定事件 ID，不得作为可见标签。托盘创建或更新路径必须直接通过 `rust_i18n::t!("tray.show_window")` 与 `rust_i18n::t!("tray.quit")` 消费当前规范化 locale；中文精确显示“显示窗口/退出”，英文精确显示“Show Window/Quit”，不支持的 locale 回退英文。设置页语言变化必须通知 Rust GUI adapter 并在不重启应用的情况下刷新已安装托盘菜单；任何 `tray.*` 原始键、混合语言或只有重启后才生效都阻断初始化。
- 托盘门禁决定：在既有三个托盘生命周期回归上增加 `tray_labels_resolve_for_supported_locales`、`tray_labels_fall_back_to_english`、`language_change_updates_tray_menu_labels` 三个固定命名且必须含真实断言的回归；与两个单实例回归合计八个固定命名回归。`verify-gui-lifecycle-contract.mjs` 必须检查托盘安装函数中两项 `rust-i18n` 解析、双语资源和新增回归，拒绝把稳定 ID/原始翻译键作为菜单标签。初始化 Computer Use E2E 必须从真实设置页依次切换中文与英文，在不重启应用时逐次打开真实托盘并核对上述精确标签和无原始 key，再继续验证关闭隐藏、恢复与退出生命周期。
- 设置页决定：初始化固定设置页只展示当前应用/版本、中英文语言选择及浅色/深色/跟随系统三态主题。默认模板、props、翻译资源、回归和真实初始化界面都不得包含隐私标题、统计同意开关、未配置统计占位或对应固定翻译键。统计能力仍是可选的产品级出站能力；只有产品明确启用并记录产品、隐私与出站事实后，才能建立独立产品级明确同意界面，不能恢复初始化设置页中的固定隐私区块。
- 侧栏决定：初始化侧栏固定为 `136px` 宽的单一状态，不建立 `collapsed` 状态、Jotai 折叠 atom、展开/折叠按钮或 Tooltip-only 名称。顶部按 DOM 顺序显示 `56px` 选中 Logo 与权威当前版本；每个产品功能项和赞助/设置/关于固定项使用 `30px` Tabler 图标，固定图标在上、名称在下，名称使用 `11px` 字号、`10em` 行内宽度、水平居中和最多两行，并保留不截断的完整可访问名称。功能项仍从顶部向下增长，底部视觉顺序仍为赞助、设置、关于；主内容固定偏移 `136px`，1440×900 主窗口继续同时容纳侧栏和三张赞助档位卡。
- 取代范围：本决定取代 ADR-20260825-001/-002/-004/-005 中关于默认可折叠/默认收起侧栏、折叠 Tooltip、展开 248px 宽度和初始化设置页统计同意的现行部分，并收紧 ADR-20260825-006/-007 的托盘资源门禁；单实例、托盘可见图标、关闭隐藏/恢复/退出、三态主题、关于页更新、赞助页和可选统计传输的安全边界继续有效。
- 影响范围：最新 Product Spec、AGENTS/README、Agent Policy、工程规则、Rust/GUI/React/Mantine 基线、实例化/初始化/GUI adapter/GUI 支持/GUI 初始化 E2E Skills 及元数据、React 模板与双语资源、GUI 生命周期结构检查器及专项回归、Harness validator/治理回归和当日 Changelog。非 GUI 接口、业务 core、发布签名、最终候选 E2E 选择和可选统计传输协议不变。
- 理由：原始翻译键可见说明“资源存在”不足以证明原生菜单实际解析并响应当前语言；运行时解析、断言回归和真实双语切换共同关闭该漏检路径。初始化设置页不应为默认禁用的可选出站能力占用空间或暗示产品正在采集数据。固定单态的图标上文字下导航减少无价值状态和交互，较宽侧栏与显式文字又比纯图标/Tooltip 更易识别，并保留足够的中文标签宽度。
- 风险与缓解：`10em` 只是约十个当前字体字符的布局预算，不保证任意语言单行容纳，因此允许最多两行并保留完整 `aria-label`；真实窗口 E2E 继续检查裁切、中心线和可访问名称。运行时更新原生菜单需要 GUI adapter 持有菜单项或安全重建菜单，必须由应用生命周期拥有且不得重复增加菜单项。严格双语托盘检查会阻断无法观察系统托盘的宿主，这是既有有意失败语义。
- 验证：Harness 侧模板/validator 回归锁定 `136px`、`56px`、`30px`、`11px`、`10em`、图标上文字下、无折叠控件和默认设置页无隐私/统计内容；Node.js 生命周期检查器专项回归锁定两项 `rust-i18n` 调用、新增三个有断言测试，并验证原始 key 与空断言回归都会失败。当前 Harness 不包含已实例化下游 GUI，因此本轮不构建或操作真实 Tauri 托盘；首次后续 GUI 初始化必须按更新后的 E2E 提供真实中英文刷新、固定侧栏和精简设置页前向证据，其他宿主保持 `Unverified`。
- 恢复或调整标准：只有项目负责人基于真实下游可用性证据明确改变导航或默认设置范围，并以新 ADR 同步尺寸、文本溢出、无障碍、模板与 E2E 时，才能调整单态侧栏或重新引入默认设置项。若 Tauri/rust-i18n API 演进，可替换菜单刷新实现，但不得恢复原始 key 可见、只验证资源文件、语言切换必须重启或默认统计同意区块。

## ADR-20260825-007：GUI 初始化强制单实例并以真实双启动锁定唯一主进程与主窗口（强制默认已由 ADR-20260827-001 取代，启用分支继续有效）

- 日期：2026-08-25
- 状态：Accepted
- 关联需求：项目负责人在把系统托盘提升为强制初始化约束后，进一步要求所有新初始化的 GUI 同时是单实例应用。
- 背景：既有 GUI 基线已经固定关闭隐藏、托盘恢复和显式退出，但没有阻止用户再次启动同一应用时形成第二个长期应用进程或第二个主窗口。只写“单实例”文字也无法确认插件注册顺序、第二进程退出、既有窗口唤醒和进程/窗口唯一性，因此需要与托盘相同的结构、回归和真实宿主三层门禁。
- 官方能力依据：采用 Tauri 2 官方 `tauri-plugin-single-instance`。官方文档说明该插件确保同一应用只运行一个实例，第二次启动会触发既有实例回调并由插件关闭新实例；插件必须最先注册，回调可取得 `app`、启动参数与工作目录。Windows、macOS、Linux 均受支持；Linux Snap/Flatpak 因沙箱限制还需为基于会话 DBus 的服务声明 own/talk 权限（[Tauri Single Instance](https://v2.tauri.app/plugin/single-instance/)，核对日期：2026-08-25）。
- 依赖与顺序决定：根 `[workspace.dependencies]` 是 `tauri-plugin-single-instance` 版本与来源的唯一事实源，声明经过最低直接版本解析和项目 MSRV 测试的兼容稳定下界；GUI member 只使用 `workspace = true`。插件必须是 Tauri Builder 的首个 plugin，不能放到其他插件之后，也不能仅在前端模拟互斥。
- 生命周期决定：同一用户会话再次启动相同应用身份时，第二进程必须通知既有实例后在限定时间内退出。回调只调用 GUI adapter 内共享的 `restore_main_window`，对既有 `main` 窗口依次执行显示、取消最小化和聚焦，不创建第二个主窗口，不替换第一主进程。托盘左键与 `show_window` 同样复用该恢复函数。中性脚手架必须忽略且不得记录第二次启动的参数和工作目录，不得把它们解释为文件打开、深链或其他产品动作；以后确需该能力时必须独立批准，并由 core 判定业务语义。
- 结构与回归门禁：把 ADR-20260825-006 引入的托盘检查器扩展并改名为 `verify-gui-lifecycle-contract.mjs`。它除原托盘契约外，还失败关闭地检查 workspace/member 的单实例依赖、插件初始化、首插件顺序、只恢复已有窗口的中性回调，以及 `single_instance_plugin_is_registered_first`、`second_launch_restores_existing_main_window` 两个固定回归；与三个托盘回归合计五个固定命名生命周期回归。检查器继续只使用 Node.js 标准库，并拒绝路径越界、源码符号链接、非法 UTF-8 和空源码。
- 真实双启动门禁：`$desktop-test-gui-initialization-e2e` 构建本次 debug/no-bundle 二进制并持有第一次启动句柄后，先关闭隐藏主窗口，再以独立受管子进程启动同一精确二进制。第二次启动必须在 15 秒内退出；第一 PID 必须持续，原主窗口身份必须不变且恢复、取消最小化并聚焦。宿主进程与窗口枚举必须只发现一个长期应用主进程和一个主窗口；WebView/系统 helper 不计为第二应用主进程。随后继续执行真实托盘生命周期、侧栏和菜单路由检查，并回收两个子进程句柄。
- 失败语义：缺少依赖、首插件顺序、回调、任一固定命名回归，第二进程残留、第一进程被替换、出现第二主窗口、窗口未恢复聚焦、进程/窗口唯一性无法可靠判定，或双启动无法执行，都阻断初始化能力裁剪和唯一基线提交。不得因 Draft、宿主限制、小型应用或 `milestone_e2e` 值跳过、降级或记为 `Not run`。
- 渠道边界：本机非沙箱调试证据只证明当前宿主。同一应用身份的范围以当前用户会话为准，不声称跨用户全局互斥。Snap/Flatpak 必须在对应打包清单声明插件服务所需的会话 DBus own/talk 权限，并在真实沙箱最终候选重验；缺少渠道清单或候选证据时该渠道保持 `Unverified`，不能拿普通 Linux 调试结果替代。
- 影响范围：最新 Product Spec、AGENTS/README、Agent Policy、工程规则、Rust GUI 基线、验证矩阵、实例化/初始化/GUI adapter/GUI 初始化 E2E Skills 及其元数据、GUI 生命周期结构检查器与专项回归、Harness validator/治理回归和当日 Changelog。非 GUI 初始化、自动启动、文件关联/深链、发布签名及最终候选 E2E 选择规则不变。
- 理由：官方插件提供跨平台互斥与第二启动通知，但只有首插件顺序才能避免其他插件抢先产生副作用；共享恢复函数让单实例与托盘保持一致的窗口可达性语义。结构检查快速阻止漏接线，固定回归锁定顺序和回调，真实双启动证明最终二进制的进程/窗口行为，三层证据共同防止“声明单实例但仍可开两个窗口/进程”。
- 风险与缓解：不同宿主的进程树和窗口枚举能力不同，严格唯一性判断可能让无可观察能力的 runner 无法初始化；这是有意失败，应转到可枚举真实应用主进程与窗口的宿主。Linux 沙箱需要额外 DBus 配置，按渠道独立验证。回调不使用参数/工作目录，避免中性脚手架提前建立未经批准的外部输入契约或把敏感路径写入日志。
- 验证：Node.js 专项回归覆盖完整单实例/托盘契约，以及缺失 workspace 单实例依赖、插件未最先注册、缺少单实例命名回归等失败路径。Harness Python 治理回归与 validator 锁定新文件名、依赖/首插件/五个回归和真实双启动场景。本 Harness 不包含已实例化下游 GUI，因此本轮不构建或双启动真实 Tauri 应用；首次后续 GUI 初始化必须提供真实前向证据，其他宿主和 Snap/Flatpak 保持 `Unverified`。
- 恢复或调整标准：只有项目负责人明确改变产品/平台范围，并以新 ADR 记录替代的多实例交互、窗口所有权、外部参数语义、渠道风险与真实候选证据时，才能调整或移除单实例基线。若 Tauri 官方 API、进程模型或沙箱权限要求变化，可依据官方文档与真实下游证据替换实现/探测方法，但不得恢复只写文档、插件非首位、无法判定也通过或静默多实例。

## ADR-20260825-006：GUI 系统托盘改为不可省略的结构与真实生命周期双门禁（不可省略默认已由 ADR-20260827-001 取代，启用分支继续有效）

- 日期：2026-08-25
- 状态：Accepted
- 关联需求：项目负责人发现新初始化的 GUI 项目再次丢失系统托盘，要求把托盘及其窗口生命周期提升为强制约束，不能再由初始化实现自行省略。
- 背景：既有 Product Spec、GUI adapter 与初始化说明已经描述 `tray-icon`、显示/退出和关闭隐藏，但基线提交前的真实 GUI E2E 只验证应用启动、侧栏居中和菜单路由，没有检查托盘图标、原生托盘菜单或关闭隐藏/恢复/退出生命周期。因此生成项目即使没有实际托盘，仍可能通过既有门禁并提交；问题属于可执行验收覆盖缺口，不是缺少文字约束。
- 强制实现决定：任何选择 GUI 的初始化都必须在根 Tauri 依赖启用 `tray-icon`，并从用户所选母版通过项目本地 Tauri `icon` 命令生成 `src-tauri/icons/32x32.png`。该文件必须是普通非符号链接、32×32 8-bit RGBA 非交错 PNG、至少含一个非透明像素，且被 `tauri.conf.json` 的 `bundle.icon` 精确引用。托盘安装函数必须从 Tauri Builder `.setup(...)` 实际调用，在同一实现组装并绑定只含 `show_window`/`quit` 的 `Menu`、把 `default_window_icon()` 当作必需值、调用 `.icon(...)` 并成功 `.build(app)`；默认图标缺失必须中止启动，不能静默创建无图标托盘。关闭处理必须从 `.on_window_event(...)` 可达。主鼠标左键释放和 `show_window` 都显示、取消最小化并聚焦主窗口；原生 `CloseRequested` 只 `prevent_close()` 后隐藏且进程继续；只有 `quit` 显式结束应用并回收 owned task。默认不加入自动启动。菜单绑定同时满足 Tauri 官方说明的 Linux 图标可见性要求。
- 结构门禁决定：最初新增标准库 Node.js 托盘检查器；ADR-20260825-007 已把它扩展并改名为 `verify-gui-lifecycle-contract.mjs`。当前 GUI 初始化构建前必须用该检查器验证 workspace Tauri feature、member 的 `workspace = true`、32px 图标的格式/非透明像素/配置引用、从 `.setup` 可达且在同一函数绑定 Menu/default icon/icon/build 的真实 Rust 托盘安装、已注册的关闭事件、点击/菜单/窗口/退出源码、稳定 ID、中英文原生资源，以及 `tray_show_restores_and_focuses_main_window`、`close_request_hides_without_exit`、`tray_quit_exits_application` 三个固定命名托盘回归；单实例部分按 ADR-20260825-007 追加。未调用死代码、缺图标后继续、只创建菜单项或只注册菜单事件都失败。检查器对路径越界、图标/源码符号链接、非法 UTF-8、空源码、畸形/全透明 PNG 和任何缺项失败关闭，只使用 Node.js 标准库。
- 真实生命周期门禁决定：`$desktop-test-gui-initialization-e2e` 在构建并启动本次 debug/no-bundle 二进制后，必须通过 Computer Use 定位当前宿主的真实托盘图标，保存同时识别托盘区域和应用图标的截图，并确认状态栏/通知区域出现有可见非空图形的图标，而不是透明/纯空白点击区域或只能弹出菜单的不可见占位；随后确认菜单恰好只有本地化显示/退出两个可操作项，再依次实测原生关闭使窗口隐藏但进程和托盘继续、托盘左键恢复聚焦、菜单“显示窗口”再次恢复聚焦，以及菜单“退出”结束进程并移除托盘图标。结构检查或单元测试不能替代该真实宿主证据。
- 失败语义：缺少 feature、32px 资产/配置引用/运行时接线、源码、稳定 ID、双语资源、固定命名回归、可见非空托盘图形、精确菜单或任一生命周期行为，或者当前宿主只能定位空白点击区域、无法显示、定位、操作或可靠判定托盘，都必须阻断初始化能力裁剪和唯一基线提交。不得因 Product Spec 为 Draft、页面简单、宿主差异、实现偏好或 `milestone_e2e` 值静默跳过、降级或记为 `Not run`。
- 影响范围：最新 Product Spec、AGENTS/README、Agent Policy、工程规则、验证矩阵、Rust GUI 基线、实例化/初始化/GUI adapter/GUI 初始化 E2E Skills 及其元数据、Harness validator、治理回归和当日 Changelog。非 GUI 初始化、最终候选 E2E 选择、签名、发布和 Verification 证据留存规则不变。
- 理由：系统托盘与“关闭只隐藏”共同决定桌面应用的基本可达性和退出语义；只检查配置或源码无法证明宿主实际显示了托盘，只做 Computer Use 又会在构建前才发现确定性的接线缺失。结构门禁用于快速失败，命名回归锁定代码行为边界，真实 E2E 证明最终调试二进制与当前操作系统集成，三层证据共同关闭既有漏检路径。
- 风险与缓解：真实托盘位置和可访问性会随操作系统、桌面环境和状态栏设置变化，严格失败可能使某些无可观察托盘的 runner 无法初始化 GUI。该结果是有意的安全失败：GUI 初始化必须转到能真实观察目标托盘的受支持宿主，而不是生成缺少关键生命周期的基线；其他平台仍明确标记 `Unverified`。32px PNG 检查只证明图像结构与非透明像素，不能证明当前主题/桌面实际可辨，因此必须保留真实截图门禁。检查器只识别稳定 Tauri 结构片段，若官方 API 演进则由真实迁移证据更新，不以放松门禁代替适配。
- 验证：Node.js 专项回归覆盖完整契约通过，以及缺少 `tray-icon`、托盘安装未从 `.setup` 接线、菜单未绑定、默认图标可选回退、`bundle.icon` 未引用 32px 来源、32px PNG 全透明、真实托盘创建、固定命名回归或中文资源时失败；Python Harness 治理回归与 validator 锁定检查器文件、初始化调用顺序、固定 ID/测试名、图标资产/接线和真实托盘可见图形 E2E 场景。本 Harness 不包含已实例化下游 GUI，因此本轮不构建或操作真实托盘；首个后续 GUI 下游必须产生真实前向证据后才能创建基线提交。
- 恢复或调整标准：只有项目负责人明确改变产品/平台范围，并以新 ADR 记录替代交互、可达性、退出语义、目标宿主、风险与真实候选证据时，才能调整或移除系统托盘。Tauri API、操作系统托盘模型或 Computer Use 可观察能力变化时可替换实现与探测方法，但不得恢复“只写文档、只看源码、无法观察也通过”或静默无托盘初始化。

## ADR-20260825-005：GUI 初始化提交前固定真实本机 E2E，并统一 Tabler 图标来源（E2E 场景已由 ADR-20260827-001 改为按 profile 执行）

- 日期：2026-08-25
- 状态：Accepted
- 关联需求：项目负责人要求创建完项目后，只要包含 GUI 就自动完成一次 E2E，至少证明当前项目可编译启动、左侧菜单收起时图标与 Logo 居中、所有 UI 菜单链接页面可达；同时把图标库固定为 `@tabler/icons-react`，图表相关图标优先从中选择。
- 初始化 E2E 决定：新增 `$desktop-test-gui-initialization-e2e`，只由 `$desktop-initialize-rust-project` 在 GUI 脚手架及相关非空单元测试通过后、初始化能力裁剪与唯一 `chore: initialize project` 基线提交之前固定调用一次。它不读取或询问 `milestone_e2e`，因为这是初始化结果自身的硬门禁，而不是某次发布构建的可选候选 E2E。
- 真实产物边界：在 GUI 目录运行 `pnpm tauri build --debug --no-bundle`，再用 `cargo metadata --format-version 1 --no-deps` 的 `target_directory` 与唯一 GUI binary target 精确定位本次真实本机调试二进制；启动后必须出现可见主窗口且进程持续存活。Tauri CLI 官方提供 `build --debug` 与 `--no-bundle`，适合生成可直接运行且不创建安装包的调试应用（[Tauri CLI](https://v2.tauri.app/reference/cli/)、[Debugging in Tauri](https://v2.tauri.app/develop/debug/)，核对日期：2026-08-25）。不得用 Vite 开发服务器、静态页面、Mock、旧二进制或内部函数替代。
- 可见场景：Computer Use 从真实窗口和可访问树确认侧栏初始即收起，选中本地 Logo、所有当前渲染功能图标和赞助/设置/关于固定图标沿同一侧栏中心线水平居中、可见且无裁切；随后从可访问树动态枚举全部当前菜单项，逐项激活并验证活动状态、非空页面内容，以及没有空白、崩溃、404 或未匹配路由。固定 `/sponsor`、`/settings`、`/about` 必须出现在枚举结果中。任何失败、超时、取消或无法判定都阻断裁剪与基线提交，且所启动子进程必须始终由当前步骤退出并回收。
- 图标决定：Tauri React GUI 把 `@tabler/icons-react` 固定为唯一图标库，作为带完整三段兼容下界并经过 `resolutionMode: lowest-direct` 验证的直接生产依赖；组件只做命名导入。Tabler 官方 React 包提供 typed React components 与 tree-shakable named imports（[`@tabler/icons-react`](https://www.npmjs.com/package/%40tabler/icons-react)、[React 使用指南](https://tabler.io/guides/how-to-use-svg-icons-in-react)，核对日期：2026-08-25）。菜单、操作、状态、空态和图表周边控件存在适用图标时必须优先选择 Tabler，不得另装图标库或用手写 SVG、字符、emoji 替代；图表数据可视化本身不由图标库承担。
- 模板决定：`AppSidebarTemplate` 的功能项从 `ReactNode` 收紧为 `TablerIcon` 组件；赞助、设置、关于和侧栏展开/收起使用固定 Tabler 命名组件，不再要求下游注入固定项图标，也不再使用字符箭头。收起态对 identity 容器和每个 NavLink 显式设置居中布局，模板回归锁定 Tabler 来源、Logo 容器和每个菜单项的居中标记；真实 E2E 再复核可见像素结果。
- 取代范围：本决定只为“最终候选形成后才能执行 E2E”增加一个严格限于 GUI 一次性初始化的本机调试例外；发布候选的 `$desktop-test-final-artifact-e2e`、逐次构建 E2E 选择、签名、打包、Verification 与人工复核规则保持不变。初始化 E2E 不写 `release/` 或 `docs/VERIFICATION.md`，不得称为发布候选、完整验收或交付就绪。
- 生命周期与升级：新 Skill 是初始化专用能力，通过后必须与实例化/初始化 Skills 一起从终端下游删除；Harness 升级所有权将它标记为 `tombstone`，禁止未来注入已初始化下游。失败时保留现场但不得创建基线提交。
- 影响范围：最新 Product Spec、AGENTS/README、Agent Policy、工程/验证/Rust GUI 基线、实例化/初始化/GUI adapter/GUI 支持 Skills、React 模板与测试、Harness validator、升级所有权和当日 Changelog。
- 风险与缓解：调试构建会增加 GUI 初始化耗时，视觉中心判断可能受缩放/WebView 差异影响，菜单页面也可能只在特定宿主失败。通过锁定当前宿主、限定 60 秒、精确定位本次二进制、优先可访问树、可测时使用 2 CSS px 中心差、无法判定即失败、完整进程回收和跨平台 `Unverified` 标记降低误判；最终候选仍需独立构建与验收。
- 验证：Harness 侧以 Python validator/负向回归锁定新 Skill、初始化调用顺序、Tabler 组件来源、收起态居中样式和升级 tombstone；模板 Testing Library 断言功能/固定菜单图标与 Logo identity 的居中契约。当前 Harness 本身不包含可实例化的真实下游 GUI，因此本轮不运行 Computer Use 初始化 E2E；第一次含 GUI 的下游初始化必须完成前向真实验证，其他宿主保持 `Unverified`。
- 恢复或调整标准：若官方 Tauri CLI 改变 debug/no-bundle 产物结构，或真实 WebView 证明 2 CSS px 阈值不可稳定观测，依据官方文档与真实下游截图调整定位/测量方法；不得改回开发预览、静默跳过 E2E、放宽菜单全覆盖，或恢复多图标库与字符图标。

## ADR-20260825-004：更新入口归位关于页，并固化三态主题与图标化折叠菜单

- 日期：2026-08-25
- 状态：Accepted
- 关联需求：项目负责人要求把更新功能放回关于页；设置页增加深色、浅色、跟随系统；侧栏收起时显示图标并以 Tooltip 显示名称，展开时显示图标与名称；GUI 初始化直接提供包含背景色、文字色等语义的亮色/暗色两套主题。
- 页面决定：`/settings` 继续展示当前应用名/版本、中文/英文、主题偏好和统计同意，不再承载手动更新。`/about` 在产品事实之后固定展示手动检查更新按钮和完整更新状态，再展示作者、联系方式与三段免责声明；未配置产品更新能力时按钮禁用并显示 `NotConfigured`，保持零出站。更新状态机、官方签名 updater、core SemVer 强更和根级强更门均不改变。
- 主题决定：GUI 初始化新增唯一 `AppThemeProviderTemplate`，Mantine 根继续以 `defaultColorScheme="auto"` 启动，使用带明确本地存储 key 的 color-scheme manager 持久化 `light`、`dark`、`auto` 三态偏好。两套主题分别定义页面背景、surface、主文字、次文字、边框和强调色；应用根表面、固定侧栏与 Sponsor 等页面消费这些语义，不得只改变一个页面或只改变文字。`auto` 使用运行时系统主题，不把初始化机器当时的主题固化进产物。该实现遵循 Mantine 官方的 [color schemes](https://mantine.dev/theming/color-schemes/)、[CSS variables resolver](https://mantine.dev/styles/css-variables/) 与 [MantineProvider](https://mantine.dev/theming/mantine-provider/) 契约（核对日期：2026-08-25）。
- 导航决定：产品功能项及赞助、设置、关于三个固定项的图标全部改为必填。折叠侧栏只隐藏可见文本标签，不隐藏图标；每项用本地化 Tooltip 补充可见名称，并继续以 `aria-label` 提供不依赖 Tooltip 的可访问名称。展开侧栏同时显示图标与名称。Tooltip 的单子节点/ref 要求由 Mantine `NavLink` 满足（[Tooltip](https://mantine.dev/core/tooltip/)，核对日期：2026-08-25）。Logo→版本顺序、默认收起、功能区和固定底部顺序不变。
- 所有权：语言与主题都是 GUI adapter 的设备级展示偏好，不进入 shared core；React 只调用 Mantine color-scheme context 的 setter，不复制业务状态。更新状态仍由 Rust adapter/core 提供，React 不解析 SemVer、不判断强更、不创建 endpoint。
- 取代范围：本决定取代 ADR-20260825-001 中“检查更新位于设置页”的页面归属，并补充 ADR-20260825-002 的主题范围从 Sponsor 局部适配扩展为应用级双主题，以及侧栏折叠规则从“保留可访问名称”收紧为“必填图标 + Tooltip + 可访问名称”。其余更新/强更/统计安全边界、Logo/版本顺序、窗口尺寸、i18n 和零出站规则继续有效。
- 影响范围：最新 Product Spec、AGENTS/README、工程规则、Rust/GUI/React/Mantine 基线、初始化/GUI adapter/GUI 支持 Skills 与参考、React 模板、双语资源、Harness validator、专项回归及当日 Changelog。
- 不包含：本轮不创建真实下游，不配置更新 endpoint，不发送统计，不构建 GUI/DMG/NSIS，不运行冒烟/E2E，也不以模板测试代替真实窗口中的主题对比度、Tooltip 定位或跨平台 WebView 渲染复核。
- 风险与缓解：图标语义不清会让折叠菜单难辨，主题 token 不完整会产生局部亮暗混杂，本地偏好也可能因浏览器存储不可用而退回默认。通过图标必填、Tooltip 与独立可访问名称、全局语义变量、亮暗差异回归、`auto` 安全默认和真实下游视觉复核降低风险；存储失败不得阻断应用启动。
- 验证：模板测试覆盖展开/折叠图标、折叠 Tooltip、三态主题切换与本地持久化、亮暗背景/文字/surface 差异、关于页更新动作与未配置禁用状态；Harness validator 增加对应资源键、模板片段与破坏性负向回归。运行 Python 专项/全量 Harness 回归与 validator；仓库未声明可执行前端清单，因此本轮不安装依赖或运行模板 Vitest，真实 GUI、视觉与跨平台范围保持 `Unverified`。
- 恢复或调整标准：若真实下游证明 Tooltip 或本地存储在目标 WebView 存在兼容问题，基于官方文档和真实候选证据调整实现；不得把更新静默移回设置页、让折叠项丢失图标/名称，或删掉任一主题来规避问题。

## ADR-20260825-003：Rust、前端依赖与受管工具统一声明可验证的最低兼容版本范围（版本选择与当前下界已由 ADR-20260831-007 取代）

- 日期：2026-08-25
- 状态：Partially superseded by ADR-20260831-007；兼容下界、最小 feature 与锁文件规则继续有效
- 关联需求：项目负责人要求 Rust 与前端都以“兼容的最小版本”作为要求，而不是指定某个精确版本，并清理当前工程中把普通依赖或工具写成指定/最新版本的规则与实现。
- 决定：依赖清单和环境契约以“完整、包含且经过验证的最低兼容稳定版本”为事实来源；锁文件负责固定某次实际解析结果。正常安装或锁定可以得到范围内较新的稳定版本，但“较新”不再是选型目标，也不能改变兼容下界。只有使用到新的 API/feature、修复安全或平台兼容问题，且提高后的下界通过相同验证时，才提高最低版本。
- Rust 边界：registry 直接依赖使用完整三段下界的 Cargo 兼容要求，例如 `1.2.3` 表示 Cargo 默认 caret 兼容范围而不是精确 `=1.2.3`；禁止普通依赖使用 `=`、通配符、未经批准的 Git/tag 或无下界范围。根 `rust-version` 继续表示最低支持 Rust，member 继承 workspace 依赖。Cargo 官方说明普通版本要求使用 caret 兼容语义，`rust-version` 声明最低 Rust（[dependency requirements](https://doc.rust-lang.org/cargo/reference/specifying-dependencies.html)、[rust-version](https://doc.rust-lang.org/stable/cargo/reference/rust-version.html)，核对日期：2026-08-25）。
- 前端与工具边界：`dependencies`/`devDependencies` 使用带完整三段下界的 caret 或由上游官方明确支持的兼容范围，禁止裸精确版本、`latest`、tag、通配符或无下界范围。`engines.node` 使用当前 Vite 基线要求 `^20.19.0 || >=22.12.0`，`engines.pnpm` 使用 `>=10.0.0`；缺失 pnpm 时安装 `pnpm@^10.0.0`，现有更高兼容版本可继续使用。macOS→Windows 受管工具 `cargo-xwin` 以本机现有且能提供 build/check/test 子命令的 0.22.0 作为已观测下界，兼容范围要求 `>=0.22.0, <0.24.0`；缺失时由 Cargo 以该范围和 `--locked` 安装，既有范围外版本失败关闭，不因当前仓库已有更高发布版而把下界抬到 0.23。Cargo 官方 `cargo install --version` 接受版本要求而非只能接受精确版本（[cargo install](https://doc.rust-lang.org/cargo/commands/cargo-install.html)，核对日期：2026-08-25）。Homebrew 的 LLVM/LLD/NSIS 只做宿主能力探测，不把 formula 当前解析结果伪装成项目兼容版本。旧式精确 `packageManager` 字段不得充当兼容要求；实际包解析由 `pnpm-lock.yaml` 固定。pnpm 官方允许 `engines` 使用范围，并提供 `resolutionMode: lowest-direct` 验证直接依赖下界（[package manifest](https://pnpm.io/package_json)、[resolution settings](https://pnpm.io/settings/other)，核对日期：2026-08-25）；Vite 官方当前 Node.js 要求为上述范围（[Vite guide](https://vite.dev/guide/)，核对日期：2026-08-25）。
- 下界验证：Rust 新增或提高直接下界时，在临时副本中执行 `cargo +nightly update -Zdirect-minimal-versions`，再用根 `Cargo.toml` 声明的最低 Rust 工具链运行受影响的非空测试；前端在临时配置中使用 pnpm `resolutionMode: lowest-direct`，于声明的最低 Node.js/pnpm 环境执行冻结清单所需的类型检查、单元测试与生产构建。日常工作仍使用提交的正常锁文件，不把最低版本解析结果覆盖为日常锁文件。
- 精确值例外：应用/Harness 发布版本、协议或模式版本、Rust edition、目标三元组、资产摘要、签名密钥指纹、按供应链安全要求钉住的 CI action commit 不是依赖兼容要求，可以继续使用精确值。普通依赖若因上游缺陷、安全事件或互操作协议必须精确固定，先记录硬规则例外、风险、验证和解除条件。
- 取代范围：本决定取代 ADR-20260805-003 及现行文档中“优先采用较新/最新兼容稳定依赖”的版本选择部分，也取代环境门禁中缺失 pnpm 时安装 `pnpm@latest` 的做法；固定技术族、最小 feature、三平台、MSRV、锁文件可复现与依赖准入的其余规则继续有效。
- 影响范围：AGENTS/README、工程规则、最新 Product Spec、Rust/CLI/TUI/MCP/GUI 基线与 Skills、中性 Cargo 资产、Node.js/pnpm/cargo-xwin 环境门禁、跨平台 MSRV workflow、Harness validator、专项回归、TECH_DEBT 与当日 Changelog。
- 理由：精确锁死清单会错误缩小消费者可用范围，追逐“最新”又无法证明真正支持的下界；把清单下界与锁文件当前解析分开，可以同时得到可声明的兼容性、日常可复现性和受控升级。最低版本解析能发现宽泛要求掩盖的隐式下界，例如库间对 serde 等共享依赖的真实约束。
- 风险：生态的最低版本解析可能暴露上游元数据错误，范围内较新版本也可能引入回归，Node.js 非连续兼容范围不能用单一数字比较。通过完整三段下界、两类解析策略、声明最低工具链测试、日常冻结锁文件和安全例外 ADR 降低风险；最低版本通过不等于所有兼容版本都已逐一验证。
- 验证：中性 Rust workspace 已以直接最低版本解析到声明下界，并在 Rust 1.90.0 上通过 7 条 workspace/all-target/all-feature 锁定测试；环境门禁增加 Node.js 范围边界、pnpm 最低版本/缺失安装范围，以及 `cargo-xwin >=0.22.0, <0.24.0` 安装、0.22/0.23 范围内复探和范围外拒绝的正负向回归；本机真实 `cargo-xwin 0.22.0` 的版本/帮助探测与 xwin `--check-only` 全门禁通过。validator 检查规则、清单、workflow 和 Skills 不再恢复精确/最新普通依赖要求。真实 GUI 依赖与完整 NSIS 交叉构建尚待首个下游前向运行，保持 `Unverified`。
- 恢复或调整标准：若 Cargo、pnpm 或固定前端技术族改变最低版本解析能力，依据官方文档与真实下游证据更新解析方法和下界；不得退回裸精确版本、`latest` 或只测试当前锁文件后宣称兼容下界。

## ADR-20260825-002：GUI 初始化采用 Logo 三选一、默认收起的 Logo→版本侧栏、双主题赞助页与 1440×900 主窗口（侧栏默认值与赞助页默认性已由 ADR-20260827-001 取代）

- 日期：2026-08-25
- 状态：Accepted
- 关联需求：项目负责人要求 GUI 初始化至少产出 2–3 个 Logo 供用户选择；左侧菜单顶部永远是 Logo、其下为当前版本并默认收起；赞助页根据亮色/暗色主题适配且最好同时支持两种主题；主窗口初始尺寸足以同时显示三张赞助档位卡和展开侧栏。本决定把“至少 2–3 个”收敛为固定 3 个，避免调用者只提供文字方案或静默默认。
- Logo 决定：选择 GUI 后，`$desktop-prepare-gui-app-identity` 先进入初始化 Logo 模式，使用当前项目展示名、可选品牌方向和有权使用的源资产实际生成正好 3 个彼此明显不同的 1024×1024 sRGB PNG 候选。三个候选必须同时作为独立图片预览并等待用户明确选择；图像生成能力不可用时也必须实际生成三种确定性字母组合/几何备选，不能以中性占位图完成。选中候选保存为 `<project-id>_gui/src-tauri/icons/app-icon-master.png`，逐字节复制为 `<project-id>_gui/public/app-identity/logo.png`，再由项目本地 Tauri `icon` 命令生成平台图标。`docs/GUI_APP_PROFILE.md` 记录三个候选、来源、预览证据、SHA-256、用户选择和最终路径；首次真实产品 GUI 开发只补齐其余身份或让用户重新选择，不重复假装从零生成。
- 侧栏决定：固定侧栏的 Jotai 初始值为收起。选中的本地 Logo 是侧栏 DOM 的第一项，权威当前版本紧随其下；展开和折叠状态均不得隐藏 Logo 或版本。应用名通过 Logo 替代文本和其他权威界面继续可访问，产品功能区与赞助/设置/关于固定底部组维持 ADR-20260825-001 的顺序。
- 主题决定：Mantine 根使用 `defaultColorScheme="auto"`，同一应用产物始终携带亮色和暗色 token；赞助页通过 `useComputedColorScheme` 消费运行时有效主题，分别选择明确的背景叠层、卡片 surface、强调/对比色，并让非透明亮色档位插图置于受控的中性承载面。初始化宿主当时的主题不得删除另一套呈现，也不得只替换文字颜色后声称完成主题适配。该路线与 Mantine 官方的 [color scheme context 与 computed scheme](https://mantine.dev/theming/color-schemes/) 一致（核对日期：2026-08-25）。
- 窗口决定：Tauri `app.windows` 的主应用窗口固定使用逻辑像素 `width: 1440`、`height: 900`、`minWidth: 960`、`minHeight: 640`、`center: true`、`preventOverflow: true` 且允许调整大小。默认 1440×900 在展开 248px 侧栏时仍使赞助页三张档位卡横向呈现；工作区较小时由既有响应式布局降为两列或单列。Tauri 官方 `WindowConfig` 支持 `width`、`height`、`minWidth`、`minHeight`、`center` 和 `preventOverflow`（[配置参考](https://v2.tauri.app/reference/config/#windowconfig)，核对日期：2026-08-25）。该主窗口与 `bundle.macOS.dmg.windowSize` 的 660×400 安装卷窗口完全独立，ADR-20260824-002 的 DMG 背景、落点、构建和最终卷验证门禁保持不变。
- 取代范围：本决定补充并在 GUI 身份时序上取代“只在首次真实 GUI 开发生成/选择图标”的旧条款；补充 ADR-20260825-001 的侧栏规则为“默认收起且 Logo→版本”；补充 ADR-20260824-004 的赞助页为同一产物同时支持亮色/暗色。托盘、关闭隐藏、标题、固定路由/导航、i18n、作者/免责声明、品牌媒体、零出站、更新/强更/统计安全边界均继续有效。
- 影响范围：最新 Product Spec、AGENTS/README、工程规则、Rust/GUI 基线、初始化/GUI 身份/GUI adapter/GUI 支持 Skills 与参考、React/Mantine 模板和双语 Logo 替代文本、Harness validator、专项回归及当日 Changelog。
- 不包含：本轮不为 Harness 自身生成产品 Logo，不创建真实下游，不构建 GUI/DMG/NSIS，不运行冒烟/E2E，也不签名、公证、staple 或发布。模板组件和契约测试不能代替真实下游对候选 Logo、小尺寸清晰度、主题对比度、窗口工作区适配或跨平台渲染的人工/真实产物复核。
- 理由：在初始化提交前完成 Logo 选择可避免侧栏、平台图标和后续身份资料各自使用不同占位物；固定 Logo→版本顺序使折叠导航仍能表达应用身份和版本。把亮/暗两套语义同时交付比读取初始化机器主题后只生成一种样式更可靠；主窗口与 DMG 窗口分离则消除既有 660×400 发布配置被误当成应用窗口尺寸的歧义。
- 风险：生成候选可能包含不可辨认细节或未经确认的相似视觉，1440×900 在较小屏幕上会被工作区限制，亮色源插图在暗色界面仍可能显得突兀。通过用户明确选择、无细小文字/32×32 可辨认要求、来源与摘要记录、`preventOverflow`、960×640 响应式下限、受控插图承载面和亮/暗双渲染测试降低风险；商标权属与真实视觉质量仍由用户负责最终复核。
- 验证：为 `AppSidebarTemplate` 增加默认收起常量、必填本地 Logo、Logo→版本顺序和两种折叠状态测试；为 `SponsorPageTemplate` 增加运行时 computed scheme、亮/暗主题标记和差异化背景/surface 测试；为 Harness validator 增加候选数量/选择路径、主窗口与 DMG 窗口分离、侧栏顺序/默认值和双主题的正负向回归。运行相关 Python 单元测试、模板 TypeScript/Vitest、Skill quick validator、完整 Harness validator与差异检查；未运行的真实下游、视觉与跨平台范围保持 `Unverified`。
- 恢复或调整标准：若真实下游证明三候选成本不成比例、1440×900 在目标设备普遍不可用，或 Mantine 主题接口发生兼容变化，项目负责人可基于实际候选、设备矩阵和官方迁移资料以新 ADR 调整候选数、窗口值或主题实现；不得通过静默选择、恢复占位 Logo、隐藏折叠态版本、把 DMG 尺寸当主窗口，或删除另一主题规避本决定。

## ADR-20260825-001：GUI 固定侧栏与设置页，并固化签名更新、强更和最小统计契约（侧栏模式与支持页默认性已由 ADR-20260827-001 取代）

- 日期：2026-08-25
- 状态：Accepted
- 关联需求：项目负责人要求 GUI 始终显示版本号，使用可收起的固定左侧菜单；产品功能从顶部向下排列，底部固定项从下向上为关于、设置、赞助；设置页提供中英文切换和检查更新。同时要求完整审查更新检查、强制更新与统计上报逻辑，并把安全、状态和构建要求固化到对应 Skills。
- 来源审计：只读复核 `/Users/manonloki/Documents/ym-work/ai/bifang_codex_usage_collect` 的现有结构后，保留其版本可见、底部支持入口和设置入口等可复用意图，但不照搬其网络实现。来源把桌面共享秘密写入客户端、以带稳定设备标识和系统信息的明文查询参数发送统计、使用 detached 异步任务、直接信任远端 `forcedUpdate` 布尔值，并只对下载地址做宽松协议判断；这些做法无法提供发布者真实性、目标绑定、任务回收或数据最小化保证，不得成为 Harness 默认能力。
- 导航与版本决定：所有 GUI 初始化固定创建可收起的左侧菜单。产品功能使用单独的可滚动区域从顶部向下注入；底部固定组的视觉顺序为赞助、设置、关于，因此从窗口底部向上读取为关于、设置、赞助。固定路由为 `/sponsor`、`/settings`、`/about`。展开和折叠状态都必须直接显示来自权威打包元数据的版本，不从页面常量复制；主窗口标题继续使用 `{applicationName} {version} {contactChannel}:{contactValue}`。折叠状态、窄窗口、键盘焦点与文本缩放都必须保持入口和版本可发现。
- 设置页决定：设置页固定展示当前应用名与版本、中英文切换、检查更新状态/动作和统计上报同意。语言切换继续使用 `i18next`/`react-i18next`、`rust-i18n` 与 `tauri-plugin-os` 的统一 locale 事实，并持久化用户选择。中性初始化没有产品出站配置时，检查更新显示 `NotConfigured`，统计开关禁用且默认不同意，所有固定页面保持零出站；设置入口存在不等于远端能力已启用。
- 更新决定：启用更新时使用官方 Tauri updater 的签名更新制品和公开验证密钥，生产 endpoint 限定 HTTPS，并拒绝降级及 target、arch、channel 不匹配。稳定 UI 状态固定为 `NotConfigured`、`Idle`、`Checking`、`UpToDate`、`OptionalUpdate`、`RequiredUpdate`、`Failed`；失败不得伪装为最新版。检查任务必须单飞并由应用生命周期拥有，具备取消、超时和关闭回收，禁止 detached task。一般检查失败默认 fail-open；若渠道要求 fail-closed，必须另行批准离线和恢复风险。
- 强更决定：远端 `forcedUpdate` 布尔值不是可信策略。GUI adapter 只把已验证发布者真实性、目标和渠道绑定的 `minimumSupportedVersion` 交给 shared core；core 以严格 SemVer 比较当前版本并返回稳定的必须更新结论。`RequiredUpdate` 由应用根级不可关闭门阻断普通路由挂载，只允许安装已经验证签名的更新或安全退出；安装失败继续留在门内并提供重试/退出。React 不拥有 SemVer 或强更资格判断。
- 统计决定：统计传输默认关闭，必须取得明确同意。固定基线只允许每进程一次 `app_started`，由 Rust GUI adapter 使用 HTTPS JSON `POST` body 发送精确字段白名单：`schemaVersion`、`eventName`、`applicationCode`、`applicationVersion`、`osFamily`、`architecture`、`locale`、`distributionChannel`、`occurredAtUtc`。禁止 GET/query、稳定设备或安装标识、用户名、主机名、路径、令牌、自由文本和业务载荷。队列只驻留内存且最多 32 条，同一时刻最多一个在途请求，连接/总超时分别为 5/10 秒，最多重试两次并使用 1/4 秒退避；撤回同意立即取消在途请求、清空队列并恢复零出站，进程退出不得为了上报阻塞。新增事件、字段、稳定标识或持久队列必须重新经过产品、隐私和适用法律批准。
- 客户端秘密决定：桌面二进制和前端 bundle 不能保密。服务端共享口令、发布私钥和任何代表服务端身份的长期 secret 不得进入 Rust/TypeScript、Tauri 配置、样例、测试、日志或前端产物。公开 updater 验证密钥可以随客户端分发；签名私钥只在已批准构建运行时安全来源中存在。
- 构建决定：`$desktop-build-tauri-release` 从产品事实解析 `updaterEnabled`。启用时要求 `bundle.createUpdaterArtifacts: true`、公开验证密钥、受限 HTTPS endpoints 和安全提供的 `TAURI_SIGNING_PRIVATE_KEY`，收集实际 archive 与 `.sig`、用公开密钥验证，并把 updater/Tauri 版本、channel、target、arch、公钥指纹、相对路径、大小、SHA-256 和签名验证结果写入候选 manifest。安装包代码签名/公证与 updater archive 签名是独立门禁；允许 unsigned 安装包不能绕过 updater 签名。构建不创建更新 feed、不上传制品也不发布。
- 取代范围：本决定在导航、设置、更新、强更和统计范围内补充并取代 ADR-20260824-004 的“更新检查与遥测继续全部留待未来、初始化只建关于/赞助入口”条款；该 ADR 关于托盘、关闭隐藏、动态标题、作者/联系人/免责声明、固定赞助内容、本地品牌资产和默认零出站的其余决定继续有效。此前 ADR 关于 core-first、i18n、生命周期任务所有权、秘密隔离、真实候选和签名发布门禁继续有效。
- 影响范围：最新 Product Spec、AGENTS/README、工程规则、Rust/GUI 基线、初始化/GUI adapter/GUI 支持/Tauri 构建 Skills、React/Mantine 模板、双语资源、产品差异模板、Harness validator、专项回归和当日 Changelog。
- 不包含：不修改来源仓库，不创建或构建真实下游，不配置真实 endpoint、统计接收方、签名私钥、更新 feed 或产品强更版本，不发送统计，不调用生产更新服务，不安装更新，也不运行冒烟、E2E、签名、公证或发布。真实下游运行、更新安装、强更恢复、统计接收和跨平台行为保持 `Unverified`。
- 理由：导航、版本、语言与本地支持页面是每个 GUI 都需要的一致外壳，可以在初始化时安全建立；网络更新、强更和统计则涉及发布者真实性、隐私、离线恢复和生命周期资源，必须把入口与启用能力分离，并以可验证的状态机、签名、core 规则、最小字段和构建清单约束实现。
- 风险：根级强更门可能在错误策略下锁死应用，更新签名密钥轮换可能使旧客户端无法升级，统计字段仍可能被接收方关联，侧栏固定布局可能在极窄窗口中挤压内容。通过认证最低支持版本、默认 fail-open、签名与 target/channel 绑定、零稳定标识、明确同意/撤回、字段白名单、无持久队列、折叠/窄窗口测试和安全退出降低风险；真实候选仍需使用实际 endpoint、签名制品和目标平台完成前向验收。
- 验证：为侧栏位置、折叠状态版本可见、顶部功能区、固定底部顺序、设置页中英文切换、`NotConfigured` 零出站和根级强更门增加 React/Testing Library 模板测试；为 Skill/参考/资源键、更新状态、强更所有权、统计字段与生命周期边界、Tauri updater 构建/签名清单增加正负向 validator 回归；运行默认 Python 回归、完整 Harness validator、前端严格 TypeScript/Prettier/Vitest、Skill quick validator 和差异检查。本轮不以模板测试声称真实更新或统计可用。
- 恢复或调整标准：若真实下游证明导航顺序、支持入口或语言范围需要改变，由项目负责人更新固定产品范围并同步模板和回归；若 updater、渠道或隐私法规要求不同状态、签名轮换、fail-closed、事件或字段，必须提供真实渠道/威胁/合规证据，以新 ADR 同步 core 契约、产品配置、构建、恢复路径和验收，不得通过信任远端布尔值、嵌入客户端 secret、恢复稳定设备标识或取消签名验证规避本决定。

## ADR-20260824-004：GUI 初始化固定托盘、关闭隐藏、动态标题与关于/赞助页面（托盘与支持页默认性已由 ADR-20260827-001 取代）

- 日期：2026-08-24
- 状态：Accepted
- 关联需求：项目负责人要求把系统托盘作为 GUI 默认配置，托盘包含“显示窗口”和“退出”，关闭窗口不退出应用；同时要求从 `/Users/manonloki/Documents/ym-work/ai/bifang_codex_usage_collect` 提取已经使用的关于页、赞助页、规则和资源，让关于页包含作者、作者联系方式与免责声明，窗口标题按“应用名称、程序版本、联系方式”组成，并在初始化时直接建立关于/赞助页面及菜单入口。
- 来源审计：只读核对来源仓库当前实现与 Git 事实后，确认其桌面生命周期使用稳定托盘菜单、关闭隐藏、显式退出和本地化原生文案；关于页当前作者为守城工作室、联系方式为 `QQ 2222980`，包含三段中英文免责声明；赞助页使用现有三档价格/权益、双支付码与 12 个 sponsor 媒体。来源还包含自动启动等能力，但本需求只批准“显示窗口/退出”，因此自动启动不进入默认托盘。Harness 已有响应式 Sponsor 模板比来源固定布局更安全，继续保留该结构而不回退固定宽度或三栏。
- 生命周期决定：所有选择 GUI 的初始化固定启用 Tauri `tray-icon` feature。托盘只含本地化“显示窗口”和“退出”；显示菜单与托盘左键都执行主窗口显示、取消最小化和聚焦。主窗口 `WindowEvent::CloseRequested` 必须调用 `prevent_close()` 后隐藏，不能终止进程；只有托盘退出执行显式应用退出，存在生命周期 owned task 时先取消并回收。通知、快捷键、自动启动、伴随进程和其他托盘菜单仍需独立范围批准。
- 标题决定：主窗口原生标题与前端 `document.title` 固定消费同一事实并组装为 `{applicationName} {version} {contactChannel}:{contactValue}`。应用名来自当前下游 Tauri/GUI 身份，版本来自权威 workspace/打包元数据，联系字段来自共享品牌 profile 的 `contacts.windowTitle`；不得写死一次构建版本或维护第二份标题常量。本次把该联系人从来源旧值校正为当前 `QQ 2222980`，同时保留 `windowTitle` 与 `support` 两个语义角色。
- 页面决定：GUI 初始化直接建立 `/about` 和 `/sponsor` 文件路由并在应用导航显示。关于页注入当前应用名和权威版本，固定展示 `about.studio` 表示的作者、profile 的 `contacts.support` 以及来源当前三段中英文免责声明；产品标语、许可、隐私和其他动作仍按真实下游事实可选注入。赞助页继续使用 Harness 已改进的响应式 Mantine 结构，固定展示 19/199/1999 CNY 档位、权益、联系人、支付说明和双支付码；初始化完整复制 `media/sponsor/*`，包括当前未引用小图，支付码保持原字节和本地化替代文本。
- i18n 与网络决定：由于初始化现在包含真实页面和托盘文案，`i18next`/`react-i18next`、`rust-i18n` 与 `tauri-plugin-os` 必须在 GUI 初始化阶段接入，不再延后到首次业务页面。固定资源提供中文与英文，其他缺失系统语言回退英文，语言切换仍可发现且持久化。固定标题、关于页、赞助页和托盘全部是本地能力，不产生网络请求。更新检查、updater banner 打包、遥测、远程帮助、订单、权益、账户、付款确认和支付自动化继续默认禁用并需要独立范围/安全批准。
- 产品实例与传播决定：初始化不创建 `docs/GUI_SUPPORT_SURFACES.md`；固定基线由 `$desktop-add-gui-adapter` 自动消费 `$desktop-prepare-gui-support-surfaces` 资产。只有产品修改该基线、增加其他支持界面或启用出站能力时，才创建受保护的差异实例文档。非 GUI 下游继续裁剪全部 GUI Skill/资产。该决定在“选择 GUI 本身不自动创建页面/路由/运行时媒体”和“中性脚手架可延后 i18n”范围内取代 ADR-20260820-002、ADR-20260820-001 与 ADR-20260806-001 的相应条款；它们关于身份隔离、支付边界、媒体摘要、core-first、默认零出站和固定 i18n 技术选型的其余条款继续有效。
- 影响范围：最新 Product Spec、AGENTS/README、工程规则、Rust/GUI 基线、初始化/GUI adapter/GUI 身份/GUI 支持 Skills、品牌 profile/翻译/About 模板/媒体 manifest、Harness validator、专项回归和当日 Changelog。
- 不包含：不修改来源仓库，不创建真实下游项目，不加入自动启动、更新服务、遥测或支付业务，不运行生产网络、支付、签名、公证、构建、E2E 或发布；Windows、Linux 与真实下游运行保持 `Unverified`。
- 理由：托盘与关闭隐藏决定桌面应用能否在关闭主窗口后继续存在，必须从首个 GUI scaffold 就形成一致生命周期；作者/免责声明和固定赞助页面属于整个产品家族的共享支持表面，初始化统一生成可避免每个下游重复或遗漏。继续把任何出站与交易能力留在独立范围，可以在增加默认本地界面的同时维持最小权限和零网络副作用。
- 风险：用户可能误以为关闭窗口已经退出，静态联系人、免责声明、价格、权益或支付码可能随时间失效，默认赞助媒体也会增加包体积。通过清晰托盘退出入口、本地化菜单、固定字段门禁、品牌复核责任、原字节清单与明确“静态展示不代表交易状态”降低风险；真实候选仍需在各目标平台运行验收后才能声称可用。
- 验证：为品牌联系人、固定免责声明、默认 sponsor 打包策略和页面模板增加正负向回归；更新初始化/GUI/支持界面的必需片段门禁，要求 `tray-icon`、显示/退出、`CloseRequested`/`prevent_close()`、标题公式、固定路由/导航、作者/联系人/免责声明和零出站；运行模板 Vitest/Testing Library、Python 专项与完整回归、Skill quick validator、完整 Harness validator 和差异检查。本轮不构建或运行真实下游，跨平台桌面生命周期仍待前向验证。
- 恢复或调整标准：若项目负责人要改变托盘菜单、关闭即退出、默认页面、作者/联系人/免责声明或赞助内容，必须以新产品范围决定同步初始化、品牌事实和回归；若真实平台证明关闭隐藏或托盘 API 在目标 Tauri/WebView 组合不可靠，可在保留显式退出和可恢复窗口的前提下记录平台专用替代证据。不得以移除测试、硬编码版本、静默启用自动启动/网络或把支付码解释为付款状态来规避本决定。

## ADR-20260824-003：Superpowers 默认关闭，环境门禁收敛为初始化与错误恢复（低于下界的环境处置已由 ADR-20260907-002 取代）

- 日期：2026-08-24
- 状态：Partially superseded by ADR-20260907-002；Superpowers 默认值、环境门禁触发范围和单次重试继续有效
- 关联需求：项目负责人要求默认禁用 Superpowers，并要求环境检测、安装等动作只在初始化阶段或实际遇到环境错误时进行，不得每次任务重复走环境流程。
- 背景：Harness 源策略与推荐预设此前都把 Superpowers 设为 `enabled`，使默认路径开放了并非所有下游需要的外部工作流能力。环境规则虽已避免普通文档任务，却仍允许显式构建仅因“缺少可复用环境证据”在真实命令前运行完整门禁，Tauri 构建还会因选择 xwin 目标预先安装发布工具；这会把证据状态和任务类型误当成环境故障，并在环境实际可用时重复探测或安装。
- 策略决定：`docs/AGENT_POLICY.md` 的 Harness 源值和初始化推荐预设统一使用 `superpowers: disabled`。推荐预设继续启用已受显式请求约束的 Worktree/Subagent 和候选冒烟，并保持 E2E 建议默认值为 `disabled`。下游仍可通过自定义选择明确启用 Superpowers；没有该明确选择时，本次及后续 Agent 都不得调用或遵循 `superpowers:*` Skill。
- 环境决定：中性 `$desktop-initialize-rust-project` 在写入脚手架前主动运行一次与已选接口匹配的完整环境门禁并补齐缺失适用项。初始化完成后，日常开发和构建必须先运行本次真实测试或构建命令；只有该命令已经失败，且保存的命令、退出状态和脱敏诊断明确指向门禁管理的工具链、目标或系统依赖缺失/不兼容时，才调用 `$desktop-check-development-environment`。门禁成功后只重试原失败命令一次，仍失败即停止并保留两次结果。
- 排除与目标门禁：新任务、新会话、首次修改代码、显式构建、选择某个目标、缺少/过期环境证据或工具链可能变化都不得触发环境预检。代码/编译语义错误、测试断言、普通依赖解析或网络、产品配置、凭据、签名和公证失败不得误判为环境错误。macOS→Windows xwin 专用门禁只有在实际失败命令就是已批准的 xwin 构建且错误属于其受管工具时才运行；不得自动安装 Homebrew，也不得临时拼装门禁外安装命令。
- 影响范围：最新 Product Spec、Agent Policy、AGENTS/README、Rust/GUI 基线、实例化/初始化/实施/环境/CLI 构建/Tauri 构建 Skills、Harness validator、相关回归和当日 Changelog。
- 不包含：不移除环境门禁脚本，不改变其供应链校验、接口条件或失败关闭规则；不改变用户自定义启用 Superpowers 的能力，不构建真实下游或候选，不运行签名、公证、E2E 或发布。
- 理由：默认关闭可选的 Superpowers 能力能保持下游最小权限和可预测流程；把环境门禁绑定到一次性初始化或已观察到的真实环境失败，可以消除按任务/证据状态重复探测，同时保留缺失工具时的自动恢复闭环。
- 风险：先尝试真实命令会在缺少工具时产生一次预期失败，并可能增加构建恢复时间；错误分类过窄会把真实环境故障直接返回，过宽则会把代码或凭据问题误判为可安装依赖。通过保留原命令/退出状态/脱敏诊断、限定受管错误类别、只重试一次和禁止门禁外安装降低风险。
- 验证：更新策略预设正反向回归、初始化/环境/构建 Skill 必需片段与旧预检描述拒绝门禁；运行默认 Python 单元测试、完整 Harness validator 和差异检查。本次不实际安装工具链或构建候选，环境脚本的既有隔离测试继续证明其安装与失败路径。
- 恢复或调整标准：若真实下游证明某项 Superpowers 能力应成为所有项目的最小默认，必须由项目负责人明确其权限、触发器和失败边界后更新预设；若某类环境错误无法在真实命令后安全恢复，可为该具体不可逆风险增加窄的前置探测，但不得恢复按任务、显式构建或证据状态运行完整环境流程。

## ADR-20260824-002：GUI 初始化创建项目内 macOS DMG 背景并由构建固定引用

- 日期：2026-08-24
- 状态：Accepted
- 关联需求：项目负责人要求在初始化项目时创建 macOS 构建背景图，并让构建 Skill 引用这张图片。
- 背景：现有 Tauri 规则已经要求最终 DMG 包含本地背景并验证 `.background/background.png`，GUI 身份流程也要求正式背景经过预览批准，但中性初始化没有提供真实图片或确定性项目路径，构建 Skill只检查最终卷。结果是新 GUI 下游可能带着背景配置空缺进入打包，直到 Finder 布局阶段才失败，也无法从初始化基线追溯构建实际消费的图片。
- 决定：`$desktop-initialize-rust-project` 携带一张无产品、Harness 或第三方身份的 660×400 PNG；选择 GUI 时，在删除初始化能力前逐字节复制为 `<项目标识>_gui/src-tauri/dmg/background.png`。`$desktop-add-gui-adapter` 把 `tauri.conf.json` 的 `bundle.macOS.dmg.background` 固定为 `./dmg/background.png`，窗口固定为 660×400，应用与 Applications 落点分别为 `(180, 220)` 与 `(480, 220)`。初始化源资产只负责建立基线，运行时和构建只能消费项目内副本。
- 视觉批准与构建决定：首次真实 GUI 开发由 `$desktop-prepare-gui-app-identity` 展示该基线，让用户批准或在同一路径替换，并在 `docs/GUI_APP_PROFILE.md` 记录当前 SHA-256、尺寸、落点和文案语言。`$desktop-build-tauri-release` 在任何测试前校验项目内图片为非符号链接 660×400 PNG、摘要与资料一致、Tauri 配置仍引用固定路径；随后仍须对最终 DMG 只读验证 `.DS_Store`、背景、唯一应用包与 Applications 链接。源码图片检查不能替代最终卷检查。
- 影响范围：最新 Product Spec、AGENTS/README、Rust/GUI 基线、实例化/初始化/GUI 身份/GUI adapter/Tauri 构建 Skills、Harness 必需文件与初始化/发布 validator、专项回归和当日 Changelog。
- 不包含：不创建具体下游项目，不生成应用图标，不替任何产品批准正式视觉，不构建 DMG/NSIS，不运行 E2E、签名、公证、stapling 或发布；其他平台保持 `Unverified`。
- 理由：把一张真实、无产品身份的图片放进一次性初始化载荷，可以让每个 GUI 下游从首个提交起拥有可解析、可预览且可由 Tauri 直接消费的背景；固定项目内路径避免初始化 Skill 裁剪后出现悬空引用，同路径替换又保留正式产品定制空间。
- 风险：中性图片可能被误认为正式品牌资产，固定尺寸和坐标可能不适合未来产品文案，生成图片的文字或构图也可能在缩放后失真。通过明确“待 GUI 身份批准”、固定预览步骤、尺寸/摘要门禁、允许同路径替换和最终 DMG 只读检查降低风险。
- 验证：解析 PNG 签名和 IHDR，确认资产为 660×400、非空且非符号链接；视觉检查文字准确、箭头方向与两个落点区域；新增初始化资产尺寸负向回归与 Tauri 构建引用负向回归；运行相关 Python 单元测试、默认 discovery、完整 Harness validator、变更 Skill quick validator 和差异检查。本次不构建真实下游或候选。
- 恢复或调整标准：若真实下游证明其他尺寸、坐标或无文字背景更可靠，项目负责人可更新中性资产与唯一基线值，并同步初始化、GUI 身份、Tauri 配置、构建门禁和回归；不得通过移除项目内背景、跳过视觉批准或只检查源码配置来绕过最终 DMG 布局证据。

## ADR-20260824-001：日常开发收敛为必要单元测试，构建逐次确认 E2E 并运行全量单元测试

- 日期：2026-08-24
- 状态：Accepted
- 关联需求：项目负责人要求收敛每次开发流程，除必要 ADR、Changelog 等事件触发记录和本次开发需要的单元测试外，不增加额外步骤；每次构建必须全量运行单元测试，并询问用户是否启用 E2E。
- 背景：现有 Harness 把任务分为快速、标准、里程碑三档，多步骤或中等风险开发会自动引入持久 Work Plan，所有实现轮次还强制执行文件行数、中文声明注释、lint/静态等全仓门禁；E2E 则只在里程碑末端按持久策略解析，构建阶段明确禁止询问或消费。这套分层虽然审计完整，但与项目负责人要求的日常最短开发闭环和逐次构建选择不一致。
- 开发决定：日常开发统一直接进入 `$desktop-implement-change`，只完成当前批准范围、增加或更新本次必要的单元/回归测试并运行这些测试。不得仅因多步骤、多模块、中等风险、可并行或 Agent 偏好自动增加 `$desktop-plan-change`、Work Plan、全仓测试、格式化、lint、静态检查、构建、冒烟、E2E、Verification 或人工复核。Product Spec、ADR、Changelog、Product Status、Work Plan 与 Verification 仍按各自真实事件触发；用户明确要求计划、跨会话交接、发布/审计或必要风险边界时可以单独启用对应能力。
- 构建决定：显式构建在任何测试或编译前必须解析一次“当前构建是否启用 E2E”。若用户已在当前构建请求中明确选择则复用且不重复询问，否则询问一次；`docs/AGENT_POLICY.md` 的 `milestone_e2e` 仅是建议默认值，不能替代逐次选择。构建必须运行项目全部非空单元测试：Rust 使用 workspace/all-targets/all-features 的锁定测试，GUI 同时运行完整 Rust workspace 和前端单元测试套件；失败或零测试阻断候选。
- 阶段边界：E2E 不混入单元测试、编译、签名或打包命令。当前构建选择为启用或产品/渠道硬要求时，只在最终真实候选形成后由验收/E2E 能力执行；禁用时记录 `Not run` 与剩余风险。安全、隐私、数据迁移、破坏性操作、生产/付费/凭据副作用、对外兼容契约、签名、发布和渠道要求仍保留解决当前风险所必需的授权与门禁，不得被“精简开发”解释为绕过。
- 取代范围：本决定取代 2026-08-03 起“每项任务必须选择快速/标准/里程碑，复杂开发自动建立 Work Plan”以及“构建阶段不得解析 E2E 选择”的长期流程要求；此前关于真实候选、core-first、非 Mock 验收、签名公证、跨平台状态和人工发布复核的决定继续有效。
- 影响范围：最新 Product Spec、AGENTS/README、Agent Policy、工程规则、Rust/发布/验证文档、规划/实施/构建/验收/E2E/升级与适配器 Skills、Harness validator、候选 workflow 契约、回归测试和当日 Changelog。
- 风险：只运行本次相关单元测试可能延后发现跨模块、格式、静态或平台退化；逐次询问 E2E 会增加构建前一次交互；持久 E2E 偏好与当前选择可能被混淆。通过在显式构建中强制全量单元测试、让发布/渠道继续保留必要门禁、把持久值严格限定为建议默认值、并要求 E2E 只针对最终真实候选来降低风险。
- 验证：为开发最小闭环、构建逐次 E2E 解析和 Rust/GUI 全量非空单元测试增加确定性 validator 与负向回归；运行相关 Python 单元测试、默认 discovery、完整 Harness validator、统一文件行数门禁和差异检查。本次不构建真实下游、不运行 E2E、签名、公证或发布，这些范围保持 `Unverified`。
- 恢复或调整标准：若真实下游证明开发期必须恢复某项普遍门禁，应由项目负责人明确该门禁、触发条件和成本后以新 ADR 调整，不得通过 Skill 自行恢复全套流程；若逐次 E2E 选择造成不可接受的自动化阻断，可改为用户明确的构建参数，但仍必须逐次解析且不得仅依赖持久偏好。

## ADR-20260820-002：把产品家族品牌支持资源作为条件 GUI 依赖完整传播（运行时 sponsor 默认打包已由 ADR-20260827-001 取代）

- 日期：2026-08-20
- 状态：Accepted
- 关联需求：项目负责人要求再次检查真实下游的关于页、赞助页、相关约束与模板，并进一步确认支付二维码、固定价格/联系人、品牌更新 banner 和未使用小图都应整体进入 Harness，因为本 Harness 产出的下游共享同一品牌；来源下游自身的项目名称等仍不得同步。
- 背景：只读审计确认来源有 12 张赞助图片和 1 张更新 banner，没有已跟踪视频。真实页面同时固化三档赞助价格、品牌联系人、双支付码与中英文品牌赞助说明；关于页还混有来源产品名、产品功能、路由和更新服务。ADR-20260820-001 曾基于去身份化原则排除全部资源，但项目负责人随后明确：工作室品牌、赞助材料和更新视觉不是单一下游身份，而是所有产出产品共享的品牌事实。
- 边界决定：补充并在资源传播范围上取代 ADR-20260820-001 的“Harness 只传播 Skill/参考且不复制资源”限制。`$desktop-prepare-gui-support-surfaces` 必须携带完整品牌依赖包：13 个原始图片字节、固定赞助档位/价格、品牌联系人、中英文品牌赞助文案、产品实例文档模板、媒体清单、类型化 React 页面/媒体组件与非空组件测试。GUI 下游完整保留该 Skill 资产；只有产品明确选择关于、赞助或更新视觉后，相关文件才进入应用 bundle，选择 GUI 本身仍不自动创建页面、路由或网络请求。
- 身份隔离决定：共享工作室名称、联系人、赞助价格、支付二维码与 banner 是明确批准的品牌资产；来源下游的产品名称/标识、产品功能文案、路由、版本、固定服务地址、更新/遥测协议字段、秘密、签名身份、绝对路径与历史证据仍必须排除。终端下游按需生成的 `docs/GUI_SUPPORT_SURFACES.md` 继续由升级器保护，Harness 只提供 `.template.md`，不得把某个产品实例作为所有产品的完成事实。
- 支付与媒体决定：媒体清单逐文件记录路径、MIME、尺寸、SHA-256、用途、品牌/支付敏感性、复制来源类别和内部专有复用批准。支付二维码必须保持原始字节且不得优化、重绘或解码后重建；携带或展示静态支付码不授权订单、权益、账户、自动支付、支付状态确认或生产副作用。图片必须提供语义化 `alt` 或显式装饰声明。来源没有视频，因此不伪造视频文件；媒体组件仍要求本地视频使用 controls、字幕轨、文字稿、可选 poster、禁止 autoplay 与远程追踪，并尊重 reduced motion。
- 页面决定：关于页保持产品名、版本、功能区块和动作由当前下游事实注入，但默认复用品牌联系人；赞助页默认消费品牌档位、文案与全部赞助资源，同时修复来源的固定 800px、固定三栏、全页 `pointer-events: none` 和支付图片缺少替代文本问题。未使用小图不删除，按原文件完整打包并在清单中标为可选/当前未引用；更新 banner 只作为本地视觉资源，不隐式启用更新检查或固定 endpoint。
- 影响范围：最新 Product Spec、AGENTS/README、工程规则、Rust/GUI 基线、`$desktop-prepare-gui-support-surfaces` 的参考与依赖资产、GUI 初始化/升级条件传播、Harness validator、专项回归、当日 Work Plan、Product Status 与 Changelog。
- 理由：产品家族共享品牌时，保留原始品牌与支付资产能避免每个下游重复复制或错误替换，并保持赞助信息一致；条件安装、产品实例保护和严格排除来源项目身份仍能维持 scaffold 与具体产品之间的边界。
- 风险：支付码、价格和联系方式可能过期或被误用于未获批准页面；原始图片的逐文件外部来源记录不足；未使用图片会增加仓库和包体积。通过专有内部复用批准记录、摘要锁定、敏感性标记、只有已选界面才进入 bundle、付款说明人工复核、无自动交易逻辑和未来品牌复核条件降低风险。
- 验证：逐字节比较 13 个目标资产与来源、解析并核对图片尺寸/MIME/SHA-256、视觉检查品牌 banner 与支付码未被改写；对模板运行 TypeScript 与非空 Vitest/Testing Library 前向测试；验证响应式布局、图片替代文本、视频无 autoplay 且需要字幕/文字稿；运行 GUI 支持专项正负向回归、升级 conditional ownership 回归、完整 Harness validator、统一行数门禁、链接/差异扫描，并证明来源下游产品名、标识、固定服务地址和秘密没有进入资产包。
- 恢复或调整标准：品牌联系人、价格、支付码或视觉更新时必须由项目负责人重新确认，替换后更新清单摘要与相关测试；若产品家族不再共享该品牌，则恢复去身份化资产或把品牌包迁出通用 Harness。若未来加入真实视频，必须先记录来源/内部复用批准、字幕、文字稿、poster、体积预算和打包路径，再通过同一条件传播与 validator 门禁。

## ADR-20260820-001：从真实终端下游反向抽象 GUI 支持界面与 Tauri 分发门禁，同时隔离产品身份

- 日期：2026-08-20
- 状态：Accepted
- 关联需求：项目负责人要求从用户指定、已经过真实使用磨合的终端下游中抽象并同步 Harness 工程改进到模板，反复检查到没有遗漏，同时不得同步该下游的项目名称、品牌或其他产品实例。
- 背景：来源锁与 Git 历史显示，终端下游在既有 Harness 基线上积累了三类可复用改进：Homebrew 将 LLD 从 LLVM 拆包后的 xwin 工具链处理、使用已授权 `notarytool` Keychain profile 的公证探测、以及 GUI 支持界面和 DMG Finder 拖拽布局。来源同时包含产品名称、联系人、赞助资源、固定服务字段、秘密常量、签名身份和打包图片；直接复制会把一个产品的身份、安全缺陷和业务协议变成所有下游的默认值，也会违反中性 scaffold、core-first、升级保护和秘密隔离规则。
- 抽象决定：反向同步采用“来源锁基线 → 终端下游当前字节 → 当前模板工作树”的三方审计。只接收能脱离产品身份成立、具有明确失败模式和非空回归的工程能力；产品名称/标识、联系人、资源、固定 endpoint/协议字段、秘密、签名身份、版本、产物、哈希、验证结论和业务实现一律排除。来源路径和名称只用于当前任务只读审计，不进入长期 Harness 规则、Skill、脚本、参考或产品事实。
- GUI 支持界面决定：新增条件 Skill `$desktop-prepare-gui-support-surfaces`。窗口标题、关于/帮助/赞助入口、更新检查和遥测彼此独立，只有已批准 GUI 产品明确选择的项才实现；中性 scaffold 不预装页面、路由、资源、配置或网络请求。终端下游按需创建的 `docs/GUI_SUPPORT_SURFACES.md` 保存产品实例并由升级器保护，Harness 只传播 Skill/参考。展示与宿主机制属于 GUI adapter，跨接口资格、值域、默认值和状态转换属于 core；涉及权益、支付、账户、自更新、强制更新或渠道发布时必须另行定范围。
- 出站与秘密决定：每项远程能力默认禁用并具有独立的目的、触发器、方法、允许地址/字段/响应、重定向、超时、响应大小、重试/并发、owner/取消/关闭、日志、同意与保留清单。可选网络失败默认 fail-open，但不得把失败解释为成功；禁止 detached task 和无限重试。遥测默认关闭并要求字段级最小化、同意/退出、标识符生命周期、保留与删除边界。秘密实值只来自已批准安全运行时来源，不能进入源码常量、文档、配置样例、测试夹具、日志或前端 bundle。
- Tauri 分发决定：macOS→Windows xwin 环境门禁分别探测/安装/复探 Homebrew `llvm` 与可能拆分的 `lld`，已存在但缺少命令的 formula 失败关闭而不静默重装。Apple 公证允许一组完整环境凭据或已授权且在线可用的 Keychain profile，两种模式互斥，探测不输出 profile 名或秘密。macOS DMG 必须在最终布局、签名、公证和 stapling 后只读挂载真实字节，验证非空 `.DS_Store`、本地背景、唯一顶层应用包和指向 `/Applications` 的链接，再计算摘要；里程碑验收对当前最终字节重跑同一检查，不得自动接受软件许可或沿用旧候选证据。headless CI 不得无界等待 Finder AppleScript，任何后处理都使旧签名、摘要与验收证据失效。
- 影响范围：最新 Product Spec、AGENTS/README、工程规则、Rust/GUI 基线、GUI 身份/适配器/初始化 Skills、新支持界面 Skill、Tauri 构建与环境 Skills、发布文档、升级 ownership policy/manifest、Harness validator、专项测试、当日 Work Plan、Product Status 与 Changelog。
- 不包含：不修改终端下游，不复制其名称、路径、品牌、联系人、资源、服务端协议、秘密、签名/公证身份、候选或历史证据；不在模板创建 `docs/GUI_SUPPORT_SURFACES.md`；不实际构建 DMG/NSIS，不运行冒烟/E2E，不签名、公证、staple、上传、tag、push 或发布。
- 理由：真实下游暴露的环境兼容和打包失败比假设性设计更能证明 Harness 缺口，但只有剥离产品实例并把安全边界改为默认禁用、最小授权和最终字节验证，才能安全传播到未知下游。条件 Skill 和受保护实例文档让方法可以升级，而产品事实不会被模板或其他产品覆盖。
- 风险：文本门禁只能证明规则和已知身份没有直接泄漏，不能证明未来调用者不会填入不合规产品实例；Finder 布局 helper 的模拟挂载回归不能替代真实 DMG 前向证据；Keychain profile 在线探测依赖 Apple 服务可用。通过固定地址拒绝、模板实例文件缺失门禁、条件 ownership 顺序、秘密来源规则、专项失败路径测试、最终双向来源审计和 `Unverified` 标记降低风险。
- 验证：运行 xwin、公证与 DMG 布局专项回归，Skill 结构校验，GUI 支持界面中性/固定地址负向回归，升级条件规则顺序回归，默认 Python discovery、完整 Harness validator、统一文件规模检查、Rust 中性资产门禁、Markdown 链接、差异和身份/固定凭据扫描；真实 DMG/NSIS、Windows/Linux、签名、公证、冒烟/E2E 与发布均不记为通过。
- 恢复或调整标准：若真实下游证明某项支持界面需要稳定的跨产品 schema，应先以去身份化、默认禁用和兼容迁移证据提出新决定，不得直接复制产品协议。若 Tauri/Apple/Homebrew 官方行为变化，应以新的前向失败和官方资料调整探测或布局门禁；不得通过移除最终字节检查、输出 profile/秘密或恢复产品默认值来绕过。

## ADR-20260812-001：扩展 Rust/GUI 技术事实标准并建立收敛的中文声明注释门禁

- 日期：2026-08-12
- 状态：Accepted
- 关联需求：项目负责人要求从本机两个成熟下游 Harness 同步有意义的 Rust、前端技术选型，并把中文注释门禁优化到当前 Agent-first Harness。
- 背景：Rust 来源提供了 HTTP middleware、配置、可观测性、协议/存储/认证候选和可执行中文注释检查器，但采用单 package/single-bin Server 架构、默认多线程 runtime，直接复制会违反当前 workspace/core-first 与四类独立适配器边界。Web 来源补齐 Vite、文件路由、严格 TypeScript、前端质量工具链、Mantine 规范、配置/日志/Release 扫描和 TypeScript AST 注释检查思路，但其中文门禁工作计划仍未完成，机械检查全部局部变量与匿名回调，并自动生成数百条“保存变量”“执行处理逻辑”式套话，与当前语义注释和闭包豁免规则冲突。两个来源工作树均有未提交内容，因此本决定以用户指定的本机工作树字节作为审计输入，不把来源 `HEAD` 当成这些能力的发布溯源。
- Rust 技术决定：获批 HTTP 服务固定使用 Tokio + Axum + Tower/Tower HTTP；真实配置固定使用 config-rs，`notify` 仅用于批准的窄热重载；本地结构化日志正式固定为 tracing + tracing-subscriber + tracing-appender，OpenTelemetry OTLP/HTTP 只有在产品、隐私、采样、endpoint 和失败行为获批后默认关闭地追加。每个 spawned task 必须有 owner、取消、并发上限和关闭回收，禁止 detached task；timeout 不等于业务成功，锁不得跨越不受控 `.await`，HTTP 在监听前完成必需检查并支持 graceful shutdown。
- 条件技术决定：OpenAPI 使用 utoipa + utoipa-axum + Scalar code-first；GraphQL 使用兼容的 async-graphql + async-graphql-axum；非关系型能力优先官方 MongoDB async driver 与 redis-rs Tokio；本地认证优先 jsonwebtoken + Argon2id。这些只是能力触发后的固定候选，不进入中性依赖，且不得绕过消费者、数据、隐私、安全、密钥和生命周期范围闸门。本条扩展而不取消 ADR-20260805-003 的 Tokio、Clap、SeaORM、anyhow、thiserror、serde 与 jiff 事实标准。
- GUI 技术决定：Tauri React 前端固定追加 Vite、TanStack Router 文件路由与自动 route code splitting、严格 TypeScript、ESLint/`typescript-eslint`、Prettier、Vitest、Testing Library、jest-dom、user-event 与 jsdom。`src/routeTree.gen.ts` 是唯一生成路由树并按根相对精确排除。Mantine 采用单 Provider/单 theme、语义令牌、稳定样式优先级、完整交互状态、窄窗口/文本缩放、键盘/焦点/live region 和 Testing Library 语义测试规范。
- GUI 配置与日志决定：`development/test/release` 是统一逻辑 profile，但只有真实公开配置出现时才建立配置文件和类型化冻结对象；所有 Vite 变量均视为用户可读，禁止凭据，产品模块不得散落读取 `import.meta.env`。真实前端日志使用稳定 `level/scope/event/context` 和严格脱敏，Tauri 优先通过窄命令汇入 Rust tracing，不建立第二套持久日志或默认远程遥测。Release 在 Tauri 打包前静态检查最终 Vite `dist`，拒绝 source map、开发/测试 endpoint、debug/info 哨兵、本机绝对路径和未经批准的 console 输出。
- Rust 注释门禁决定：下游永久保留 workspace-aware `check_rust_chinese_comments.py` 及专项测试。检查器从 Cargo package/virtual workspace 根解析全部 members，扫描每个 package 的 `build.rs`、`src/**/*.rs` 和 `tests/**/*.rs`，机械覆盖 struct、enum、union、type alias、trait、具名函数、关联函数、方法和测试；要求声明自身紧邻且包含汉字的 outer doc comment，支持中间 attribute。它不要求字段、局部变量、闭包和宏展开产物，且以稳定 JSON、根相对 path/line/column/kind/name 和退出码 0/1/2 区分通过、声明违规与运行错误。
- TypeScript 注释门禁决定：只有选择 GUI 的下游才复制并运行随 `$desktop-add-gui-adapter` 提供的 TypeScript Compiler AST 参考检查器。机械范围收敛为 interface、type alias、enum、class、具名函数、方法/访问器/构造器、直接及后置命名/默认导出的箭头组件/hooks，以及测试文件或显式 `vitest` 导入中的 `test`/`it` 场景；字段、局部变量、循环绑定、业务同名调用和普通匿名/极短回调继续由所属结构/函数注释覆盖。门禁同时接入 `pnpm lint` 与项目 validator，精确排除生成路由树，并对语法残缺、非法 UTF-8、NUL、源码符号链接和空扫描失败关闭。非 GUI 下游不得因该门禁要求 Node.js/pnpm。
- 语义边界：两类检查器都只证明注释存在、归属正确且至少含一个汉字，不能判断业务意义、正确性或时效；人工/Agent 仍按 `docs/ENGINEERING_RULES.md` 复核。禁止提供自动注释器或批量生成套话，门禁不得扩张为注释比例、字符数或关键词质量评分。
- 影响范围：最新 Product Spec、AGENTS/README、Rust/GUI 基线、Mantine 参考、工程规则、初始化/实施/重构/适配器/构建/验证/升级 Skills、升级所有权、Harness validator、中文注释检查器与回归、当日 Work Plan 和 Changelog。
- 不包含：不复制来源的单 crate Server 或 Web/Extension/Plasmo/Chrome 架构，不新增独立 WEB adapter，不改变 core-first/current-thread/Tauri runtime/i18n/依赖锁定规则，不预装条件依赖，不捆绑页面或业务起始资产，不修改中性 Cargo 资产或锁文件，不构建、验收或发布真实下游候选。
- 理由：同步能力事实而保留当前架构边界，可以复用来源中成熟的生态组合和 AST/诊断思路，又避免把宿主专用架构与低价值注释噪音引入通用 Harness。workspace-aware、失败关闭和双接线能覆盖 Agent 容易遗漏的声明归属；保留有限豁免和语义复核可以让注释质量高于机械覆盖率。
- 风险：自维护 Rust 词法扫描器无法完整展开过程宏或替代 rustc；TypeScript 参考检查器尚未由本 Harness 真实生成到下游；前端最终产物扫描规则可能需要按真实配置字段扩展。通过 Cargo/Clippy/测试补充语法与宏边界、专项负例、Harness bridge、能力触发、精确排除、首次真实 GUI 下游前向证据和 `Unverified` 标记降低风险。Rust 检查器 501–2000 行候选已复核为单一“Cargo workspace Rust 声明注释检查”职责，内部的 workspace 枚举、词法识别、诊断和 CLI 都服务同一结果模型，因此当前保留为一个高内聚文件。
- 验证：运行 Rust 检查器专项回归与 Harness bridge 回归；对完整中性 Rust workspace 单次根调用验证全部成员；使用来源 Web 已安装的 TypeScript/Vitest 只读依赖执行 GUI 参考检查器专项测试；运行默认 Python discovery、完整 Harness validator、行数门禁、Markdown 链接、升级器回归和差异检查。没有真实 GUI 初始化/构建候选，因此该前向范围保持 `Unverified`，不得声称已交付真实 GUI scaffold。
- 恢复或调整标准：真实下游若证明某项固定技术不满足 MSRV、Node/WebView、平台、安全、性能或渠道要求，必须用新硬规则例外 ADR 记录替代证据与迁移/恢复标准。若机械门禁稳定误报或漏报受管声明，应先补回归并收窄/修正 AST/词法规则；不得以降低语义要求、扩大目录排除或重新引入自动套话作为恢复方式。

## ADR-20260806-003：文件规模治理改为 500 行语义复核与 2000 行硬门禁

- 状态：Accepted
- 关联需求：项目负责人明确要求单文件超过 500 行时基于业务高内聚、职责单一和职责相近性考虑拆分，不满足即重构；超过 2000 行必须强制拆分；Rust 文件结构继续采用目录/`mod.rs` 形式。
- 背景：ADR-20260805-002 把全部人工维护文本设为 400 行硬上限，能够强制早期拆分，但也会机械拒绝职责高度集中且仍可独立审阅的文件。新的治理目标是把架构质量判断放回真实职责边界，同时保留一个不可降级的极端文件规模上限。
- 决定：原“所有人工维护文本超过 500 个物理行时必须在日常流程进入语义复核”的时点已由 ADR-20260908-006 取代；当前只在当次发布审查启用时逐项判断业务是否高内聚、是否围绕单一职责、各部分职责是否相近。三项均成立时可以保留至 2000 行，任一项不成立时必须按真实职责重构拆分；超过 2000 行仍无条件失败并强制拆分，2000 行通过、2001 行失败。
- Rust 结构：Rust 模块拆分统一使用 `<module>/mod.rs` 作为目录入口，职责相近的子模块置于该目录；不得以同级 `<module>.rs` 加同名目录或空壳转发层规避职责判断。
- 自动化边界：统一检查器机械列出 501 至 2000 行的复核候选，但不替代 Agent 的语义判断；只有超过 2000 行或检查器运行错误时返回失败。生成锁文件/生成物和原样隔离第三方文件继续沿用封闭范围排除。
- 替代关系：本决定明确替代 ADR-20260805-002 中“人工维护文本最多 400 行、401 行失败且无例外”的行数阈值、完成门禁和恢复标准；该 ADR 的项目记忆事件触发决定及历史实施事实继续有效。
- 理由：500 行阈值促使 Agent 在文件规模开始影响审阅时进行职责分析，避免为行数而拆出低内聚的空壳文件；2000 行硬门禁防止语义复核被用作无限增长的豁免。
- 风险：语义复核比单一阈值更依赖判断，可能出现不一致。缓解方式是检查器稳定列出候选，执行 Skill 强制报告三项判断，Rust 拆分结构保持确定性，2000 行以上继续机械失败。
- 验证：覆盖 500/501/2000/2001 行边界、Git 可见范围、生成文件排除、CLI 退出码与 Harness 报告接线；运行默认 Python 回归、完整 Harness validator、现行阈值引用扫描和差异检查。
- 恢复或调整标准：若 500 行语义复核长期产生不可接受的一致性或审阅成本，可由项目负责人重新调整复核字段或阈值；不得在没有新决定时放宽 2000 行硬门禁。

## ADR-20260806-002：新增异步优先、Tracing 落盘可读日志与产出物真实可用验收三项硬规则

- 日期：2026-08-06
- 状态：Accepted
- 关联需求：项目负责人明确要求为 Harness 新增三项约束：(1) 能使用异步的场景默认使用异步；(2) Tracing 可观测信息必须落盘到日志文件，且格式便于人类阅读与分析；(3) 所有产出物的验收都以真实可用为准，禁止以 Mock 冒充验收证据；并要求把三项约束转化为对 AI Agent 友好、可执行的表达写入 Harness 工程文档。
- 背景：现有规则已经把异步优先限定在 Rust 适配器的 I/O/等待/计时/进程/协议范围（`AGENTS.md`、`docs/RUST_CLI_TEMPLATE.md`），core 侧只是“可以”暴露 `async fn` 而非默认；tracing 已被 ADR-20260805-003 固定为可观测性标准，但只约束事件/span 结构化与脱敏，未规定落盘位置和可读性；`docs/VERIFICATION.md` 与 `docs/ENGINEERING_RULES.md` 已在里程碑验收层面禁止 Mock/stub/占位冒充真实候选，但未把“真实可用优先于 Mock”提升为覆盖全部产出物验收（含快速/标准路径完成声明）的通用原则，也未与既有测试隔离规则（对不可控外部依赖如网络、时间的合理测试替身）划清边界。三项都是长期、难以逆转的工程判断标准，按规则须记入当日 ADR。
- 决定：
  - **异步优先**：下游 Rust core 与全部适配器只要存在真实 I/O、等待、计时、进程、协议或跨边界调用，默认必须实现为异步（`async fn`），只有确认调用链纯 CPU 密集、无等待点、无跨边界调用时才保留同步实现；core 暴露的默认操作签名优先异步而非“可以选择异步”，且保持运行时中立（不出现 `tokio::runtime::Handle`、`JoinHandle`、Tokio 套接字或适配器类型），只有真实业务需要 Tokio 具体原语时才增加 core 的 Tokio 生产依赖。本决定强化而不替代 `docs/RUST_CLI_TEMPLATE.md` 现有“异步与运行时边界”一节的 CPU 密集判断、`spawn_blocking` 使用条件与例外记录要求。
  - **Tracing 落盘可读日志**：已启用 tracing 的下游 Rust 项目必须把结构化 event/span 同时写入本地日志文件，不得只写标准错误或丢弃；日志文件必须使用便于人类直接阅读与检索的格式，至少包含时间戳、级别、目标 target 与事件消息，并支持按时间或大小滚动避免无限增长；标准输出继续保持 `docs/CLI_CONTRACT.md` 规定的单一 JSON 文档，不得被日志污染。日志文件默认写入项目约定的本地日志目录，不得记录密钥、令牌、个人数据或未脱敏业务载荷，沿用 ADR-20260805-003 已有的脱敏要求；推荐通过 `tracing-subscriber` 的可读 `fmt` layer 搭配 `tracing-appender` 之类的滚动文件 writer 实现，或等效方案。
  - **验收以真实可用为准**：任何路径（快速、标准、里程碑）对产出物给出“完成”“可用”“已验证”结论时，必须以真实运行、真实调用产出物本身得到的可观察结果为依据；模拟实现、测试替身、占位页面、中性脚手架、开发预览、仅调用内部函数或仅展示 Mock 数据的结果不得被称为验收通过。该决定与既有测试隔离规则并行不冲突：单元/集成测试仍可以为不可控外部依赖（网络、当前时间、随机性、用户主目录等）使用受控测试替身以保证确定性，但测试替身证明的是代码单元内部正确性，不能被引用为“产出物已经真实可用”的验收证据；里程碑路径现有的真实候选、绑定源码提交和运行环境要求不变，本决定把同一判断标准明确扩展到快速/标准路径的完成声明措辞。
- 影响范围：`AGENTS.md`、`docs/RUST_CLI_TEMPLATE.md`、`docs/VERIFICATION.md`、最新 Product Spec、当日 Changelog。
- 不包含：不新增机械门禁强制扫描“未落盘的 tracing 调用”或“未异步化的函数”，语义判断仍由人工/Agent 依据代码执行路径审查；不改变里程碑路径已有的真实候选、签名、冒烟/E2E 门槛；不改变既有测试隔离规则允许的替身范围；不为中性初始化脚手架无条件添加日志文件写入依赖，只在真实业务出现 tracing 使用路径时按需引入 `tracing-subscriber`/`tracing-appender` 等配套 crate。
- 风险：把“异步优先”误读为强制所有函数异步化会在纯 CPU 密集路径引入不必要的运行时开销；日志落盘若配置不当可能无限增长或记录敏感信息；“真实可用验收”若与测试隔离规则混淆，可能被误用为禁止一切测试替身，损害测试的确定性和可重复性。通过保留 CPU 密集例外、复用现有脱敏与滚动要求、以及明确测试隔离与产出物验收的边界来降低风险。
- 验证：静态核对 `AGENTS.md`、`docs/RUST_CLI_TEMPLATE.md`、`docs/VERIFICATION.md`、最新 Product Spec 新增片段与本决定一致；运行默认 Python 回归（`python3 -m unittest discover -s scripts`）与 `python3 scripts/validate_harness.py` 确认文档链接、必需片段和文件行数门禁未退化。
- 恢复或调整标准：若真实下游证明某类操作因目标平台、宿主强制同步 API 或测量证据不适合异步化，必须按硬规则例外记录理由、影响范围和恢复条件；若日志落盘格式或位置需要适配特定部署环境，须由项目负责人明确确认后以新 ADR 调整；本决定持续有效直至被新的明确决定替代。

## ADR-20260806-001：固定下游 GUI i18n 技术选型事实标准，界面国际化成为开发期硬性必选项

- 日期：2026-08-06
- 状态：Accepted
- 关联需求：项目负责人明确要求在开发 GUI 工具时，i18n 支持是开发期必选项而非可选增强；要求为 Rust 后端与 React 前端分别确定成为事实标准的 i18n 框架；默认语言必须跟随系统语言；界面必须提供可发现的语言切换入口；并要求把上述约束整理写入文档与 Skill。
- 背景：`$desktop-add-gui-adapter` 此前只固定了 Tauri 2 + React + TypeScript + Mantine UI + TanStack Router + TanStack Query + Jotai 技术栈（见 ADR-20260805-001 与 `docs/RUST_CLI_TEMPLATE.md`），未对界面文案的国际化方式、系统语言探测来源和语言切换入口作出约束；缺少事实标准会导致不同下游各自选型、探测方式不一致、Rust 侧原生文案（托盘、通知、原生菜单）与 React 侧界面文案使用不同的语言状态来源，产生体验分裂和维护成本。
- 决定：
  - Tauri GUI 前端固定使用 `i18next` 搭配 `react-i18next` 作为 React 界面文案国际化事实标准，负责翻译资源加载、参数插值、复数规则和组件层语言消费；这是当前 React 生态最广泛采用的 i18n 组合，与已固定的 Mantine/TanStack/Jotai 技术栈均有成熟集成先例。
  - Tauri Rust 后端（GUI adapter 层，不含 core）固定使用 `rust-i18n`，负责系统托盘菜单、原生窗口标题、系统通知等未经过 React 渲染路径、必须由 Rust 直接产出的用户可见文案；`rust-i18n` 是 Rust 生态中该场景下载量与采用率最高的宏驱动 i18n crate。
  - 系统语言探测统一使用官方 Tauri 插件 `tauri-plugin-os` 的 `locale()` API，在应用启动时获取一次操作系统 BCP-47 locale，作为前端 `i18next` 初始化语言和 Rust 端 `rust-i18n` 当前语言的唯一探测来源；不得分别依赖 WebView 的 `navigator.language`、环境变量或平台专有 API 造成前后端探测结果不一致。
  - i18n 接入在 GUI 开发中是硬性必选项：任何真实产品 GUI 首次向用户交付带文案的页面、命令结果或原生机制文案时，必须同步接入上述 i18n 技术栈，不得以“当前只支持一种语言”“页面简单”为由推迟；中性初始化脚手架阶段允许暂不接入，但首次业务页面/命令必须已满足本决定。
  - 默认语言必须在首次启动时使用 `tauri-plugin-os` 探测到的系统 locale；探测到的语言没有对应翻译资源时回退到英文。GUI 必须提供可发现的语言切换入口（如设置页或菜单项），用户手动切换后的选择必须持久化并在后续启动时覆盖系统探测结果，直至用户重置。
- 能力边界：固定选型表示 GUI 能力被选中且进入首次真实页面/命令开发时必须采用，不表示中性初始化无条件预装；未选择 GUI 的下游不引入 `i18next`、`react-i18next`、`rust-i18n` 或 `tauri-plugin-os`。具体已支持的语言列表、翻译文案内容和文案来源由下游产品按真实需求确定，本决定不预设任何具体语言集合。
- 架构边界：翻译资源、语言判断、区域化格式化和语言切换交互属于 GUI 前端与 Tauri 适配层职责；core 必须保持语言无关，只暴露稳定、语言中立的领域错误标识、结构化数据和机器可读时间戳（沿用既有 jiff 决定），由 GUI 适配器和 `i18next` 把错误标识、数值和日期时间映射为当前语言的展示文案。不得把翻译文案、语言选择逻辑或区域化格式化规则下沉到 core，即使当前只有 GUI 一个消费者也同样适用；这与 ADR-20260805-001 的 core-first 边界一致。
- 依赖边界：前端依赖通过 pnpm 包清单与锁文件引入 `i18next`、`react-i18next`；Rust 端依赖通过根 `[workspace.dependencies]` 声明 `rust-i18n` 与 `tauri-plugin-os`，且只在 GUI 适配器成员中启用，不得让 core 或其他适配器依赖上述任一 i18n 包。用户语言偏好是 GUI 本地的设备级展示偏好而非权威业务状态，必须由 GUI 适配器自有的本地持久化位置（而非共享 core 的持久化层）保存，不得借此新建第二套业务数据权威副本。
- 影响范围：`docs/RUST_CLI_TEMPLATE.md`、`.agents/skills/desktop-add-gui-adapter/SKILL.md`、`.agents/skills/desktop-add-gui-adapter/references/gui-baseline.md`、`.agents/skills/desktop-add-gui-adapter/references/react-frontend-baseline.md`、`AGENTS.md`、最新 Product Spec、当日 Changelog、`scripts/harness_validation_governance_tests.py` 中引用最新 Product Spec 文件名的回归。
- 不包含：不实现任何具体产品的翻译文案或语言列表；不构建、验收或发布候选；不为 CLI/TUI/MCP 适配器引入 i18n 义务（当前决定范围限定在 Tauri GUI）；不新增机械门禁强制校验文案是否已翻译，该项留待真实下游出现具体页面后按需评估。
- 风险：把“必选项”误读为初始化阶段无条件预装会增加中性脚手架体积；把语言判断或翻译文案下沉到 core 会破坏 core 语言中立性并可能诱发第二套展示状态；分别探测系统语言会造成 Rust 原生文案与 React 界面文案语言不一致。通过能力触发、core-first 架构边界重申、统一 `tauri-plugin-os` 探测来源和本决定的机械回归降低风险。
- 验证：静态核对 `rust-i18n`、`react-i18next`/`i18next`、`tauri-plugin-os` 在 GUI baseline、React 前端 baseline、`$desktop-add-gui-adapter` SKILL 和 `docs/RUST_CLI_TEMPLATE.md` 中的职责、能力触发与 core/adapter 边界描述一致；运行默认 Python 回归、完整 Harness validator、Markdown 链接和文件行数检查确认无退化。
- 恢复或调整标准：若真实下游证明 `i18next`/`react-i18next`、`rust-i18n` 或 `tauri-plugin-os` 无法满足目标 WebView、Rust MSRV、平台或安全约束，必须先记录替代技术、证据、迁移影响和恢复条件的硬规则例外 ADR；永久替换本事实标准须由项目负责人明确确认并以新 ADR 取代本决定。

## ADR-20260805-004：ADR/Changelog/Work Plan/Product Status/Product Spec 改为只保留最新日期文件

- 日期：2026-08-05
- 状态：Accepted
- 关联需求：项目负责人明确要求清理 `docs/adr/`、`docs/changelog/`、`docs/work_plan/`、`docs/project_status/`、`docs/product_spec/` 下的历史日期文件，视其为可清理的中间产物；经确认后仍要求直接删除，即使这与 ADR-20260805-002 当日已接受的“不改写旧日期 ADR、Changelog 或 Verification 的真实历史”条款冲突。
- 背景：五类按日项目记忆此前的写入规则要求旧日期文件永久保留作为审计轨迹，且 ADR-20260805-002 当日已把该保留义务写成明确决定。随着仓库运行超过两周，历史日期文件数量持续增长、彼此高度重复（同一批 core-first/行数门禁/技术选型决定被跨多个日期反复复述），检索和阅读成本上升；`docs/ENGINEERING_RULES.md` 第 128 行本身也已确立“文档不保存被删除内容的注释版本；历史交给版本控制”的原则，只是此前未应用到这五类目录。`docs/verification/` 与 `docs/verification/human_review.md` 受独立单元测试（`scripts/harness_validation/test_document_partitions.py`）保护，记录里程碑/发布/人工复核证据，不属于本决定范围。
- 决定：`docs/adr/`、`docs/changelog/`、`docs/work_plan/`、`docs/project_status/`、`docs/product_spec/` 五类目录此后只在仓库中保留日期最新的一份日期文件；一旦新自然日文件写入并综合了前一份仍有效的事实，旧日期文件立即从工作树删除，不再另存副本或归档目录。历史事实的唯一恢复入口是 Git 版本控制（`git log`/`git show` 定位对应提交）。本决定同时修正 ADR-20260805-002“历史决定”一节中关于 ADR/Changelog 的保留表述，理由是同一未发布自然日内的决定允许被后续决定完全替代（`docs/adr/README.md` 写入规则）。
- 不适用范围：`docs/verification/` 证据分卷、`docs/verification/human_review.md`、`docs/HARNESS_ENGINEERING.md` 与 `docs/harness_engineering/` 方法论分卷不改变留存规则，继续保留全部历史、签署字段与失败事实，并由既有单元测试守护。
- 影响范围：`docs/adr/README.md`、`docs/changelog/README.md`、`docs/work_plan/README.md`、`docs/project_status/README.md`、`docs/product_spec/README.md` 的写入规则与日期文件索引；`docs/ENGINEERING_RULES.md` 相关表述；当日 Changelog。
- 不包含：不改变 `docs/verification/` 和方法论分卷的留存规则；不改变同一天只维护一份文件、新自然日需综合重写前一份等既有写入规则；不追溯要求为已删除的历史内容单独制作摘要或归档文件。
- 理由：五类目录的历史价值已由 Git 提交历史完整承载，仓库内重复保留只增加检索噪音和长期维护负担；里程碑、发布和人工复核相关的强审计需求由 `docs/verification/` 独立满足，两者不需要同一套留存强度。
- 风险：若后续需要快速比对某历史日期的完整快照，需改用 `git show <commit>:docs/xxx/YYYYMMDD_xxx.md` 而非直接在工作树浏览，检索便利性下降；跨系统迁移或不携带 Git 历史的仓库副本会丢失这部分记忆。通过在本条目和索引文件中明确指向 Git 历史作为恢复路径来降低风险。
- 验证：`python3 scripts/validate_harness.py` 与 `python3 -m unittest discover -s scripts` 全部通过；`docs/verification/` 分卷事实守恒测试（`test_document_partitions.py`）未受影响；人工核对五类目录仅剩 2026-08-05 一份日期文件且索引、Markdown 链接一致。
- 恢复或调整标准：若项目负责人后续认定需要恢复某类目录的历史留存，须以新 ADR 明确恢复范围，并可从 Git 历史中检出对应日期文件写回工作树；本决定持续有效直至被新的明确决定替代。

## ADR-20260805-003：固定下游 Rust 技术选型事实标准（Rust 版本与依赖选择规则已由 ADR-20260831-007 取代）

- 日期：2026-08-05
- 状态：Partially superseded by ADR-20260831-007；技术族与职责边界继续有效
- 关联需求：项目负责人明确要求先把 Tokio、Axum、Clap、SeaORM、tracing、anyhow、thiserror、serde 与 jiff 固化为 Rust 技术选型事实标准，未来再继续扩展。
- 决定：Tokio 是异步运行时标准；Axum 是获批 Rust HTTP 服务标准；Clap 是 CLI 参数与子命令标准；SeaORM 是获批关系型数据库 ORM 标准；tracing 是结构化可观测性标准；anyhow 用于二进制和适配器装配边界的错误上下文；thiserror 用于 core、库和可恢复边界的稳定类型化错误；serde 是序列化/反序列化标准；jiff 是日期、时间、时区、时间戳和时间跨度标准。
- 能力边界：固定选型按真实能力触发，不等于中性初始化无条件安装所有依赖。未批准 HTTP 服务时不引入 Axum，未批准关系型持久化时不引入 SeaORM，没有真实观测、序列化或日期时间路径时不添加占位依赖。Tokio 随所选 Rust 适配器生效，Clap 只随 CLI 生效；Axum 不恢复已经移除的独立 WEB 适配器。
- 架构边界：Axum 路由、提取器和响应映射属于 HTTP 边界，SeaORM 实体与驱动属于基础设施边界，业务规则、迁移策略、事务语义和结果解释仍由 core 决定。thiserror 保留跨接口可判定错误，anyhow 只补充应用边界上下文，不得成为公开稳定领域错误契约。tracing 使用结构化 event/span，并禁止记录密钥、令牌、个人数据或未脱敏业务载荷。
- 依赖边界：全部 Rust 依赖仍由根 `[workspace.dependencies]` 统一声明，成员只用 `workspace = true`；只启用满足真实能力的最小 feature，并在 Rust 1.90、Windows/macOS/Linux 与锁定验证约束内采用较新兼容稳定版本。
- 影响范围：最新 Product Spec、Rust 基线、AGENTS/README、初始化与实施 Skills、Harness validator、回归测试和当日 Changelog。
- 不包含：本轮不选择具体版本、不更新中性 Cargo.lock、不实现 HTTP 服务、数据库、日志管线或产品业务，也不构建、验收或发布候选；后续扩展技术族由项目负责人继续确认。
- 风险：把“固定”误读为全部预装会增加编译体积、供应链面和无用抽象；把 anyhow 下沉到 core 会破坏稳定错误匹配；把 tracing 当作任意载荷日志会产生数据泄露。通过能力触发、错误分层、最小 feature、敏感字段禁录和机械事实门禁降低风险。
- 验证：静态核对九项技术的职责、能力触发、core/adapter 所有权和例外路径；新增 validator 必需片段回归，并运行默认 Python 回归、完整 Harness validator、Markdown 链接、文件行数与差异检查。
- 恢复或调整标准：真实下游若证明某项固定技术无法满足 MSRV、目标平台、安全、性能或渠道硬要求，必须先记录替代技术、证据、迁移影响和恢复条件的硬规则例外 ADR；永久替换事实标准须由项目负责人明确确认并以新 ADR 取代本决定。

## ADR-20260805-002：项目记忆排除日常维护流水账，并硬限制人工维护文本为 400 行

- 日期：2026-08-05
- 状态：Accepted
- 关联需求：项目负责人明确要求 Bug 修复、重构等维护操作不计入 ADR、Changelog 等项目记忆，并严格限制单文件不得超过 400 行。
- 背景：现有事件触发规则仍允许用户可感知的缺陷修复进入 Changelog，多个执行入口也可能因“发生了修改”而更新其他记忆；这会把维护流水账混入产品边界、长期决定和交付证据。现有文件规则只是 400/800 行软审查，validator 只给警告，仓库已有多份维护脚本、测试和长文档超过 400 行。字面限制所有 UTF-8 文件又会覆盖 Cargo/pnpm 等工具生成且不可拆分的锁文件，破坏当前可复现构建契约。
- 记忆决定：普通缺陷修复、不改变可观察行为的纯重构、格式整理、测试补强和内部清理本身不触发 Product Spec、ADR、Product Status、Changelog 或 Verification；完成结果只在最终回复和测试/CI 中报告。复杂、多模块、跨会话或中风险维护仍可因执行协调独立建立 Work Plan。若同一任务另外改变产品边界、引入长期决定或硬规则例外、形成重要阻断/交接、进入发布/人工复核/长期审计，或触发安全/渠道硬要求，只记录该独立事件；“修复”或“重构”标签不得绕过门禁。已有 TECH_DEBT 条目恢复准确状态不属于新增修复记录。
- 行数决定：所有由人类或 Agent 维护的文本文件，包括源码、测试、脚本、文档、配置、Skills、参考和模板，统一按 `splitlines()` 等价的物理行计数并硬限制为最多 400 行；400 行通过，401 行失败。超限必须按职责拆分，不能以警告、计划说明或 ADR 例外降级。工具生成且禁止手工编辑的锁文件/生成物与原样内嵌第三方文件不属于人工维护文件；这是范围定义而非超限豁免，其生成器、模板和维护说明仍受门禁。
- 机械决定：提供可随 `$desktop-implement-change` 保留到下游的单一检查器，使用 Git 的 NUL 分隔清单覆盖已跟踪和未忽略的未跟踪文件，不跟随符号链接，并明确报告枚举、读取和 UTF-8 文本判断失败。Harness validator、初始化、实施、里程碑验证和发布准备复用该门禁；升级传播必须同时包含检查器和回归测试。
- 历史决定：不改写 Verification 的真实历史，过长的现有 Verification 和方法论文档只做事实守恒拆分，不删除失败证据、人工签署或有效方法论。ADR、Changelog、Work Plan、Product Status、Product Spec 五类按日记忆改为只保留最新日期文件，旧日期文件随即删除、历史交由 Git 版本控制承担（见 ADR-20260805-004，同日修正本条）。当前未发布的同日 Changelog 合并为新规则下仍合格的新增/变化，移除作为维护流水账的“修复”分组。
- 影响范围：最新 Product Spec、工程规则、AGENTS/README、项目记忆索引、执行/验证/发布/升级 Skills、Harness 方法论、当前 Verification 组织、下游初始化与升级传播、文件行数检查器、validator 和回归测试。
- 不包含：不改变产品用途、接口组合、core-first 职责、发布授权或历史验收结论；不要求手工拆分或编辑 Cargo/pnpm 等工具生成锁文件，也不把普通维护任务包装成新的长期决定。
- 理由：事件触发的项目记忆能保留产品、架构、阻断和交付事实而不制造实现流水账；统一 400 行硬门禁可把职责拆分从建议变为可重复验证的完成条件，同时保持生成依赖锁的真实性与可复现性。
- 风险：过度排除可能漏记被称为“修复”的安全、发布或长期架构事件；通过独立触发器优先和禁止用任务标签绕过门禁降低风险。大规模文件拆分可能造成导入、测试发现、链接或历史证据丢失；必须以行为回归、链接检查、事实守恒复核和完整 validator 收口。
- 验证：覆盖 400/401 行、无尾换行、隐藏/空格路径、已跟踪/未忽略文件、忽略文件、二进制、符号链接、生成锁文件及 Git/读取失败；拆分全部现有超限人工维护文件；运行默认 Python 回归、升级器回归、完整 Harness validator、Markdown 链接与差异检查。
- 恢复或调整标准：若某类文件被证明不是人类或 Agent 维护，必须以可机械识别的工具生成或第三方来源纳入封闭分类，不得按路径任意放宽。人工维护文本的 400 行上限和日常维护不形成项目记忆流水账持续有效，除非项目负责人以新的明确产品约束替代。

## ADR-20260805-001：强制采用 core-first 与 CLI/TUI/MCP/GUI 薄适配器架构

- 日期：2026-08-05
- 状态：Accepted
- 关联需求：项目负责人要求后续业务逻辑尽可能由 core 统一实现，CLI、MCP、TUI、GUI 只作为调用 core 的薄层；系统托盘等接口或平台特有能力仍由对应适配器实现，并要求本轮完成当前未完成目标。
- 背景：现有 Harness 已禁止 core 反向依赖适配器，并要求 CLI 不重复业务规则，但最新 Product Spec 没有保留早期“业务在 core、适配器只映射”的完整约束，Rust 基线又只对 CLI 写出明确薄层职责。方法论文档还把“平台能力”笼统列入 core，可能诱导把系统托盘、窗口生命周期或终端恢复错误下沉；反过来，MCP/GUI 的“验证输入”和 TUI 的“应用状态”也可能被误读为允许在适配器内实现领域校验、业务状态和用例编排。
- 决定：Core-first 成为所有下游的硬架构规则。凡不依赖某一具体接口或宿主才能成立的领域类型、业务规则、语义校验、业务默认值、应用用例与工作流编排、领域状态转换、稳定领域错误、平台无关权限、权威业务状态、迁移与持久化策略，都必须在共享 core 中实现并通过明确 API 暴露；即使当前只存在一个适配器也同样适用。
- 薄层决定：CLI、TUI、MCP、GUI 仅负责运行时和依赖装配、接口语法或协议结构解析、展示与纯交互状态、调用 core，以及把 core 结果/错误映射为参数错误、协议结果、视图状态或退出码。适配器不得复制、改写或另建业务规则、平台无关校验、业务权限、权威业务状态、迁移或持久化策略；包含条件、重试或状态决策的跨 core 调用编排必须提升为单个 core 用例 API。
- 校验决定：适配器可以拒绝无法解析、缺少协议必填字段或违反宿主能力约束的调用；值域、跨字段关系、资源状态、业务权限、幂等性、可否执行以及会改变业务结果的默认值由 core 判定。格式正确但业务无效的输入必须进入 core，并在全部适配器中保留同一稳定领域错误。
- 平台边界：CLI 参数/标准流/退出码，MCP schema 注解/stdio 生命周期，TUI 焦点/选择/滚动/按键/终端恢复，以及 GUI React 状态、窗口/WebView、CSP、系统托盘、通知、自动启动和平台生命周期属于对应适配器。托盘点击、快捷键或通知动作触发的业务行为仍调用 core；平台机制本身不是 core-first 例外。文件系统、网络、外部进程等具体驱动可留在适配器或基础设施模块，何时调用、业务策略和结果如何改变领域状态仍归 core；真实需要时 core 可以定义运行时中立的能力接口，由适配器装配实现，不为假想未来预建抽象。
- 依赖决定：每个适配器直接依赖 core；core 不直接或通过 workspace 中间 crate 到达任何适配器或接口框架；适配器彼此不直接或通过 workspace 中间 crate 依赖、启动或解析输出。不含接口依赖的基础设施 crate 仍可被 core 合法复用。薄层按职责判断而不按行数、分支数或文件大小判断，协议映射和展示代码较长不自动构成违规。
- 测试决定：新增或修改业务行为时先提供 core 的核心成功路径和最高风险失败路径测试，再覆盖适配器映射与接口契约。每个公开操作在计划、实施或完成输出中记录“适配器操作 → core 用例 API → core 测试”映射；仅修改布局、协议映射或平台机制而不改 core 时，必须说明其接口/宿主专属性。语义归属仍由 Agent/人工评审，自动门禁只验证确定的依赖方向、必需规则和中性资产边界，不用关键词或代码行数伪装完整语义证明。
- 例外：普通接口/平台机制按上述边界实现，无需 ADR。真正把接口无关业务保留在适配器属于硬规则偏离，必须按 `docs/ENGINEERING_RULES.md` 记录理由、范围、风险、替代验证和恢复标准；“目前只有一个接口”、实现方便或避免新增 core API 不是可接受理由。
- 影响范围：最新 Product Spec、AGENTS/README、工程规则、Rust 基线、方法论文档、产品定义/规划/实施/验收/初始化 Skills、四类适配器 Skills 与参考、默认 Skill 提示、中性 Rust 资产、Harness validator、回归测试、升级受管工程层和变更记录。
- 不包含：不在 Harness 根实现具体产品业务；不为未知产品增加通用服务容器、仓库 trait、插件系统、额外 crate 或跨接口注册器；不把纯 UI/协议/平台状态塞入 core；不构建真实下游、候选或发布物，也不运行冒烟/E2E。
- 理由：把接口无关行为放在单一 core 中可让未来新增接口直接复用相同业务语义、错误和测试，并防止多个入口产生行为漂移；同时保留明确的平台机制边界，可以避免 core 反向耦合 Tauri、终端、MCP 或 CLI。
- 风险：过度集中可能把焦点、窗口或协议状态误放 core，也可能促使维护者为假想适配器提前抽象；只依赖文本片段的门禁无法证明真实业务语义。通过职责清单、单适配器同样适用、平台机制正例、禁止按行数判薄层、依赖图检查和语义复核降低风险。
- 验证：增加 core-first 规则/Skill 必需片段与负向退化测试；校验中性 core 不含接口语义和接口框架生产依赖，CLI 直接依赖 core；为可复用检查器覆盖合法方向、缺失 core 依赖、core 反向依赖、adapter 间依赖、重命名/目标依赖与接口框架泄漏。运行相关 Python 回归、中性 Rust 测试、Skill 结构/Markdown 链接、完整 Harness validator 和差异检查。
- 恢复或调整标准：若真实下游证明某项行为只能依赖特定接口或宿主，则把机制保留在该适配器并记录边界；若其中仍存在可复用业务决策，则只下沉该决策到 core。若 core 开始包含展示/协议状态，应把这些状态移回适配器；若适配器出现重复业务分支，则抽取为 core 用例。除非项目负责人以新的长期决定替代，本硬规则持续有效。
