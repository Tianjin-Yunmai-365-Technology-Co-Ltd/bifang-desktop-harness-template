# 变更记录索引

本目录是符合本项目触发规则的用户和维护者可感知变化的唯一正文来源；本项目不使用普通缺陷修复的 `Fixed` 类别。Harness 版本使用 `Version.md` 声明的上海时区 `YYYYMMDDHHMM`；下游产品默认遵循语义化版本规范 2.0.0。

## 写入规则

- 文件名固定为 `YYYYMMDD_CHANGELOG.md`，同一天的实际变化持续更新同一文件，并以该日结束时仍有效的最新描述为准。
- 只记录已经发生且符合资格的新增、变化、移除、安全事项和重要已知问题；未实施需求按 Product Spec、ADR 和 Work Plan 各自触发条件处理，不进入 Changelog。
- 普通缺陷修复、不改变可观察行为的纯重构、格式整理、测试补强和内部清理一律不进入 Changelog，即使其结果可感知；只在最终回复和测试/CI 中报告。若同一任务另有安全事项、产品/契约变化、发布要求或其他独立合格事件，只记录该事件。
- 除上述排除外，已经发生且用户或维护者可感知的行为、契约或维护流程变化写入当日变更记录；不要求写“不适用”占位。
- 同一未发布日期内的中间态和随后被替代的描述应直接合并或删除，避免把已经失效的规则继续暴露为当前能力。
- 发布准备从日期文件汇总对应版本内容；不得把尚未实现或未验证的能力写成已交付。
- 新下游只在首次出现符合上述范围的实际变化时创建日期记录；产品规格转为 `Approved` 本身、普通缺陷修复和纯重构均不要求预建 Changelog。
- 只保留日期最新的 Changelog 文件；被后续日期完全取代的旧日期文件随即删除，不在仓库中另存历史副本，历史由 Git 版本控制承担（见 ADR-20260805-004）。

## 日期文件

- [2026-08-05](20260805_CHANGELOG.md)
