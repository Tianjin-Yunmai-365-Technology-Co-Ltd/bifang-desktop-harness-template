# 工程规则

本文档是所有下游项目在文件组织、代码注释、文档维护、测试组织、规则例外和机械检查方面的唯一详细事实来源。`AGENTS.md`、项目 Skills 和技术基线只保留任务入口、语言特定补充与本文件链接，不复制整套规则。

## 1. 适用范围与规则等级

- 本规则适用于所有由 Harness 建立或维护的下游项目。通用规则不绑定语言；语言或工具链文档可以增加不削弱本规则的具体要求。
- Harness 自身的文档、Skills 和随附的中性资产必须体现本规则。新建和本次修改的内容立即遵守；未触及的存量维护脚本采用渐进治理。
- 当前规范根同时包含 Harness 专用 `Version.md` 和活动 `.agents/skills/desktop-instantiate-project/SKILL.md` 时，该根只接受 Harness 自身工程维护，以及创建终端下游所需的固定初始化字段和所选初始化路径精确要求的身份选择。产品目的、业务功能、产品专属 UI/文案/数据、远程地址、凭据、产品构建或发布需求不得在 Harness 源中接收、分析、记录或实施；即使与合法初始化字段同一消息提供，也只能拒绝并要求用户在初始化完成、切换到唯一终端下游根目录后重新提出。
- **硬规则**用于保护架构边界、正确性、跨平台能力和交付可信度。违反时必须修复；确需例外时按第 6 节记录决策。
- **默认规则**用于保持可读性和一致性。有真实、局部且可说明的理由时可以偏离，不要求为普通组织选择增加正式决策记录。

## 2. 文件、模块与依赖边界

### 2.1 硬规则

- Core-first 是硬规则。凡不依赖某一具体接口或宿主才能成立的领域类型、业务规则、语义校验、业务默认值、应用用例与工作流编排、领域状态转换、稳定领域错误、平台无关权限、权威业务状态、迁移和持久化策略，都必须在共享 core 中实现并通过 core 自有类型的明确 API 暴露；即使当前只选择一个适配器也同样适用。
- CLI、TUI、MCP、GUI 适配器必须保持薄层，只负责运行时与依赖装配、接口语法或协议结构解析、展示和纯交互状态、调用 core，以及把 core 结果和错误映射为接口输出。薄层按职责判断，不按文件行数、分支数或协议映射代码量判断。
- 适配器不得复制、改写或另建业务规则、平台无关校验、业务权限、权威业务状态、迁移、持久化策略或稳定领域错误。适配器处理器若需要用条件、重试或状态决策编排多个 core 调用，必须把该编排提升为一个 core 用例 API。
- 输入检查必须区分接口语法/协议结构与领域语义。无法解析、缺少协议必填字段或违反宿主能力约束的调用可由适配器拒绝；值域、跨字段关系、资源状态、业务权限、幂等性、可否执行和会改变业务结果的默认值必须由 core 判定，并在所有适配器中保留同一稳定领域错误。
- 接口或宿主特有机制属于对应适配器：例如 CLI 参数、标准流和退出码，MCP schema 注解与 stdio 生命周期，TUI 焦点、选择、滚动、按键和终端恢复，以及 GUI 单实例、深链接、全局快捷键、路由/查询/交互状态、窗口状态、WebView、CSP、系统托盘、通知和自动启动。托盘点击、深链接、快捷键、通知动作或界面事件触发的业务行为仍必须调用 core；中性单实例与深链接只恢复既有窗口，中性全局快捷键能力不预置动作或绑定。这些平台机制本身不构成 core-first 例外。
- 文件系统、网络、外部进程和操作系统 API 等具体驱动集中在职责明确的适配器或基础设施模块。何时调用、允许什么、失败如何解释以及结果怎样改变领域状态属于 core；真实边界需要时可由 core 定义运行时中立的能力接口并由适配器装配实现，不得为假想未来预建抽象。
- GUI 初始化必须先把 `system_tray`、`system_notification`、`autostart`、`about_page`、`sponsor_page`、`single_instance`、`deep_link`、`global_shortcut` 的 `enabled|disabled` 与 `sidebar_mode` 的 `compact|detailed` 按该顺序写入 `docs/GUI_APP_PROFILE.md` 唯一 `gui-initialization-config` 代码块。八项条件能力不得推断、遗漏或残留 `pending`；未选择侧栏时归一化为 `detailed`，最终九项不得缺失。`deep_link = enabled` 必须同时有 `single_instance = enabled`，非法组合必须拒绝并重新确认，不得自动更正。`global_shortcut = enabled` 时还必须有唯一 `gui-global-shortcut-contract` JSON 块；中性初始化固定为 `schemaVersion: 1` 与空 `actions`，不得注入默认 chord、恢复窗口动作或其他产品事实，禁用时该块必须缺席。`system-locale`、`updater`、`window-state` 与 `dialog` 不是 profile 字段，选择 GUI 后不询问且无条件建立。
- 所有 GUI 无条件调用 `$desktop-add-gui-system-locale`、`$desktop-add-gui-updater`、`$desktop-add-gui-window-state` 与 `$desktop-add-gui-dialog`。根 `[workspace.dependencies]` 统一声明 `tauri-plugin-os = "2.3.2"`、`tauri-plugin-updater = "2.11.0"`、`tauri-plugin-window-state = "2.4.1"`、`tauri-plugin-dialog = "2.7.3"`，GUI member 只用 `workspace = true`；前端生产依赖固定声明 `@tauri-apps/plugin-dialog = "^2.7.3"`。os、updater、window-state 三项保持 Rust-only：不安装对应 JavaScript 包，不向 WebView 授予 `os:*`、`updater:*` 或 `window-state:*` ACL。dialog 是唯一固定 WebView plugin 基线：主窗口 capability 必须精确包含 `dialog:default`，以官方默认权限集开放 message、open、save 全部对话框类型；不得使用 wildcard、deprecated `ask`/`confirm` alias、`dialog:deny-*` 或额外文件系统权限，dialog 也不得加入自有 `invoke_handler`。系统语言唯一来源是 `locale()`；updater 无完整产品配置时固定为 `NotConfigured` 且零出站，并在 `tauri.conf.json` 显式写入 `plugins.updater = { endpoints: [], pubkey: "" }`，不得依赖缺省或 `null` 配置，也不得借空配置注入 endpoint、签名事实或出站能力；window-state 只恢复 `SIZE | POSITION | MAXIMIZED`，不恢复可见性、最小化、全屏或窗口装饰。中央 Builder 顺序固定为：启用时的 single-instance、启用时的 deep-link、固定 os、固定 updater、固定 window-state、固定 dialog、启用时的 notification、autostart、global-shortcut，每项恰好注册一次；托盘不是 plugin，只从 `.setup(...)`/`.on_window_event(...)` 接线。
- 所有 GUI 固定公开 `get_app_metadata`、`get_system_locale` 与 `set_interface_language` 三个窄 Tauri command，并由 React 实际消费。`get_app_metadata` 必须返回由打包名称、Cargo 版本、联系人、产品定义状态与动态标题组成的权威类型化元数据，不得返回占位字符串或由浏览器环境猜测；`get_system_locale` 必须通过官方 OS locale 与已保存语言状态解析当前语言，`set_interface_language` 必须规范化输入、更新同一权威语言状态并同步 `rust_i18n`。中央 Builder 只能有一个合并后的 `.invoke_handler(tauri::generate_handler![...])`，其中三项固定命令与按 profile/页面启用的条件命令各恰好出现一次；不得按 Skill 重复注册 handler、遗漏固定命令或用同名空壳满足结构检查。
- GUI 初始化固定建立 adapter-only 的动态标题、设置页、i18n、三态主题、亮暗语义主题和用户选择的 Logo。Logo 流程必须在生成前按请求顺序绑定 `candidate-1`、`candidate-2`、`candidate-3`，始终以该顺序展示生成工具交付的三个原始候选；用户选择前不得验证格式、尺寸、色彩、像素、摘要或质量，也不得转码、缩放、裁剪、补边、改色、压缩、覆盖重命名或标准化。用户明确选择后只验证和按需标准化所选项，拒绝项不补做处理，再生成平台图标和本地 `/app-identity/logo.png`。`/about` 与 `/sponsor` 分别只在相应选择启用时建立，未选页面不得留下路由、导航、组件或运行时资源。所有 GUI 展示与交互先按 [`docs/design_standards/README.md`](design_standards/README.md) 精确匹配，产品 profile/当前请求中已批准的专属标准优先；侧栏的唯一像素与 AppShell 接线来自 [`docs/design_standards/tauri_sidebar.md`](design_standards/tauri_sidebar.md)。当前 compact 为 `80px`、`6px` 内容内边距、`36px` Logo、`22px` 图标和全宽居中名称，不得使用固定 `em/ch` 盒或折叠按钮；detailed 为 `248px`/`76px` 与 `72px`/`44px` 两档且同样使用 `22px` 图标。detailed 的独立设备偏好由 GUI AppShell 拥有，侧栏 ActionIcon 回调、`navbar.width` 与 `data-navbar-width` 必须同步，不得进入 core 或页面会话 store。功能项向下增长，底部按已选赞助、固定设置、已选关于生成。主窗口首次启动或持久状态缺失、损坏、越界时以 1440×900、最小 960×640、居中作为回退，后续仅恢复仍与当前显示器工作区相交的尺寸/位置/最大化状态，并与 660×400 DMG 窗口独立；设置页不含隐私/统计控件。所选关于页提供固定 updater 基线的 `NotConfigured`/零出站检查、更新日志、作者、联系人和免责声明；禁用关于页只移除该页及其加载命令，不得裁掉 updater Rust 基线或根级强更门。所选赞助页使用运行时主题并打包完整 sponsor 媒体。真实更新、强更或统计仍需独立产品批准与安全配置。
- UI 标准中的像素、密度、组件层级、交互所有权和可访问名称是硬规则。没有精确命中或用户要求特殊设计时，必须在实现前取得明确批准；像素或信息架构偏离同步更新下游 `docs/GUI_APP_PROFILE.md` 与当日 ADR。Harness 源不创建虚假的产品 profile，初始化也不得自行发明新密度。
- 所有 GUI 初始化必须保留发布专用 `src-tauri/tauri.release.conf.json`，其中资源 map 只能按真实 GUI Cargo manifest 根把项目根 `release-notes.json` 映射为 `release-notes.json`：Cargo 位于 GUI 根时使用 `../release-notes.json`，传统 `src-tauri/Cargo.toml` 布局使用 `../../release-notes.json`，同时存在或都不存在时失败关闭；中性调试构建不传该配置，也不提前创建根发布事实。正式 Tauri 构建必须显式以 `--config src-tauri/tauri.release.conf.json` 合并它，并在打包前校验映射/源摘要、打包后逐字节核对实际应用资源；macOS 最终 DMG 还必须只读挂载并比较唯一 `.app/Contents/Resources/release-notes.json`。`about_page = enabled` 时 GUI adapter 注册无路径参数的异步 `load_release_notes`，以 `BaseDirectory::Resource` + `tokio::fs` 读取固定资源，经 Rust 与 React 双层 schema/上限校验后展示 loading/error/retry；不得为此启用通用前端文件系统权限或同步 `std::fs`。禁用关于页时命令、加载器、弹窗和文案缺席，但正式候选仍携带更新日志供制品与 manifest 核验。
- 仅当 `system_tray = enabled` 时才调用 `$desktop-add-gui-system-tray`，启用 `tray-icon` 并应用完整托盘硬契约：`src-tauri/icons/32x32.png` 是普通非符号链接的 32×32 8-bit RGBA 非交错 PNG，至少一个像素非透明且被 `bundle.icon` 引用；安装函数从 Tauri Builder `.setup(...)` 可达并绑定双项 `Menu`、必需 `default_window_icon()`、`.icon(...)` 与 `.build(app)`，关闭隐藏由 `.on_window_event(...)` 注册。稳定 ID 为 `show_window`/`quit`，可见标签通过 `rust_i18n` 解析为中文“显示窗口/退出”或英文“Show Window/Quit”并支持无重启刷新；左键和显示项恢复聚焦，关闭只隐藏，退出项结束应用。结构检查、六个固定命名回归和真实宿主场景全部适用；透明点击区域、无可见图形或未调用死代码均不能通过。选择 `disabled` 时不得保留 feature、安装函数、菜单/locale 资源、`prevent_close()` 或隐藏调用；必须由 `.on_window_event(...)` 接入主窗口 `CloseRequested` 并显式调用 `AppHandle::exit(0)`，再由 `close_last_window_exits_application` 与真实 E2E 锁定。
- 六项条件宿主能力必须分别由 `$desktop-add-gui-system-tray`、`$desktop-add-gui-system-notifications`、`$desktop-add-gui-autostart`、`$desktop-add-gui-single-instance`、`$desktop-add-gui-deep-link` 和 `$desktop-add-gui-global-shortcut` 拥有，只在对应 profile 字段为 `enabled` 时调用；禁用时其依赖、feature、插件/生命周期、配置、命令、ACL、设置/状态、i18n 与专属回归/场景必须缺席。通知保持 Rust-only 平台分流、生命周期拥有的串行 worker、默认关闭和可见失败；macOS 开启时必须先读系统授权状态，仅 `NotDetermined` 请求并复读，只有明确授权才持久化，拒绝/受限/仍未授权则通过只接收内部 `AppHandle` 的固定 Rust opener 打开当前应用的 Notifications 系统设置并保持关闭，打开失败也必须可观察。自启以 OS 状态为权威、默认不注册并在初始化 E2E 恢复原登录项；单实例回调仅恢复窗口；深链接只接受身份派生的 `app-<kebab-id>://restore`，冷/热启动均先验证再恢复；全局快捷键把能力、可空期望绑定和 OS 实际注册分层，动作/chord/固定或可编辑策略完全来自明确需求，Rust action 表与 contract 逐字段一致且 typed dispatcher 不得 wildcard/no-op，空 contract 或全部 `null` 时不得占用系统键位，退出、初始化失败与 E2E 只注销模块实际拥有的 chord。它们都不得凭此承载或批准业务动作。
- `$desktop-prepare-gui-support-surfaces` 维护产品家族共享的固定本地页面/媒体依赖资产；固定赞助档位/价格、作者、品牌联系人、免责声明、支付二维码、更新 banner 与小图是经项目负责人批准的封闭例外。每个媒体文件必须有可审计清单、尺寸、摘要、用途、来源与许可责任。支付二维码必须标记为敏感支付材料，只有 `sponsor_page = enabled` 时随完整 `media/sponsor/*` 进入 GUI bundle；未选择赞助页时不得进入运行时 bundle。携带或展示支付材料不授权订单、权益、账户或自动支付逻辑。updater banner 只有选择更新视觉时才进入 bundle；视频必须本地打包、提供 controls、字幕轨和文字稿并禁止 autoplay 与远程追踪。
- GUI 前端固定直接依赖 `@tabler/icons-react`，并以命名组件提供菜单、操作、状态、空态和图表周边图标；存在适用 Tabler 图标时不得引入其他图标库、手写 SVG、字符或 emoji。图表绘制能力仍按真实数据可视化需求单独选择。两种侧栏都必须把 Logo、当前渲染图标与可见文字沿同一中心线居中且无裁切，保留完整可访问名称；仅详细模式收起后允许 Tooltip 补充被隐藏的可见名称。
- GUI 交互事件必须绑定在实际拥有该动作的语义元素本身。按钮、链接、`Switch`、`Checkbox`、菜单项等控件的动作不得由祖先 `div`、Card、`Table.Tr` 或 `Table.Td` 代理；例如表格中的 `Switch` 只能在用户操作该 `Switch` 时切换，点击所在行或单元格不得切换。设置卡中的 `Switch` 同样只能在用户操作该 `Switch` 时切换，点击所在 Card、标题或说明不得切换。标题与说明从控件视觉结构中拆出时，仍必须用稳定 ID 和 `aria-labelledby`/`aria-describedby` 与 `Switch` 建立程序化关联，不得只保留脱离说明的泛化 `aria-label`。父级确有独立动作时只执行父级自身语义，并在事件传播会冲突时明确隔离；不得从父级直接调用、模拟点击或修改子控件动作。相关回归必须分别点击控件和其周围父级区域，并核对可访问名称与说明关联，证明只有控件触发目标状态变化。
- GUI 中用于恢复页面工作上下文的纯交互状态必须在当前应用进程内跨路由保留，包括活动选项卡、已应用查询/筛选、排序、分页页码与每页数量，以及产品明确需要恢复的视图选择。状态由应用根 Jotai store 中的页面级 atom 持有，atom 在页面模块顶层稳定创建，不能放在会随 route unmount 重建的组件或局部 Provider 中；关闭隐藏、切换页面或单实例唤醒不得清空，真正退出应用后自然回到默认值。此类页面会话状态不得接入 `atomWithStorage`、localStorage、sessionStorage、IndexedDB、Tauri Store、配置文件或数据库，也不得写入 URL 形成跨进程恢复；语言、主题等已明确批准的设备级偏好不属于此限制。TanStack Query 继续拥有异步数据和缓存，core/持久业务状态保持权威，atom 只保存查询控件和值，不镜像结果数据。
- 查询条件、活动数据范围或每页数量发生变化时，页面会话状态必须把页码重置为 1。路由返回后按已保存条件发起查询；只有查询成功、当前页码大于 1 且该页结果为空时，才原子回退到第 1 页并以新 query key 重新查询一次。加载中、取消、超时或错误不得被解释为空数据，第 1 页为空不得形成重试循环。恢复时失效的枚举选择按当前可用项回到安全默认，不得用陈旧界面选择修改 core 结果。
- 所有用户可见版本号必须在展示边界先移除已有 `v`/`V` 前缀，再添加且只添加一个小写 `v`，覆盖窗口标题、侧栏、设置/关于页、更新状态、强更提示、CLI `--version`、发布物名称和更新日志。Cargo、JSON/协议、`.harness/version-state.json` 与 manifest 的机器 `version` 字段继续使用不带展示前缀的原始版本；只有明确命名的 `releaseNotesVersion` 保存带 `v` 展示值。
- 含 GUI 的一次性初始化必须在相关非空单元测试通过后、裁剪初始化能力和创建唯一基线提交前运行 `$desktop-test-gui-initialization-e2e`。新下游在该 E2E 全程保持 pre-Git 状态；即使目标路径位于 Harness 或其他父仓库之下，父 Git 也不构成目标的独立仓库边界，不得提前在目标中执行 `git init` 来满足版本或 E2E 门禁。`version_gate init` 是唯一允许在 pre-Git 目标根写入版本状态的版本命令；其他版本命令继续要求目标精确等于独立 Git 根。只有 E2E 成功且初始化专用能力完成裁剪后，才在目标根创建独立 Git、即时配置 local 提交身份/模板并形成唯一基线提交。检查器读取九项 profile，始终验证 system-locale/updater/window-state 三项 Rust-only 固定基线、dialog 固定 WebView 基线、updater `NotConfigured` 零出站和窗口合法状态重启恢复/无效状态回退；dialog 必须验证固定依赖、唯一且有序注册、前端生产依赖和主窗口精确 `dialog:default`，并拒绝任何额外文件系统授权；按 profile 验证六项条件宿主能力的完整实现或禁用无残留。深链接必须覆盖宿主事件与冷/热分发，但 macOS 自定义 scheme 的 OS 注册在 debug no-bundle 阶段明确为 `Not verified`；中性全局快捷键 contract 必须证明零默认注册和零残留，只有 contract 明确存在安全可观察的非空绑定时才触发实际 chord，并在恢复原配置后确认临时/owned chord 已释放。任一适用场景失败、无法观察或无法恢复都阻断初始化。
- 依赖方向必须清晰：每个适配器直接依赖 core；core 不得直接或通过 workspace 中间 crate 反向到达 CLI、TUI、MCP、GUI、终端 I/O、进程状态或接口框架；适配器彼此不得直接或通过 workspace 中间 crate 依赖、启动或解析输出。
- 程序入口和接口处理器只负责建立运行环境、装配依赖、委托调用和映射终止结果，不得承载业务规则。
- 只有独立发布、独立生命周期、独立权限或由工具链强制要求的依赖边界才能新增 crate/包。新增时记录它为什么不能只是现有模块。
- 直接依赖和受管工具的清单必须表达经过验证的最低兼容范围。新增或主动更新时先选择官方 registry 当前最新非预发布候选，验证 Rust 1.95、peer、平台和实际 API/feature 后，把通过的最高兼容稳定版写成完整下界；清单不得写 `latest`、tag、通配符或无下界范围。已安装工具处于支持范围内时直接复用。
- 受管开发环境恢复不得通过修改项目兼容要求迁就旧宿主：工具缺失时安装，可证明低于最低下界时按当前宿主的受管路线自动升级，范围内稳定版原样复用；Node.js 25.x 按低于下一段允许下界 26.0.0 处理。存在显式上界时，高于上界仍阻断；预发布、无法解析或损坏状态同样失败关闭。只读检查必须保持零写入并明确报告 `upgrade-required`。不得降低门禁、回退依赖或锁文件、注入 shim、改用旧版工具，或寻找替代工具链来适配旧环境。
- 锁文件与兼容要求职责分离：根锁文件固定当前真实解析结果，不是最低兼容事实。每个新增或提高的直接下界都必须在声明的最低 Rust/Node.js/pnpm 工具链完成最低直接版本解析和非空测试；最新候选不兼容时按版本从新到旧选择第一个通过者并记录原因。
- 应用发布版本、协议/模式版本、Rust edition、平台目标三元组、资产摘要和按安全要求钉住的 CI action commit 不属于依赖兼容范围，不得为了形式统一改写成依赖版本范围。
- 自动生成代码、内嵌的第三方代码和人工维护代码必须明确隔离。不得直接修改生成物或内嵌的第三方内容。

### 2.2 默认规则

- 按职责、变化原因和领域概念拆分，不按类型数量、文件对称性或假想未来机械拆分。
- 一个文件围绕一个主要职责组织。总是一起变化且紧密协作的类型和函数优先放在一起。
- 出现独立领域概念、不同变化原因、平台边界或已经难以清楚维护时，应拆分文件或模块。
- Rust 代码超过 400 个物理行时进入建议重构区间；前端代码超过 500 个物理行时进入建议重构区间；其他人工维护文本超过 500 个物理行时进入语义复核区间。日常开发只报告硬超限，不因进入建议区间追加审查或阻断完成；明确发布且当次 `reviewSelection: enabled` 时，才集中检查业务是否高内聚、是否围绕单一职责，以及各部分职责是否相近，任一项不成立时按真实职责重构拆分。
- Rust 模块需要拆分为多个文件时，使用 `<module>/mod.rs` 作为目录入口，并把职责相近的子模块放入该目录，例如 `domain/mod.rs`、`domain/model.rs` 与 `domain/service.rs`；不得同时保留 `<module>.rs` 与同名目录，也不得为规避行数创建空壳转发层。
- 前端模块需要拆分时按功能或页面职责建立高内聚目录，调用方默认从具体组件、hook 或能力文件导入；不得为了对称性强制增加 `index.ts` 桶文件，也不得用 `export *` 隐藏真实来源。
- 不为每个类型或函数单独创建文件，不预建没有真实消费者的多层目录或扩展点。
- 禁止以 `utils`、`common`、`helpers`、`misc` 等兜底名称隐藏职责；共享代码必须使用真实领域或能力名称。
- 避免不能表达边界的 `manager`、`processor`、`handler` 等泛化名称。只有名称确实对应领域角色时才使用。
- 单元测试靠近被测单元；跨模块、CLI 契约和端到端测试放入独立测试目录。
- 只有转发作用且没有独立约束价值的模块应删除或合并。

### 2.3 分层代码行数门禁与通用文本上限

- Rust 代码：400 行及以内不因行数触发专项复核；401 至 800 行是发布审查可见的建议重构候选，但不阻断日常任务；800 行通过，801 行失败并强制拆分。Rust 多文件模块必须使用 `<module>/mod.rs` 目录结构，不能用同级 `<module>.rs` 加同名目录、空壳转发或压缩可读性规避门禁。
- 前端代码：500 行及以内不因行数触发专项复核；501 至 1000 行是发布审查可见的建议重构候选，但不阻断日常任务；1000 行通过，1001 行失败并强制拆分。前端拆分按功能职责保持目录高内聚，不强制 `index.ts` 桶文件。
- 其他人工维护文本：500 行及以内不因行数触发专项复核；501 至 2000 行是发布审查可见的语义候选，但不阻断日常任务；2000 行通过，2001 行失败并强制拆分。文档、非 Rust/前端脚本、配置、Skill、参考、模板和人工维护夹具适用此通用配置。
- 机械分类以文件后缀为准：`.rs` 使用 Rust 配置；`.astro`、`.cjs`、`.css`、`.cts`、`.html`、`.js`、`.jsx`、`.less`、`.mjs`、`.mts`、`.sass`、`.scss`、`.svelte`、`.ts`、`.tsx`、`.vue` 使用前端配置；其余 UTF-8 人工维护文本使用通用配置。
- 行数按与 Python `splitlines()` 等价的物理行计数，不扣除空行、注释或声明；不得通过压缩可读性、合并无关语句或移除必要注释规避门禁。
- 建议重构或语义复核区间不改变日常任务结论。只有当次发布选择启用审查时才基于真实业务边界集中判断；内容不再高内聚、未围绕单一职责或各部分职责不相近时，返回开发循环按职责拆分并修正引用后再发布。
- Rust 超过 800 行、前端超过 1000 行或其他人工维护文本超过 2000 行时必须拆分；计划说明、审查结论、警告或 ADR 都不能把硬超限改判为通过。
- 工具生成且禁止手工编辑的锁文件/生成物，以及原样内嵌且明确隔离的第三方文件，不属于人工维护文件。这是封闭的范围定义，不是任意路径豁免；生成器、模板、补丁和维护说明仍按自身文件类型受分层规模治理。
- 文件规模治理与职责判断同时成立：文件未达到对应建议阈值不证明职责合理，adapter 未超限也不证明其符合 core-first；语义边界仍按本节其余规则审查。

## 3. 中文代码注释

### 3.1 必需覆盖

- 所有人工维护的数据结构、接口、函数和方法都必须有符合业务语义的中文注释，不只覆盖公共 API。
- 数据结构注释说明业务角色、关键状态和必须维持的不变量；语义、单位或约束不直观的字段单独说明。
- 接口或 trait 注释说明职责边界、调用约定、输入输出、错误和副作用。
- 函数与方法注释至少说明其业务目的、主要处理逻辑或关键思路、重要前置条件与边界情况，以及返回结果、错误和副作用。
- 简单取值方法、构造器和薄转发函数也需要中文注释，但可以只用一句准确说明，不得为了满足格式编造复杂逻辑。
- 测试函数注释说明测试场景、关键条件、执行思路和预期结果。

### 3.2 内容质量

- 注释解释业务含义、设计原因、关键不变量和约束，不逐行翻译代码。
- 能通过更清晰的命名或结构表达的内容，先改进代码，再保留仍然必要的业务说明。
- 修改代码时同步更新相关注释。与实现冲突的注释按缺陷处理。
- 复杂算法说明不变量、阶段和取舍，不记录可直接从控制流读出的步骤。
- 非直观兼容处理、外部限制或规避方案必须说明来源、适用条件和删除条件，并链接对应决策、上游问题或官方资料。
- `unsafe` 代码块必须紧邻安全性说明，明确调用者保证、关键不变量及其成立原因。
- 禁止保留被注释掉的旧代码；历史交给版本控制。
- 不使用署名、时间线式修改记录或“由 AI 生成”等元信息注释。
- 中文术语在项目内保持一致；代码标识符继续使用英文。

### 3.3 临时标记与有限豁免

- `TODO`、`FIXME`、`HACK` 不得裸写，必须包含具体原因和完成或删除条件。当前任务不能解决时同步登记 `docs/TECH_DEBT.md`。
- 闭包和极短内联回调可以由所属函数的注释统一说明。
- 自动生成代码和内嵌的第三方代码豁免；宏展开结果由宏定义处统一说明。
- 纯声明型测试数据、静态常量集合和测试夹具可以通过所属模块或数据集说明统一覆盖。
- 豁免只消除逐项注释要求，不允许隐藏安全约束、数据来源或容易误用的业务语义。

### 3.4 机械存在性门禁

- Rust 下游必须保留 `.agents/skills/desktop-implement-change/scripts/check_rust_chinese_comments.py`，但只在本次变化需要、用户明确请求治理检查或发布/渠道硬要求时运行；日常开发和普通构建不为它增加独立步骤。它从 Cargo package 或 virtual workspace 根解析成员，扫描每个 package 的 `build.rs`、`src/**/*.rs` 和 `tests/**/*.rs`，并对人工维护的 `struct`、`enum`、`union`、type alias、trait、具名函数、关联函数、方法和测试函数检查声明自身是否具有紧邻、包含至少一个汉字的 outer doc comment。attribute 可以位于文档注释与声明之间。
- Rust 门禁不要求逐字段、局部变量、闭包或宏展开产物；非直观字段仍按第 3.1 节语义复核。模块注释、父项注释、普通 `//`、纯英文文档注释或字符串中的中文不能替代声明自身的证据。
- 选择 GUI 的下游必须在 GUI 前端保留 TypeScript Compiler AST 中文注释门禁，并同时接入 `pnpm lint` 与项目 validator。机械范围只包括 interface、type alias、enum、class、具名函数、方法/访问器/构造器、直接或后置命名/默认导出的箭头函数组件与 hook，以及测试文件或显式 `vitest` 导入中的 `test`/`it` 测试场景；显而易见的字段、局部变量、循环绑定和普通匿名/极短内联回调不逐项检查，继续由所属结构或函数注释覆盖。
- TypeScript 门禁只精确排除已声明的生成物（例如根相对的 `src/routeTree.gen.ts`）和依赖/产物目录，不得因任意路径片段名为 `build` 等通用词而跳过人工源码。Rust 与 TypeScript 门禁都必须对无有效扫描对象、语法残缺、非法 UTF-8、NUL、源码符号链接、坏 JSON 或运行超时失败关闭，并报告根相对文件、行、列、声明类别和名称。
- “包含至少一个汉字”只证明注释存在且归属正确，不证明内容有业务意义；语义质量仍按第 3.2 节由人工或 Agent 复核。门禁不得自动生成或批量补入“保存变量供当前逻辑使用”“执行处理逻辑”等套话。
- 非 GUI 下游不得因为 TypeScript 门禁要求安装 Node.js 或 pnpm；GUI 下游以 `$desktop-add-gui-adapter` 的前端基线和门禁参考实现为唯一接线来源。

## 4. 文档规则

### 4.1 单一事实来源

- 每项事实只能有一个权威来源。其他文档可以摘要并链接，但不得复制一份会独立演化的规则。
- 文档与代码、配置或测试冲突时，必须调查真实行为并在同一任务中修正，不能默认任一方正确。
- 文档中的命令、路径、配置项、版本和能力必须来自真实仓库，不得虚构或把计划中的能力写成已实现。
- 未确认信息明确标为“待确定”，未运行检查标为“未执行”，未实际验证的平台或行为标为 `Unverified`。
- 产品目标、边界、约束或成功标准变化时更新日期最新的 Product Spec；长期重要、难以逆转的决定和硬规则例外写入当日 ADR。范围清楚的日常实现不为每项需求创建 ADR。Product Spec、ADR、Changelog 或 Work Plan 被自身事件独立触发时，必须记录相关稳定 `change_id` 与 `$desktop-manage-version` 只读计算或提交结果中的 `required_version`；版本变化本身不得触发或补造项目记忆。
- Product Status 只在重要阻断、跨会话交接、真实渠道发布后的记录阶段或用户要求时更新；Work Plan 只在用户明确要求、跨会话交接或发布/高风险协调确有必要时维护；Verification 只保存真实渠道发布后追加的发布/候选验收事实，或独立回顾性人工复核/长期审计证据。
- 普通缺陷修复、不改变可观察行为的纯重构、格式整理、测试补强和内部清理本身不触发 Product Spec、ADR、Product Status、Changelog 或 Verification；结果只在最终回复和测试/CI 中报告，不写“不适用”占位。已有 TECH_DEBT 条目在问题消除后必须恢复真实状态，但不新增修复流水账。
- 构建请求、执行、成功、失败、重试、全量单元测试结果、产物路径/摘要/签名状态，以及候选 E2E、完整验收、状态更新和就绪复核，都不触发 Product Spec、ADR、Changelog、Product Status、Work Plan 或 Verification。候选事实只写入忽略的 `release/` 原子集合、manifest 声明的相邻证据和最终回复，不得复制到 tracked 项目记忆；真实渠道发布成功后，才从已发布的默认分支 closing commit 开始后续受管 feature 生命周期记录 Verification/发布/Product Status 并 finalize 版本周期。独立回顾性人工复核或长期审计也必须使用自己的受管 feature 生命周期，且不得改写或反向批准活动候选。
- 中性初始化必须解析非空中文与英文项目展示名称，且至少一个由用户直接提供；只提供一种语言时由 Agent 自动翻译另一种，并在首次写入前通过完整汇总统一确认。`LICENSE.zh-CN.md` 只写确认后的中文名称，`LICENSE.en.md` 只写确认后的英文名称；README 身份摘要和按 locale 拆分的双语 GUI 资源文件（`zh-CN`/`en-US` 各自的字面文本，不含运行时按界面语言注入的 `applicationName`）必须从同一确认结果派生，不得把机器标识当作缺失译名。
- 根 `release-notes.json` 是下游面向最终用户的发布更新日志事实，不是按日项目记忆，也不替代 `docs/changelog/`。它只在准备首个正式发布时创建，使用 `schemaVersion: 2`；每个功能优化或问题修复条目把非空 `zh-CN` 与 `en-US` 文案绑定为一个翻译对，任一语言缺失、空白或同语言重复都失败关闭。每次发布候选构建前，根据上一次正式发布版本到当前发布源码的真实差异更新当前版本，按最新在前只保留近 5 个版本，每版“功能优化”和“问题修复”各不超过 10 个双语条目。普通缺陷修复即使不触发 Changelog，仍应进入对应发布版本的“问题修复”；失败发布可替换同版本条目，但任何更新日志字节变化都要求重新提交、构建和验收候选。
- 构建不得整理、生成或改写更新日志；它只读校验发布准备已经整理的近五版，再通过发布专用 Tauri 合并配置嵌入相同字节。源文件、合并配置、构建后资源、最终 DMG 内资源、manifest 摘要或 About 运行时任一环不一致都阻断对应候选。
- 上一条“构建”专指发布候选。Windows 原生本地安装试包属于开发制品：使用 `$desktop-build-tauri-local-install`，不读取或生成 `release-notes.json`，不使用发布专用 Tauri 配置，不写 `release/`，不提交，也不解析 E2E/性能选择；不得把普通“继续构建”升级解释为发布授权。
- 同一维护任务若另外改变产品目标/边界/约束/成功标准、引入长期决定或硬规则例外、形成重要阻断/交接、完成真实渠道发布，或另行进入回顾性人工复核/长期审计，才按该独立事件和受管 feature 生命周期更新对应记忆。候选验收阶段只写忽略的 `release/` 原子证据；任务被称为“修复”或“重构”不能绕过门禁。
- 除上述维护排除外，用户或维护者可感知且符合 Changelog 资格的真实行为/流程变化写入当日 Changelog。修改代码或规则时仍须同步受影响的权威设计文档和测试；同步事实不等于创建项目记忆。
- 修改工程规则时，同步检查 `AGENTS.md` 的入口路由、相关 Skill、本文件和适用的机械检查；任务专属细节只在其唯一事实源中校验，不得为了通过检查复制回根入口。
- 删除或替换规则时清理失效入口、链接和摘要，避免旧规则继续进入 Agent 上下文。

### 4.2 固定职责

| 文件 | 唯一职责 |
|---|---|
| `README.md` | 用户入口、用途、真实使用方式和维护状态 |
| `AGENTS.md` | 轻量启动门禁、任务路由、永久地图、硬约束摘要和验证入口；不保存任务专属实施细节 |
| `Version.md` | Harness 模板当前版本、初始版本与发布状态；下游 Rust 项目不继承此事实来源 |
| `.harness/version-state.json` | 仅下游保存当前正式发布周期、首功能提升状态、待发布变化及已消费缺陷 ID；受保护且不是当前版本的第二事实源 |
| `.harness/git-branch-chain.json` | 仅下游保存受管 feature 串行链的远端名、冻结默认/基线/父 OID、活动叶子，以及关闭前节点 OID/可推导清理状态；实际远端 OID 每次实时复读，升级时受保护，不得手改扩大 ref 范围 |
| `release-notes.json` | 仅下游在首次发布准备时创建；以 schema v2 保存供制品与关于页复用的近 5 个正式发布版本中英文用户更新日志，每版两类各最多 10 个翻译对，升级时受保护 |
| `docs/product_spec/README.md` | 产品规格日期规则和索引 |
| `docs/product_spec/YYYYMMDD_product_spec.md` | 当日完整产品目标、范围、约束和成功标准；最新日期文件是当前规格 |
| `docs/AGENT_POLICY.md` | 下游 Agent 能力开关、Session/Worktree 进度标题及其持久执行语义 |
| `docs/ENGINEERING_RULES.md` | 文件、注释、文档、测试、例外和机械检查详细规则 |
| `docs/CLI_CONTRACT.md` | 下游 CLI 的统一机器接口契约 |
| `docs/RUST_CLI_TEMPLATE.md` | 下游 Rust 共享核心与可选适配器初始化基线 |
| `docs/GUI_SUPPORT_SURFACES.md` | 仅在终端下游修改 GUI 固定本地基线、增加支持界面或出站能力时创建；保存差异、资源、所有权与出站实例，Harness 模板不得预创建 |
| `docs/HARNESS_ENGINEERING.md` | 本模板方法论入口、分卷索引与阅读路由 |
| `docs/harness_engineering/*.md` | 方法论主题正文；章节编号跨分卷保持稳定 |
| `docs/project_status/README.md` | 产品状态日期规则和索引 |
| `docs/project_status/YYYYMMDD_product_status.md` | 当日完整阶段、已完成、待确认和下一步；最新日期文件是当前状态 |
| `docs/work_plan/README.md` | 工作计划日期规则和索引 |
| `docs/work_plan/YYYYMMDD_work_plan.md` | 当日完整当前批准变更实施步骤；最新日期文件是当前计划 |
| `docs/adr/README.md` | ADR 规则与日期文件索引 |
| `docs/adr/YYYYMMDD_ADR.md` | 当日产品边界变化、长期重要取舍和规则例外的唯一正文 |
| `docs/VERIFICATION.md` | 验证原则、矩阵、证据分卷索引与写入路由 |
| `docs/verification/*.md` | 真实渠道发布后追加的发布/候选验收证据，以及独立回顾性审计/人工复核正文；历史失败不得改写 |
| `docs/TECH_DEBT.md` | 已知但不在当前任务解决的问题、影响和复核条件 |
| `docs/RELEASE.md` | Harness 时间版本、下游自动语义化版本、版本同步和发布要求；不重复保存 Harness 当前版本事实 |
| `docs/changelog/README.md` | 变更记录规则与日期文件索引 |
| `docs/changelog/YYYYMMDD_CHANGELOG.md` | 当日符合触发规则的用户和维护者可感知变化正文 |

### 4.3 编写、拆分与归档

- `AGENTS.md` 是启动路由器，不是全部规则的镜像。它必须保留启动门禁、按任务渐进读取、始终生效边界、非空 Skills 地图、非空约束地图和最小闭环；命中任务后再完整读取精确 Skill 与事实源，不预先加载同类全部 Skills 或无关项目记忆。
- 根 `AGENTS.md` 的 UTF-8 字节数不得超过 20,000，行数不得超过 120。新增详细初始化、GUI、依赖、构建、发布或验证规则时，正文写入既有唯一事实源或对应 Skill，根入口只增加确有必要的路由或跨任务摘要。
- 标题和首段先给结论，再补充原因、边界和证据。
- 规则必须说明做什么、何时适用、如何验证和允许什么例外，避免无法判定的“适当”“尽量”或“一般来说”。
- 示例必须标明是规范示例、占位示例还是真实验证结果，不得混淆。
- 内部链接使用相对路径；外部技术结论优先链接官方来源，并记录核对日期。
- 日期统一使用 `YYYY-MM-DD`；状态使用仓库规定的固定词汇。
- 文档不保存被删除内容的注释版本；历史交给版本控制、决策记录、验证记录或变更记录。
- 所有文档同样受 500 行建议区间与 2000 行硬上限；日常只强制硬上限，501–2000 行只在当次发布 `reviewSelection: enabled` 时集中复核。该复核优先识别独立主题，职责不再单一或相近时拆为有明确职责的文档，并由原入口保留目录和链接。
- `README.md`、`AGENTS.md`、方法论和项目记忆没有长文档例外；拆分必须保持每项事实的唯一来源、稳定入口、历史失败证据与人工复核事实。
- Product Spec、Product Status 和 Work Plan 只有触发条件满足时才写入；同一天只更新各自唯一日期文件，不得为同一天创建第二份。
- 需要在新自然日写入上述记忆时，先读取前一份，将仍有效事实、未完成事项和当日变化综合重写为完整当前快照，不得只写增量。
- 产品状态日期文件始终重写为当日完整当前状态，不累积流水账。
- 工作计划日期文件只保存当日完整当前计划。结果进入状态、验证、决策和变更记录；旧日期文件随即删除，历史由 Git 版本控制承担，不另建 `docs/archive/work-plans/` 副本。
- `docs/verification/` 与 `docs/verification/human_review.md` 只由真实渠道发布后的受管记录生命周期，或独立回顾性审计/人工复核生命周期写入；候选构建、E2E、完整验收和就绪复核不得为了留证污染固定的默认分支 closing commit。其中已发布版本、真实人工复核和真实验证执行证据保留历史，不得改写为通过或删除失败事实；ADR、Changelog、Work Plan、Product Status、Product Spec 五类按日记忆只保留日期最新的文件，历史由 Git 版本控制承担（见 ADR-20260805-004）。ADR、Changelog 当前最新文件行数超过人工维护文本 500 行建议重构阈值，或项目负责人明确要求整理历史记忆时，Harness 自身可通过 `$desktop-curate-harness-memory` 把不再被任何当前规范引用、已被后续决定完全取代的过期条目原文迁移到同目录 `_history.md` 永久追加保存；该文件只追加不改写，不是 Git 的替代品而是便于人类阅读的当前有效性过滤视图。本机制只治理 Harness 自身，不随下游派生，不适用 Work Plan、Product Status、Product Spec（三类整篇重写为当日快照，不逐条累积）。
- 同一未发布日期文件内，已被后续决定完全替代且会污染当前检索的规范描述可以合并为最新有效表述；合并必须新增 ADR 记录范围、理由、风险和恢复依据，并保留仍有效的决定编号、人工复核事实与失败证据。
- 日期文件名使用紧凑日期 `YYYYMMDD`，正文日期仍使用 `YYYY-MM-DD`。同一天不得创建第二个产品规格、产品状态、工作计划、ADR 或变更记录文件。
- 涉及产品边界、长期规则、硬规则例外或完整候选复核时先读 ADR 索引与最新日期 ADR；日常开发只在受影响区域存在相关决定时读取。只有最新条目显式引用仍有效的旧日期事实时才继续追溯。

## 5. 测试组织

### 5.1 覆盖与边界

- 每个代码行为变更必须由本次开发需要的相关非空单元/回归测试覆盖核心成功路径和最高风险失败路径；纯文档、元数据、格式或不可合理单测的机械变更只做解析或差异完整性所必需的最小替代检查。
- 修复缺陷时必须增加能够复现该缺陷的回归测试；无法测试时记录原因、风险和替代验证。
- 核心业务规则优先使用单元测试；跨模块契约、CLI 和真实边界使用集成或黑盒测试。
- 新增或修改业务行为时，必须先由 core 测试覆盖成功路径和最高风险领域失败，再由适配器测试覆盖输入/协议/视图映射、接口错误和生命周期。仅有适配器测试不能证明业务行为符合 core-first。
- GUI 远程能力至少测试未配置/未同意/撤回时零出站，以及方法、允许地址、字段、重定向、响应大小、超时、取消和秘密脱敏中的最高风险失败。更新另测认证最低支持版本的 SemVer 强更边界、target/channel 与制品签名；统计另测只用 POST 固定字段、无稳定标识符、有界内存队列与撤回清空。后台任务必须有 owner、取消路径与关闭回收，不能用 detached task 或无限重试冒充 fail-open。
- 每个新增公开操作必须能够追溯“适配器操作 → core 用例 API → core 测试”。只修改布局、协议映射或平台机制而不改 core 时，计划或完成输出必须说明其接口/宿主专属性；多适配器暴露同一行为时必须复用同一 core API 和稳定领域错误。
- CLI 的真实二进制契约测试在显式构建、完整验收或用户明确要求时运行，验证标准输出、标准错误、JSON、退出码和非交互行为；日常开发仍只运行本次必要单元/回归测试。
- 测试验证可观察行为，不与无关实现细节耦合；内部重构不应无故导致大量测试重写。
- GUI 交互事件归属测试必须分别操作语义控件和其父级周围区域；表格行内按钮、链接、`Switch` 或 `Checkbox` 至少证明点击行/单元格不会代理子控件动作。
- GUI 页面会话状态测试必须使用同一个应用根 store 证明选项卡、查询/筛选、排序和分页在 route unmount/remount 后保持；使用新 store 证明新进程从默认值开始；并覆盖成功空页从大于 1 的页码回到第 1 页、加载/错误不回退、第 1 页空数据不循环。测试不得以 localStorage、URL 参数或 Query 结果镜像伪造进程内状态恢复。

### 5.2 隔离与可维护性

- 测试默认确定、隔离且可重复，不依赖真实网络、当前时间、随机顺序、用户主目录或机器已有状态。
- 需要文件系统时使用独立临时目录；测试不得读写真实用户数据或共享持久状态。
- 被忽略、跳过或依赖特定环境的测试必须说明原因、启用条件和影响，并在验证报告中列为未执行。
- 测试名称使用英文 `snake_case` 表达场景、条件和预期结果；测试函数按第 3 节添加中文注释。
- 小型测试夹具靠近测试；大型测试夹具放入 `tests/fixtures/`，保持只读并注明来源和用途。
- 测试辅助代码只在重复且语义稳定时抽取，禁止建立不透明的通用测试框架。
- 测试文件、测试支持和人工维护夹具按其文件类型适用第 2.3 节：Rust 测试使用 400/800，前端测试使用 500/1000，其他测试支持与夹具使用 500/2000；硬超限始终阻断，建议区间只在已启用的发布审查中集中判断是否应按场景或领域拆分。

### 5.3 开发、构建与完整验收

- Git 与受管工具环境由 `$desktop-check-development-environment` 唯一管理：新下游在完整表单确认后、首次写入前检查，已初始化下游只在真实命令因受管环境失败后恢复一次。缺失时安装官方当前最新兼容稳定版、可证明低于最低下界时受管升级、范围内稳定版原样复用；Rust/Node.js/npm/pnpm 固定安装到当前用户受管全局根，忽略项目内 Rust 根环境变量，持久去重 PATH，并在当前进程与只读取持久 PATH 的新 shell 绑定路径和版本实际复探。Node 受管 marker 绑定本次已校验归档的精确版本与摘要，复用及最终复探必须精确相等；写入前逐级拒绝安装根、profile/config/env/fish 的 symlink/reparse/非普通对象和错误 marker，稳定链接仅可替换最终目标仍在同一受管根内的链接，Unix profile 通过同目录随机临时文件、原字节快照比较后原子替换。PATH 删除空段、所有相对/cwd 段与重复项，POSIX 解析不展开绝对字面 glob；任何工具变化（包括仅 Git）都必须由新会话精确执行验证。Git 与 Windows MSVC 只有在平台原生受信包管理器明确要求时才允许系统级/管理员边界，必须显式可见且不得静默提权，无既有权限路线即停止。不得写项目内 shim 或让生产运行使用测试覆盖；只读模式零写入。作者身份、模板和本地提交由 `$desktop-configure-git-commits` 唯一管理；已有有效身份保持，缺失字段只在独立目标仓库 local 作用域补齐，绝不写 global/system。
- 已有远端的下游由 `$desktop-manage-git-branch-chain` 唯一管理产品变更分支链：每个新需求/Bug/维护写入从已推送的动态默认 `main`/`master` 或当前登记叶子建立/续用串行 `feature-{ascii-kebab摘要}-{上海日期}`，每个逻辑提交后用显式 refspec 非强制快进并复读远端；`.harness/git-branch-chain.json` 只记录精确 ref/OID。`main`、`master` 与动态远端默认分支禁止日常写入，明确发布的受管原子事务是唯一例外：它只把冻结且满足当次 `reviewSelection` 的严格线性链 fast-forward 到动态默认分支，以默认分支和每个登记 feature ref 的精确旧 OID 作为 lease，在一次 atomic push 中推进默认分支并删除登记远端链，随后切回/快进本地默认分支并以 CAS 删除登记本地链，再从该 clean closing commit 构建；正常流程不创建或使用 `Release` 中转，只对升级前已完全推送且被活动链冻结为基线的 legacy `Release` 提供一次性精确 lease 迁移/删除，不授权远端/凭据配置、无精确 lease 的强推、merge commit、标签/上传、链外删除或扫描 `codex/*`。为维持无 merge 的严格线性历史，一条活动叶子同一时刻最多一个产品写入 Task，且该 Task 禁止 sibling 写入 Worktree；协调方只用 `integrate-task` 在两个 Worktree clean、精确 Task ref/OID、远端叶子未漂移、严格线性后代、Task 范围逐提交未触碰链状态和唯一写入占用等机械门禁通过后快进本地叶子，再由独立 `publish` 推送复读，后续写入才从新远端 OID 开始；只读并行不受影响。无关改动、疑似秘密、hook/提交/push 失败、竞态或 dirty 最终状态均阻断。
- 日常开发统一直接实施，只运行本次变更需要的相关非空单元/回归测试。不得仅因多步骤、多模块、中等风险、可并行或 Agent 偏好自动增加 Work Plan、全仓测试、格式化、代码规范、静态、集成/契约、构建、冒烟、E2E、Verification 或人工复核。
- 非必要代码/架构/职责语义审查统一延迟到明确发布。每次发布在任何写入前解析一次 `reviewSelection: enabled | disabled`：当前请求已明确时复用；安全、隐私、不可逆操作、对外兼容契约或产品/渠道硬要求强制 `enabled` 并记录来源；否则询问一次。同一发布的修复重跑复用原选择，新发布重新询问。`disabled` 只关闭非必要语义审查，绝不能跳过范围/秘密、测试、clean、分支/OID、签名、渠道或真正必需的人类签署等硬门禁。
- 已初始化下游在实施前由 `$desktop-manage-version` 只读分类，且只在变化完成并通过本次相关测试后提交版本：功能、独立缺陷修复和用户批准的 Major 按 `docs/RELEASE.md` 提升；查询、诊断、复现、重复尝试、重构、测试、文档、格式和内部清理不提升。构建只检查 Cargo/状态一致性，不能提升版本或重置周期。
- 产品范围、长期决定、合格 Changelog、重要阻断/交接和用户明确要求仍分别触发对应记录或专用流程；候选验收只写 `release/`，真实渠道发布成功后及独立回顾性审计才通过后续受管 feature 生命周期写 tracked 记录。安全/隐私、数据迁移、破坏性操作、凭据/生产/付费副作用、对外兼容契约、渠道硬要求、签名与发布仍保留解决当前风险必需的授权和门禁。
- 显式发布候选构建在任何测试或编译前解析当前候选的 E2E 选择：当前请求已明确 `enabled`/`disabled` 时直接复用，否则询问一次；持久 `milestone_e2e` 仅是建议默认值。该选择只对当前发布候选有效；本地开发试包不解析该选择。
- 构建必须运行项目全部非空单元测试。Rust 使用 workspace 全成员、全 targets、全 features 的锁定测试；GUI 同时运行完整 Rust workspace 和前端单元测试套件。测试失败或零测试阻断构建。
- GUI 正式发布在任何测试或编译前解析当次 `performanceSelection: enabled | disabled`，该选择不持久化且不得从 E2E 推断。选择 `enabled` 或产品/渠道硬要求时，才在打包前运行 `$desktop-test-gui-release-performance`，以同一干净 HEAD 的 release-profile 探针候选测量启动、交互、整进程树 CPU/RSS、重复操作内存增长和进程回收。性能测量前必须精确定位 window-state 持久文件，快照执行前的字节或“原本不存在”，每次预热/冷启动前恢复同一隔离基线，并在成功、失败、超时或取消的所有路径恢复且复核原状态；无法定位或恢复时性能结论必须失败，不得污染用户窗口偏好或让历史几何影响冷启动样本。指标失败先修复重建，只有用户显式确认才可记录 `performanceStatus: waived` 并保留原失败证据后继续，不能改判通过。选择 `disabled` 且无硬要求时跳过探针，记录 `performanceStatus: Not run`、原因和剩余风险，不生成性能证据或运行时绑定。
- 官方 updater 插件、`UpdateController` 与 `NotConfigured` 零出站状态是所有 GUI 的安装基线，不受产品/发布事实 `updaterEnabled` 控制。GUI 构建每次仍从已批准产品事实解析 `updaterEnabled`；`false` 只表示不生成、不签名且不声明 updater archive/`.sig`，不得移除固定插件或伪造更新配置；`true` 才要求受限 HTTPS endpoints、公钥、channel/target/arch、`bundle.createUpdaterArtifacts: true` 和安全私钥来源，并必须生成与当前 platform/channel/target/arch 一致的官方更新 archive 和 `.sig`、使用应用公钥实际验证后记录相对路径、大小、SHA-256 和结论。安装包允许 unsigned 不代表 updater 可不签名；发布私钥与密码只能来自批准的安全运行时，绝不能进入源码、配置、日志、manifest 或制品。
- 候选构建记录严格遵守第 4.1 节的边界：manifest 和最终回复是发布候选构建的记录出口；本地开发试包只报告实际产物路径和风险。两者都不为构建过程或结果创建、更新任何项目记忆。
- 发布候选 E2E 不混入单元测试、编译、签名或打包命令。选择启用或产品/渠道硬要求时，只在完整最终候选存在后运行；选择禁用时记录 `Not run` 和剩余风险。GUI 初始化专用的 debug/no-bundle E2E 是唯一前候选例外，只能证明脚手架在当前宿主可构建并满足三项固定基线、按 profile 启用的宿主生命周期、运行时 i18n、固定侧栏、精简设置页和菜单可达；需要安装包注册才能证明的深链接平台场景保持 `Not verified`，不能形成候选验收结论。
- 完整候选必须绑定批准场景、源码提交、运行环境和可观察结果；模拟实现、测试替身、占位页面、中性脚手架、开发预览、源码片段或只调用内部函数的结果不具备验收资格。验收失败保存证据并返回开发循环；只有存在用户要求的持久计划时才重开 Todo。
- 构建候选可以标记 `milestoneAcceptance: pending`，但不是 `ready` 或发布物。若验收后的签名、公证或打包改变产物字节、启动器、依赖或运行行为，结果是新候选，必须重新验收。

## 6. 规则例外

- 硬规则例外必须记录到当日 `docs/adr/YYYYMMDD_ADR.md`，包含被偏离的规则、理由、风险、适用范围、替代验证和恢复条件。
- Rust 超过 800 行、前端超过 1000 行、其他人工维护文本超过 2000 行的硬上限不接受 ADR 例外；各自建议重构或语义复核区间能否保留由第 2.2 节的高内聚、职责单一和职责相近性复核决定，不属于规则例外。被机械识别为工具生成或原样内嵌第三方的文件按第 2.3 节范围定义处理，不得把普通维护文件伪装成生成物。
- 默认规则允许在计划或复核记录中说明局部理由，不要求正式决策记录。
- 例外必须尽可能小，不得从单个文件或模块自动扩张为整个项目。
- 临时例外必须有删除条件；没有明确恢复路径的例外按长期架构决定处理。
- 人工批准只授权采用例外，不会把失败或未执行的验证改判为通过。

## 7. 机械检查边界

- 可确定检查的规则可以自动化，但自动化存在不代表日常开发或普通构建必须逐项运行。显式构建固定执行全量非空单元测试和实际构建；格式、lint、静态、文档链接或治理扫描只在本次变化需要、用户明确要求或发布/渠道硬要求时运行。
- Rust core/adapter 的确定依赖事实应自动检查：适配器必须直接依赖 core，core 不得直接或通过 workspace 依赖路径到达适配器或接口框架，适配器不得彼此直接或间接依赖。检查必须覆盖重命名依赖、开发/构建依赖、目标特定依赖和 workspace 可达闭包，且不得把不含接口依赖的合法基础设施 crate 误判为适配器。
- `TODO`、`FIXME`、`HACK` 与建议行数候选只在当次发布 `reviewSelection: enabled` 时由 `python3 -B scripts/validate_harness.py --release-review` 报告；日常 `python3 -B scripts/validate_harness.py` 只保留硬门禁。统一检查器仍必须分别对超过 800、1000、2000 行的文件返回失败、具体路径、配置和实际行数。
- 公共 API 文档缺失可以由语言工具链判定失败。
- 中文声明注释的存在性和归属可以按第 3.4 节机械检查；注释是否有业务意义、模块职责是否合理、函数思路是否充分，必须由人工或 Agent 语义审查，不使用字符数、注释比例或关键词数量伪装成质量检查。
- 业务逻辑是否真正位于 core 同样必须由人工或 Agent 沿真实执行路径语义审查；不得用 adapter 行数、`if`/`match` 数量、文件名或关键词扫描伪装成完整证明。
- 自动检查失败必须返回非零状态并给出具体位置，不得只打印警告后宣称通过。
- 新规则只有在能够稳定判断、重复违规且人工容易遗漏时才升级为自动门禁。
- Harness 校验器只检查模板和随附的资产；真实下游代码仍由其工具链和交付流程验证。
