# 当前工作计划

> 记忆日期：2026-08-04

## 目标与路径

为 Tauri GUI 增加专用发布候选 Skill：macOS 宿主能够构建原生 DMG 与 Windows x64 NSIS 交叉候选，并在条件允许时把 Developer ID 签名、公证和 stapling 作为一个不可拆分阶段执行。

- 当前任务路径：`标准`。原因是产品范围、多个 Skills、环境脚本、发布清单、校验器和项目记忆需要一致修改；本任务只升级 Harness 能力，不生成或发布真实下游候选。
- 范围内：新增 `$build-tauri-release`；增加 macOS xwin 环境安装/复探；明确 DMG/NSIS、签名公证一体化、manifest/收集/验收契约；同步规则、测试、受管清单和维护记录。
- 范围外：真实 Tauri 项目构建、Windows 原生运行、Apple 凭据创建或使用、Homebrew 安装、MSI、Linux GUI 交叉构建、TUI/MCP 发布、冒烟、E2E、tag、上传和正式发布。
- 协作：使用只读 Subagent 做仓库映射与官方资料核对；Harness 源策略中的并行写入仍为 `pending`，且写入区域相互重叠，因此由主 Agent 在当前工作树串行实施。

## Todo 批次 A：范围与专用构建契约

### TODO-A01（done）：确认 Tauri 构建与公证边界

- 预期行为：最新 Product Spec 和 ADR 明确 CLI/Tauri Skill 分工、macOS→Windows NSIS 限制、Windows runtime `Unverified`、Developer ID 签名公证一体化及非目标。
- 影响边界：`docs/product_spec/20260804_product_spec.md`、`docs/adr/20260804_ADR.md` 及索引。
- 依赖与风险：依赖 Tauri/Apple 官方事实；不得把官方“last resort”交叉能力写成 Windows 原生验收。
- 完成验证：日期索引、完整快照、官方命令/限制与旧 ADR 替代关系人工复核。

### TODO-A02（done）：实现 `$build-tauri-release` 与路由

- 预期行为：GUI 候选由专用 Skill 处理；macOS 原生 DMG 使用 pnpm/Tauri，Windows x64 使用 cargo-xwin + NSIS；拒绝 macOS MSI，并禁止只签名未公证的 Developer ID 候选。
- 影响边界：新增 `.agents/skills/build-tauri-release/`；更新 `$build-rust-release`、`$add-gui-adapter`、GUI 基线、README、AGENTS 和相关发布说明。
- 依赖与风险：依赖 TODO-A01；不得改变现有 CLI 原生 workflow 或错误地运行冒烟/E2E。
- 完成验证：Skill 结构/UI YAML、触发与路由片段、命令顺序、MSI/`--skip-stapling`/只签名负向静态检查。

## Todo 批次 B：环境与证据

### TODO-B01（done）：增加 macOS Tauri xwin 环境门禁

- 预期行为：安装模式在既有 Homebrew/Rust/Cargo/pnpm 条件下补齐 LLVM/LLD、NSIS、Windows Rust target 和 `cargo-xwin`，逐项复探并输出稳定状态；check-only 不写入，任一失败阻断。
- 影响边界：`$check-development-environment` 的 Skill、参考、新 POSIX helper、Python 隔离测试、Harness 初始化/必需文件校验。
- 依赖与风险：依赖 TODO-A01；自动安装不得扩张为 Homebrew 安装、静默升级或秘密输出。
- 完成验证：已有环境无写入、缺失环境安装并复探、check-only、非 macOS、缺少 Homebrew、安装失败和不兼容目标回归；Shell 语法检查。

### TODO-B02（done）：扩展安装包清单与收集/验收规则

- 预期行为：Tauri manifest 能区分接口、安装包格式、native/xwin、宿主/目标、runtime 验证、签名作用域和公证证据；收集/验收/发布准备拒绝缺失或矛盾状态，并在 stapling 后计算摘要。
- 影响边界：`docs/RELEASE.md`、`.agents/skills/collect-release-artifacts/`、`prepare-release`、`verify-delivery`、GUI 基线和 `docs/TECH_DEBT.md`。
- 依赖与风险：依赖 TODO-A02；不得破坏 CLI 三件套或把 xwin 候选标成 Windows runtime passed。
- 完成验证：release validator 正负向测试、字段一致性扫描、CLI 既有契约回归。

## Todo 批次 C：Harness 一致性

### TODO-C01（done）：更新校验器、升级受管清单和维护记录

- 预期行为：Harness 识别 21 个 Skills、新 helper/测试为必需文件，升级策略能安全下发新 Skill；README/AGENTS/规格/ADR/Changelog 与真实实现一致。
- 影响边界：`scripts/harness_validation/`、相关 Python 回归、`$upgrade-harness` ownership/policy、`docs/changelog/20260804_CHANGELOG.md`，以及需要长期审计时的 `docs/VERIFICATION.md`。
- 依赖与风险：依赖 A/B 批次；不得修改现有 CLI workflow 受审摘要或声称真实 Tauri/Apple 前向验证。
- 完成验证：相关非空 Python 回归、21 个 Skill quick validation、YAML/Python/Shell 语法、Markdown 链接、`python3 scripts/validate_harness.py` 与 `git diff --check`。

## 标准路径完成边界

全部 Todo 只有在实现及声明检查通过后才标记 `done`。本任务完成只表示 Harness 构建能力升级；真实 macOS DMG、Windows NSIS、Windows 原生行为、Developer ID 签名、公证与 stapling 均保持 `Unverified`，不得声明 `Milestone accepted`、`ready` 或发布就绪。
