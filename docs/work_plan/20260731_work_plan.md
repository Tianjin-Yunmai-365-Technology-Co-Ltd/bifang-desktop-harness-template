# 当前工作计划

> 记忆日期：2026-07-31

## 目标

把 Harness 的实施与验收流程重构为“Todo 批次持续编码/单测 → 完整真实产物验证里程碑 → 失败回编码”，一次持久化下游 Agent 策略，并交付可安全升级下游 Harness 工程部分的 `$upgrade-harness`。

## 已批准范围

- [x] 项目负责人明确只有完整、可运行、符合批准场景的真实里程碑产物可以验收，模拟实现/占位/脚手架不可验收。
- [x] 项目负责人明确工作计划必须拆成 TodoList 和验证里程碑，Todo 未完成时持续编码与单元测试，整批完成后才进入里程碑。
- [x] 项目负责人明确冒烟/E2E 只在验证里程碑考虑，其他流程不得参与。
- [x] 项目负责人明确新增下游 Harness 工程升级 Skill。
- [x] 项目负责人明确下游一次选择并持久化 Superpowers、Worktree/Subagent、里程碑冒烟和里程碑 E2E，后续优先由 Agent 自主判断。
- [x] 使用 `$define-product` 将上述四项写入 2026-07-31 产品规格，并以 ADR-20260731-001 至 004 记录。

## 当前策略解析

- 本仓库是 Harness 源而非已完成初始化的下游；`docs/AGENT_POLICY.md` 的 `pending` 只表示等待未来下游初始化选择，不能被误当成本任务逐次询问门禁。
- 当前工作树含上一批未提交且与本轮重叠的 WEB/GUI/环境/校验器修改，独立写入 Worktree 无法从 `HEAD` 准确继承；本轮在当前工作树串行写入，并使用只读 Subagent 执行安全、校验器与文档一致性审计。
- 用户已明确授权在全部检查通过后提交当前工作区全部变更并推送当前分支；该授权不等于人工里程碑签署、创建标签、发布或制品上传。
- 本轮不是产品运行时最终产物发布；冒烟/E2E 在 Todo 完成前禁止执行，验证里程碑再判断适用性。

## Todo 状态约定

- `pending`：尚未开始；`in_progress`：正在实现或修复；`blocked`：已有明确阻断与证据；`done`：实现及对应非空单元测试/相关检查已通过。

## Todo 批次 A：统一计划、实现与验收状态机

### TODO-A01（done）：更新权威治理规则

- 预期行为：`AGENTS.md`、README、`docs/ENGINEERING_RULES.md`、`docs/HARNESS_ENGINEERING.md`、`docs/RUST_CLI_TEMPLATE.md`、`docs/RELEASE.md` 使用同一 Todo/里程碑/真实产物/失败回编码语义。
- 影响边界：只改维护流程和验收定义，不改具体产品业务、接口技术栈或许可证。
- 完成验证：当前入口不再允许开发期冒烟/E2E，不再把模拟实现/脚手架/部分实现列为可验收候选；本地链接和校验器文本契约通过。

### TODO-A02（done）：更新计划、实现、验收和产物 Skills

- 预期行为：`$plan-change` 生成 TodoList 与里程碑；`$implement-change` 循环到 Todo 完成；`$verify-delivery` 只验收合格候选并在逻辑偏差时回流；构建、收集、发布和适配器 Skills 不在非里程碑流程触发冒烟/E2E。
- 影响边界：不自动发布、打包、签名、上传或代签人工复核。
- 完成验证：相关 Skills 通过 `quick_validate.py`；校验器单元测试覆盖 Todo 门禁、里程碑入口和失败回流。

## Todo 批次 B：持久化下游 Agent 策略

### TODO-B01（done）：定义并验证策略模式定义

- 预期行为：`docs/AGENT_POLICY.md` 以可解析格式记录四项偏好、确认来源/日期和 `reuse_then_infer_then_ask` 语义；Harness 初始化前允许 `pending`，下游基线禁止 `pending`。
- 影响边界：策略只授予可选能力偏好，不授权破坏性、生产、凭据、支付或发布副作用。
- 完成验证：增加合法组合、缺字段、非法值和残留 `pending` 的非空单元测试。

### TODO-B02（done）：初始化一次询问并让后续流程消费策略

- 预期行为：`$instantiate-project`/`$initialize-rust-project` 一次收集四项选择并复用；`$run-parallel-worktrees`、`$implement-change`、`$verify-delivery` 和 E2E 入口按策略与适用性自主判断，不再逐任务重复询问。
- 影响边界：并行不可拆时自动单 Agent；产品/渠道硬要求和新增外部副作用仍保持阻断。
- 完成验证：初始化/裁剪契约保留策略文件；校验器验证消费阶段与优先级。

## Todo 批次 C：交付 `$upgrade-harness`

### TODO-C01（done）：按 `skill-creator` 初始化 Skill

- 预期行为：使用官方 `init_skill.py` 创建 `.agents/skills/upgrade-harness/`，生成匹配的 `agents/openai.yaml`，没有遗留模板 TODO。
- 影响边界：Skill 位于项目 Skills，初始化裁剪后永久保留。
- 完成验证：`quick_validate.py .agents/skills/upgrade-harness` 通过。

### TODO-C02（done）：实现试运行、所有权清单和来源锁闭环

- 预期行为：Skill 明确源/目标根校验、`.harness/upstream-lock.json`、五类所有权（含 `managed-self` 受管子模式）、三方比较、引导审计、冲突阻断、逐文件显式应用、更新器入口最后自更新和更新后验证。
- 影响边界：不覆盖产品代码/记忆/状态/策略/身份/许可证/Git 历史，不恢复 tombstone。
- 完成验证：配套确定性脚本或最小可执行检查对隔离测试夹具产生结构化计划，失败返回非零。

### TODO-C03（done）：覆盖高风险升级场景

- 预期行为：上游单改、下游单改、双方修改、上游删除、本地删除、新增碰撞、`protected`、`tombstone` 和符号链接越界均有确定结果。
- 影响边界：旧下游无基线时所有重叠项默认冲突。
- 完成验证：非空单元测试覆盖核心成功路径和最高风险覆盖/越界失败路径。

### TODO-C04（done）：接入初始化保留、文档与校验器

- 预期行为：下游 Skills 地图保留 `$upgrade-harness`；初始化建立或明确迁移上游锁；LIM-016 按实际证据调整，不虚报真实下游已验证。
- 影响边界：只扩展 Harness 维护能力与初始化保留清单，不把模板校验器、日期记忆或初始化入口回迁下游。
- 完成验证：Harness 校验器检查新 Skill、资源、保留语义和 tombstone 负向契约。

### TODO-C05（done）：关闭升级器安全审计回流项

- 预期行为：来源版本/提交、目标 Git 状态和受审计划被绑定；遇到 `tombstone` 非普通节点、弱化保护清单、未落地 `manual_add`、批量部分应用或更新器自更新顺序错误时均默认拒绝。
- 影响边界：只强化 `$upgrade-harness` 的规划、逐文件应用和锁文件记录；不自动创建/删除项目文件，不修改业务、许可证、身份或 Git 历史。
- 完成验证：拆分后的生产模块均不超过 800 行；27 个隔离 Git 测试夹具覆盖来源伪造、控制文件漂移、符号链接/空目录 `tombstone`、特殊权限、保护清单和逐文件自更新顺序。

## Todo 批次 D：项目记忆与开发检查

### TODO-D01（done）：同步当日状态、验证、技术债和变更记录

- 预期行为：记录真实修改、命令、失败修复、未执行项和剩余风险；不改写历史日期事实。
- 影响边界：只更新当前日期快照和索引；历史验证、历史人工签署和旧版本事实保持原样。
- 完成验证：事实源之间无冲突，当前索引指向 2026-07-31 快照。

### TODO-D02（done）：运行由窄到宽的非空开发检查

- 预期行为：运行新增脚本/策略/校验器单元测试、相关 Python 编译、Skill 结构校验、Shell/PowerShell 静态检查（若受影响）、完整 `python3 scripts/validate_harness.py` 和 `git diff --check`。
- 影响边界：Todo 阶段不运行冒烟或 E2E，不执行发布构建、打包、签名、创建标签、推送或发布。
- 完成验证：所有必需检查通过；普通失败在本任务范围内修复并重跑。

### TODO-D03（done）：关闭校验器审计绕过

- 预期行为：注释、额外工作流步骤/输入、非法 YAML、可变 Action 标签、重复/缺字段 Todo、伪造策略确认元数据和弱化生产所有权均不能通过。
- 影响边界：使用标准库确定性检查和受审工作流全文件摘要，不增加运行时依赖或把冒烟/E2E带入开发检查。
- 完成验证：新增正负向校验器单元测试全部通过，生产工作流固定到官方发布提交 SHA，完整 Harness 校验器通过。

## 验证里程碑 M1：Harness 维护闭环

### 进入条件

- TODO-A01 至 TODO-D03（含 TODO-C05）全部为 `done`。
- 新增/修改的非空单元测试和变更相关检查全部通过。
- 工作区差异已审查，不含模板待办、模拟升级逻辑、绝对本机路径、敏感信息或无关回退。

### 可验收产物

- 一套当前可执行的 Harness 治理闭环：Todo/里程碑规则、可解析项目策略、更新后的项目 Skills、`$upgrade-harness` 及其真实试运行/冲突测试、同步校验器与项目记忆。
- 不以 Markdown 片段或未接入校验器的 Skill 声明作为产物；`$upgrade-harness` 必须能在隔离测试夹具上实际生成预期计划并拒绝危险场景。

### 验收场景

1. 新工作计划能阻止未完成 Todo 进入里程碑，完成后能进入验收。
2. 验收候选若为模拟实现/脚手架/不完整产物会被拒绝，逻辑缺失会重开 Todo 返回实现。
3. 下游四项策略可一次写入并在对应阶段消费；遇到非法或未配置状态时默认拒绝。
4. `$upgrade-harness` 能安全区分可更新、本地保留、冲突、保护和禁回迁文件。
5. 完整 Harness 校验器与全部受影响单元测试通过。

### 冒烟与 E2E 决策

- 已在全部 Todo 为 `done` 后进入 M1 评估。
- 当前里程碑交付物是文档、Skills、校验器和真实维护 CLI，不是 GUI/TUI/MCP/CLI 产品最终产物；里程碑冒烟与 Computer Use E2E 均为 `Not applicable`，没有执行。
- 升级器 27 个隔离 Git 测试夹具均通过真实子进程 CLI 调用 `plan`/`apply`/`record` 验证，不以仅调用内部函数或模拟实现代替维护场景。

### 技术验收结论

- 状态：`Technically accepted / Awaiting human review`。
- TODO-A01 至 TODO-D03 全部为 `done`；新增与受影响非空单元测试、结构校验、语法检查、完整 Harness 校验器和差异检查全部通过。
- 五个批准场景均有直接规则、生产入口和正负向自动证据；首次特殊权限回归夹具未在 macOS 保留 setuid 位，改用可观察的 sticky 位并补充能力判断后重跑 27/27 通过。
- 真实客户下游、Windows/Linux、PowerShell 原生执行和真实 GitHub 运行器仍为 `Unverified`，不影响当前 Harness 维护里程碑的 macOS 技术结论，也不得被表述为跨平台已验证。
- 源码候选提交 `d9e7e81bbb9c77ce7542902010c54f413f836faa` 创建后工作树干净；在该精确提交上重跑 27+34+12+10+4 个测试、20 个 Skills、20 个 Python 文件、完整 Harness 校验器、工作流 YAML、POSIX Shell、差异和干净工作树门禁，全部通过。
- 项目负责人尚未明确签署本里程碑最终复核；用户授权 Git 提交/推送只授权版本控制操作，不自动转化为人工验收。

### 失败回流

- 任一场景失败：保留失败证据，将对应 TODO 重新标记为 `in_progress` 或新增回归 TODO，返回 `$implement-change` 修复并补测试。
- 只有所有重新打开的 Todo 再次为 `done` 后，才重新执行完整 M1。
- 若失败揭示新的产品边界而非现有要求偏差，返回 `$define-product` 请求确认。

## 不包含

- 不实现具体下游产品业务，不创建真实客户项目，不修改商业许可正文。
- 不执行发布构建、跨平台候选、制品收集、Computer Use E2E、打包、签名、创建标签或正式发布；仅在开发检查和里程碑技术证据完成后执行用户已授权的 Git 提交与当前分支推送。
- 不把上一批 WEB/GUI 修改回退或拆出，不重写历史日期文件。

## 风险与回退

- 当前工作树与上一批变更重叠；每个补丁先基于当前内容，不能从 `HEAD` 恢复旧版本。
- 策略 `enabled` 与“当前场景适用”必须分离，避免强制并行或误触发 E2E。
- 更新器的身份渲染和混合所有权不能安全自动化时必须保守冲突，不得用整文件覆盖绕过。
- 如三方差异实现不可靠，里程碑应失败并退回只读审计版本；不得把未完成脚本宣称为可用升级能力。
