# 当前工作计划

> 记忆日期：2026-07-27

## 目标

基于当前未提交的许可证与身份改名变更审计仓库全部 Markdown 文档，修复仍有效事实源中的冲突和模板残留，新增根 `Version.md` 并把 Harness 初始版本统一为 `1.0.0`，完成验证后提交并推送当前分支。

## 范围

- [x] 读取当前 Product Spec、Product Status、工程规则、ADR、Work Plan、验证、技术债、发布规则和相关 Skills，确认需求未改变 Harness 产品边界且版本值已由项目负责人明确决定。
- [x] 枚举并审计全部 Markdown 文档，检查本地链接、当前版本、版本事实来源、项目身份、已替代规则、状态声明、本机绝对路径和日期索引；历史证据中的旧值只在其被误写为当前规则时修改。
- [x] 新增根 `Version.md`，记录当前版本与初始版本 `1.0.0`、发布状态 `Unreleased`；同步 README、AGENTS、当前 Product Spec、工程规则、发布规则、方法论、技术债和相关 Skills。
- [x] 明确 `Version.md` 只属于 Harness，`$instantiate-project` 不复制它；下游 Rust 项目继续只以根 `Cargo.toml` 为版本事实来源。
- [x] 更新 Harness validator，对 `Version.md` 的存在、精确版本、当前摘要一致性、Skill 读取和下游排除契约建立回归门禁。
- [x] 运行文档/Skill/validator 与 bundled asset 的相称验证，修复当前范围内失败，并把执行证据、未执行项和剩余风险写入 `docs/VERIFICATION.md` 与 Product Status。
- [x] 复核完整 diff，确认没有秘密、无关改动、绝对本机路径或虚假完成声明；提交当前授权范围内全部变更并推送 `origin` 当前分支。

## 不包含

- 不修改许可证法律条款，不改变产品功能、接口、依赖、MSRV 或下游 Rust 初始版本 `0.1.0`。
- 不把历史验证、旧日期 Product Spec/ADR/Changelog 中当时真实的 `0.1.0` 改写成当前值。
- 不创建 tag、发布物、签名、Release 或新 remote；用户授权的 Git 外部副作用仅为提交并推送当前分支。
- 不顺带实现真实下游 E2E、非 CLI adapter 或其他已登记技术债。

## 风险与回退

- 文档全量替换可能破坏历史证据；只修复当前有效事实和真实残留，旧日期证据保持原文。
- `Version.md` 可能与发布规则或下游 Cargo 版本形成双重事实；通过职责分离、链接摘要、下游排除和 validator 精确门禁控制。
- 当前工作树含用户既有修改；所有变更基于现状增量实施，提交前用完整 diff 复核，不回退或覆盖未知工作。
- 推送可能遇到远端前进、认证或服务不可用；只执行非强制 push，失败时保留本地 commit 并如实报告，不改写远端历史。

## 完成标准

- 根 `Version.md` 是唯一 Harness 版本事实，值为 `1.0.0` / `Unreleased`；当前摘要和发布规则一致。
- 全部 Markdown 已纳入可重跑审计，链接与当前规则门禁通过，历史事实未被伪造修改。
- Harness validator 能拒绝缺失/错误版本文件、旧 `docs/RELEASE.md` 事实源描述、Skill 未读取版本或实例化复制 Harness 版本文件。
- 相称验证通过或所有失败均已明确记录；其他平台和真实下游不被误报。
- 本地 commit 创建成功，非强制 push 成功，提交后的远端跟踪状态可核对。
