# 当前工作计划

> 记忆日期：2026-07-29

## 目标

在不伪造外部平台、真实下游或人工审批证据的前提下，收口当前仓库内可直接处理的技术债：修复 bundled CLI 的 Rust 1.90 格式门禁，降低 Harness validator 的单文件维护风险，并为并行 Worktree 单元增加可机械回归的写入身份边界；随后复核全部开放技术债，明确仍受外部前置条件阻断的项目，形成可供项目负责人最终审批的完整证据并提交。

## 范围

- [x] 读取当前 Product Spec、Product Status、Agent Policy、工程规则、最新 ADR、验证、发布规则和 `docs/TECH_DEBT.md`，确认用户已批准本任务使用并行 Worktree + Subagent。
- [x] 把本地可独立处理的 LIM-020、LIM-021、LIM-022 拆为三个独占写入单元，并公开分支、文件所有权、依赖、整合顺序和验证边界。
- [x] 按精确 Rust 1.90 rustfmt 修复 bundled core+CLI asset 的既存格式差异，运行格式和非空测试。
- [x] 将 `scripts/validate_harness.py` 按领域拆为可独立测试的模块，保留无第三方依赖的单一入口、现有输出与正负向门禁。
- [x] 强化 Worktree helper 对单元身份、工作目录、分支和工作树根的 fail-closed 检查，并增加故意越界负向测试；准确记录仓库级保护不等于宿主全局 sandbox。
- [x] 按依赖顺序整合三个单元，运行非空单元测试、格式、Python 编译、Skill 结构、Harness validator 与变更相关隔离负向检查。
- [x] 更新 Product Status、Work Plan、ADR、VERIFICATION、TECH_DEBT 和 Changelog；只关闭有当前证据的债项，逐项保留需要真实下游、外部平台、反馈入口或更多采用数据的开放项。
- [x] 创建可追溯的前置、单元和整合提交，保守移除本次三个已整合 Worktree 并删除对应临时分支；旧任务残留因不满足保守整合条件而未强制清理。

## 文件边界与实施顺序

1. **Bundled asset 格式单元**：独占 `.agents/skills/initialize-rust-project/assets/rust-lib-cli/` 内 LIM-020 直接涉及的测试文件；不得修改文档、validator 或 Worktree helper。
2. **Validator 拆分单元**：独占 `scripts/validate_harness.py`、`scripts/test_validate_harness.py` 和新增的 `scripts` 校验模块；保留单一入口与所有现有门禁。
3. **Worktree 边界单元**：独占 `.agents/skills/run-parallel-worktrees/` 的 helper、测试及必要契约说明；不得修改项目记忆或 validator。
4. **串行整合与项目记忆**：主 Agent 依次审查并 cherry-pick 三个提交，解决整合语义，运行总验证后再写入结果和技术债状态。

## 不包含与阻断项

- 不在本地伪造反馈入口、真实下游实例化、Windows/Linux 原生执行、GitHub runner、GUI/WEB/TUI/MCP 真实产物、签名、公证、客户合同或第三方许可证审计证据。
- LIM-004、LIM-005、LIM-007 至 LIM-019 中尚未满足处理条件的项目保持 Open 或原有 Mitigated；本任务报告每项前置条件，但不以文档声明替代执行结果。
- 不执行正式发布、tag、push、签名、上传、产物收集或跨平台候选构建；本次是仓库维护与交付证据复核，不是最终产物发布任务。
- 不代替人类在 `docs/VERIFICATION.md` 签署最终审批；Agent 只能提供 `Partially verified` 或基于既有真实人工记录的结论。
- 不删除脏、缺失、身份不匹配或未整合的 Worktree；分支删除只在用户本次清理授权覆盖且整合证据明确时执行。

## 风险与回退

- Validator 拆分可能改变导入路径、全局可替换测试夹具或错误顺序；保留兼容入口并以现有单元测试、完整 validator 和隔离负向检查约束。
- Worktree 边界只能约束通过 helper 执行的仓库操作，不能替代 Codex 宿主或操作系统级强制；技术债是否关闭取决于负向测试能否证明声明范围内的越界被阻断，超出范围的宿主能力继续公开。
- Rustfmt 变更应只有机械换行；若产生语义差异立即拒绝整合。
- 并行分支若修改越界、缺少测试或包含不相关内容，拒绝 cherry-pick 并在所属 Worktree 修正；不得在主工作树掩盖问题。
- 清理前使用 helper 检查每个 Worktree 干净且分支已成为明确整合引用的祖先；条件不满足时停止清理并报告残留。

## 完成标准

- LIM-020 的精确 Rust 1.90 格式检查和非空 Rust 测试通过。
- Validator 单一入口显著低于 800 行，领域模块边界清晰，Python 编译、现有正负向单元测试和完整 Harness validator 通过。
- Worktree helper 对声明范围内的身份/目录/分支越界 fail closed，成功路径和最高风险越界失败路径均有非空自动测试。
- 所有开放技术债都经事实复核；只有处理条件真正满足且有当前证据的项目标为 Closed，其余项目保留真实状态和下一步。
- 项目记忆与 Changelog 反映实际变更和检查；未运行发布动作、未验证平台、人工审批缺口和剩余风险明确。
- 主分支包含一个可追溯本地提交，工作树干净；本次创建的已整合 Worktree 完成保守清理，无必需 Subagent 留在后台。

## 实际结果

- LIM-020 与 LIM-022 已满足原处理条件并关闭；LIM-021 在 helper 范围内完成机械缓解，但宿主级绕过仍未解决，因此保持 `Mitigated`。
- Validator 单一入口为 68 行，最大领域模块 `initialization.py` 为 682 行；保留 >400 行非阻断职责审查提示，未达到 800 行原则拆分阈值。
- Python 11 个 validator 测试、10 个 Worktree 测试、11 个环境门禁测试、4 个身份改名测试、20 个 Skill 结构校验、完整 Harness validator，以及 Rust 1.90 fmt/check/Clippy/7 个测试全部通过。
- 本次三个 Worktree 均经 guard 复核、整合、保守移除，对应临时分支已删除；上一任务的三个干净 Worktree 仍因分支祖先条件不满足而保留，未强制删除。
- Release build、最终产物冒烟、Computer Use E2E、真实 GitHub 三平台候选、打包、签名、上传、tag、发布与本次人工最终复核均为 `Not run`。
