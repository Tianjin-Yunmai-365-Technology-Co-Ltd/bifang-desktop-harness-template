# 当前工作计划

> 记忆日期：2026-08-05

## 目标与路径

检查并优化 Harness，使后续下游强制采用 core-first：接口无关业务统一由共享 core 实现，CLI/TUI/MCP/GUI 保持薄适配层；系统托盘等接口或平台特性留在所属适配器，但其业务动作仍委托 core。

在上述已完成批次上继续收紧维护治理：普通缺陷修复、纯重构等日常维护不形成 ADR、Changelog 等项目记忆流水账；所有人工或 Agent 维护的文本文件严格不超过 400 个物理行，并以可传播到下游的失败门禁保证。

项目负责人随后确认把当前全部未提交变更纳入里程碑验收，将 Harness 版本确定为 `202608051301`，并授权在验收通过后提交和推送到 `origin/master`。本次只推送源码与验收记录，不创建标签、源码归档或正式发布渠道上传。

- 原实施路径：`标准`。原因是长期产品约束、工程规则、多项 Skills、中性资产、校验器和升级传播需要一致修改；当时不生成真实候选、不改变外部发布契约，也不要求完整里程碑验收。
- 范围内：恢复并强化最新 Product Spec 的 core-first 约束；建立 ADR；统一 core/adapter 所有权、输入校验和平台特性边界；强化规划、实施、验收、初始化和四类 adapter Skills；修正中性 core 的 CLI 语义泄漏；增加确定性依赖/规则回归门禁与可复用下游检查入口。
- 范围外：实现具体产品业务、为未知产品预建抽象或通用 adapter 资产、真实下游生成、跨平台产品候选、签名、公证、产品冒烟/E2E、tag、源码归档、发布渠道上传或正式发布。
- 协作：只读 Subagent 分别审计规则、adapter Skills/资产和 validator；Harness 源的写入型 Worktree 策略仍为 `pending`，所有写入由主 Agent 在当前工作树串行完成。
- 原扩展批次仍采用 `标准` 路径。原因是产品约束、项目记忆规则、方法论文档、多个 Python 模块、下游传播和回归测试需要一致修改；该批次不构建候选、不运行冒烟/E2E、不发布。
- 当前任务路径：`里程碑`。原因是项目负责人明确要求提升 Harness 版本、把当前批次标记为已验收并推送远端；版本、验收和外部推送不能降级为标准路径。

## Todo 批次 A：范围与架构事实

### TODO-A01（done）：固化 core-first 产品约束与长期决定

- 预期行为：最新 Product Spec 与 ADR 明确 core、薄 adapter、输入校验和系统托盘等平台机制的所有权，并说明单 adapter 项目同样适用。
- 影响边界：`docs/product_spec/20260805_product_spec.md`、`docs/adr/20260805_ADR.md` 及索引。
- 依赖与风险：依赖用户明确确认；不得把平台交互状态错误塞入 core，也不得以 core-first 为由预建假想抽象。
- 完成验证：完整快照、日期索引、范围内/外、可观察成功标准和硬规则例外语义人工复核。

## Todo 批次 B：规则与执行入口

### TODO-B01（done）：统一权威规则、方法论与维护入口

- 预期行为：工程规则、Rust 基线、AGENTS、README 与方法论对 core-first 和平台特性边界使用一致、可判定的表述。
- 影响边界：`docs/ENGINEERING_RULES.md`、`docs/RUST_CLI_TEMPLATE.md`、`docs/HARNESS_ENGINEERING.md`、`AGENTS.md`、`README.md`。
- 依赖与风险：依赖 TODO-A01；必须保持单一事实来源，并避免用“尽量”“薄”或代码行数作为不可验证规则。
- 完成验证：规则交叉引用、职责清单、直接/间接依赖方向、平台特性示例、硬规则例外和历史冲突已完成静态复核，并由 Harness 必需片段门禁保护。

### TODO-B02（done）：让产品定义、计划、实施、验收、初始化与四类 adapter 强制执行

- 预期行为：任何业务变更先映射或实现 core 用例和 core 测试；adapter Skills 只允许接口/宿主职责，并输出“adapter 操作 → core API → core 测试”证据。
- 影响边界：`define-product`、`plan-change`、`implement-change`、`verify-delivery`、`initialize-rust-project`、四类 adapter Skills/参考/default prompts 与中性 Rust 资产。
- 依赖与风险：依赖 TODO-B01；协议结构校验不得与领域校验混淆，TUI/GUI 交互状态不得被误判为领域状态。
- 完成验证：逐 Skill 必需语义扫描、默认提示检查、中性 core 与 adapter 职责复核、协议/领域校验边界和平台特性正反例复核均已通过；初始化裁剪后明确保留并运行 core-first 检查器。

## Todo 批次 C：机械门禁与维护记录

### TODO-C01（done）：增加低误报 core-first 校验并完成回归

- 预期行为：Harness 能拒绝规则退化、core 反向依赖 adapter、adapter 未直接依赖 core、adapter 间依赖和 core 引入接口框架；语义判断明确保留给 Agent/人工复核。
- 影响边界：可复用下游检查脚本、`scripts/harness_validation/`、相关 Python 测试、必需文件/升级清单、当日 Changelog 与 Product Spec 成功状态。
- 依赖与风险：依赖 B 批次；不得用 LOC、分支数或关键词扫描误报合法协议/展示代码。
- 完成验证：默认 Harness Python 回归 91 条（含检查器 14 条）、升级器回归 28 条、中性 Rust 7 条非空测试、Cargo fmt/Clippy、真实资产依赖图、完整 Harness validator、Markdown 链接和 `git diff --check` 均通过。

## Todo 批次 D：范围与项目记忆

### TODO-D01（done）：固化维护记忆排除与 400 行产品约束

- 预期行为：最新 Product Spec 与当日 ADR 明确普通缺陷修复、纯重构等维护操作不形成项目记忆流水账，并把人工维护文本最多 400 行定义为不可降级的完成门禁。
- 影响边界：`docs/product_spec/20260805_product_spec.md`、`docs/adr/20260805_ADR.md` 及索引。
- 依赖与风险：依赖用户明确确认；必须保留安全、发布、长期决定等独立触发器，并区分人工维护文件与不可手工拆分的工具生成锁文件。
- 完成验证：完整快照、同日唯一文件、事件触发矩阵、400/401 边界和生成文件范围经过静态复核并由 validator 必需片段保护。

### TODO-D02（done）：统一项目记忆事实来源和执行入口

- 预期行为：规则、索引和 Skills 对普通缺陷修复、纯重构、格式整理、测试补强与内部清理使用同一排除语义；复杂维护可因标准路径建立 Work Plan，其他独立事件仍按原门禁记录。
- 影响边界：`AGENTS.md`、`README.md`、`docs/AGENT_POLICY.md`、`docs/ENGINEERING_RULES.md`、Harness 方法论、六类记忆索引、Release 规则和相关定义/计划/实施/验收/升级/发布 Skills。
- 依赖与风险：依赖 D01；不得删除历史失败/人工复核事实，也不得让“修复”标签绕过安全、里程碑或发布要求。
- 完成验证：必需片段、反向过时表述、快速无记忆与标准可计划边界回归通过；当前未发布 Changelog 只保留新规则下合格内容。

## Todo 批次 E：400 行硬门禁与存量拆分

### TODO-E01（done）：建立可复用文件行数失败门禁

- 预期行为：统一检查器以 Git 清单覆盖已跟踪和未忽略文本，400 行通过、401 行失败，不跟随符号链接，并封闭识别工具生成锁文件；Harness 与下游实施/初始化/验证入口复用同一算法。
- 影响边界：`implement-change` 检查器及测试、Harness `review`/validator、初始化/验收/发布 Skills、升级所有权和传播校验。
- 依赖与风险：依赖 D01；Git/读取错误必须失败，二进制、忽略文件和生成锁文件不得误报，不能复制两套计数算法。
- 完成验证：检查器专属成功/失败/边界/路径安全回归、Harness 入口非零回归和初始化/升级传播回归通过。

### TODO-E02（done）：拆分当前全部超限人工维护文件

- 预期行为：当前工作树所有人工维护文本均不超过 400 行；Python 模块职责与测试发现保持，Verification 历史事实守恒，方法论拆分后的链接和下游 tombstone 仍正确。
- 影响边界：升级器 core/mutation/测试、Harness initialization/governance/workflow/validator 测试、`docs/VERIFICATION.md` 及证据分卷、`docs/HARNESS_ENGINEERING.md` 及方法论分卷；Cargo.lock 保持工具生成原文。
- 依赖与风险：依赖 E01；安全升级器拆分不得弱化路径/TOCTOU 门禁，历史证据不得摘要或丢失，新模块不得逃逸初始化删除或升级保护。
- 完成验证：全 Git 清单零个人工维护超限、Python 语法与默认/升级器回归、Markdown 链接、Verification 标题/签署/失败事实守恒检查通过。

## Todo 批次 F：整体验证与传播

### TODO-F01（done）：完成机械契约、回归和本轮记录

- 预期行为：validator 拒绝记忆排除或 400 行门禁退化，下游初始化与 Harness 升级保留检查器；当前规则变更记录合格且不包含普通修复流水账。
- 影响边界：`scripts/harness_validation/`、测试支持与测试模块、初始化/升级契约、当日 Changelog、Product Spec 成功标准和 Work Plan 状态。
- 依赖与风险：依赖 D/E 全部 Todo；不得把本次标准开发证据写入 Verification 或冒充里程碑验收。
- 完成验证：默认 Python 全回归、升级器回归、中性 Rust 相关回归、完整 Harness validator、Markdown 链接、`git diff --check` 与最终 `>400` 审计全部通过。

## 标准路径完成边界

全部原实施 Todo 已在实现与对应检查通过后标记 `done`。这些结果只构成进入下述验证里程碑的实现准入，不自行产生 `Milestone accepted`、`ready` 或发布就绪结论。

## Todo 批次 G：版本与里程碑验收

### TODO-G01（done）：同步 Harness 时间版本与发布元数据

- 预期行为：根版本事实源和所有当前摘要统一使用项目负责人确认的 `202608051301`，时间版本起始值仍为 `202607301002`，发布状态仍为 `Unreleased`。
- 影响边界：`Version.md`、`README.md`、`docs/RELEASE.md`、最新 Product Spec、当日 Changelog 和本计划。
- 依赖与风险：依赖项目负责人本轮明确确认；不得改写历史日期记录、创建标签、归档或把 `Unreleased` 改为已发布。
- 完成验证：版本残留扫描、日期格式、Changelog 触发范围、Markdown 链接、行数门禁和 Harness validator 通过。

### TODO-G02（done）：在当前工作树完成候选前全量验证

- 预期行为：当前全部代码、Skills、文档拆分和中性 Rust 资产通过发布清单要求的自动门禁，没有未修复失败或超限人工维护文本。
- 影响边界：当前工作树全部已跟踪修改和未忽略的新文件；不合并三个陈旧关联 Worktree。
- 依赖与风险：依赖 G01；大量拆分文件必须保持测试发现、链接、历史证据和升级所有权完整。
- 完成验证：默认 Python 全回归、升级器隔离回归、精确 Rust 1.90.0 的 fmt/Clippy/非空测试、core-first 检查器、完整 Harness validator、行数门禁、Skill/YAML/Shell 解析和 `git diff --check` 通过。

### TODO-G03（in_progress）：创建并验收精确源码候选

- 预期行为：所有范围内源码形成一个可解析候选提交，并在该精确提交上重新运行完整必需检查；Harness 治理闭环、校验器、维护 CLI、Skills 和文档均与批准场景一致。
- 影响边界：本批次源码候选提交及其 Git 身份；Harness 根无具体产品，真实下游和其他操作系统继续标记 `Unverified`。
- 依赖与风险：依赖 G02；工作树检查不能替代精确提交证据，历史 accepted 提交不能替代当前候选。
- 完成验证：候选提交哈希、干净工作树、完整自动检查结果及未执行范围已经取得；产品冒烟和 Computer Use E2E 因候选是 Harness 源码治理资产而记为 `Not applicable`，不记为通过。

## 验证里程碑 M1

- 准入：TODO-G01 至 TODO-G03 全部为 `done`；候选必须是绑定精确 Git 提交的完整 Harness 源树，工作树为空，并包含版本事实、治理脚本、Skills、中性 Rust 资产和全部已触发项目记忆。
- 批准场景：版本 `202608051301` 在当前事实源中一致；完整 Harness validator 拒绝规则、记忆、行数、core-first、升级和工作流退化；升级器真实 CLI 隔离回归通过；中性 Rust 资产在精确 1.90.0 上通过格式、Clippy、非空测试、release 构建和依赖方向检查。
- 最高风险失败路径：文件拆分丢失历史证据或测试发现、升级器路径/TOCTOU 门禁退化、adapter 绕过 core、人工维护文本超过 400 行、版本摘要残留或候选提交不干净时必须拒绝。
- 替代品拒绝：模拟、桩、占位、脚手架开发预览、历史 accepted 提交或仅工作树检查都不能替代当前完整真实候选。
- 运行时适用性：Harness 根不包含具体最终产品；产品冒烟与 Computer Use E2E 为 `Not applicable`，不记为通过。Windows/Linux、真实下游、真实远端构建矩阵、签名、公证、标签、源码归档和正式发布继续为 `Unverified` 或未执行。
- 失败回流：任一必需检查失败时记录 `Milestone rejected`，重开具体 Todo 并返回 `$implement-change` 修复；全部重新达到 `done` 后完整重跑本里程碑。
- 当前结论：`pending`，只有精确提交的全部必需门禁通过且项目负责人复核记录落库后才能变为 `Milestone accepted`。

### TODO-G04（pending）：记录人工复核与里程碑结论

- 预期行为：项目负责人本轮确认、精确候选、自动证据、剩余风险和 `Milestone accepted` 结论写入 Verification 与当日完整 Product Status。
- 影响边界：`docs/VERIFICATION.md` 及当日证据卷、`docs/verification/human_review.md`、`docs/project_status/` 与索引、本计划。
- 依赖与风险：依赖验证里程碑 M1 的全部自动门禁通过；人工批准不能把未运行的平台或正式发布改写为通过。
- 完成验证：证据路由、索引、提交引用、结论词汇、版本摘要、Markdown 链接、行数门禁和完整 Harness validator 通过。

### TODO-G05（pending）：提交验收记录并非强制推送

- 预期行为：验收记录形成独立提交，`origin/master` 以非强制快进方式包含源码候选与验收记录，本地工作树最终为空。
- 影响边界：当前独立 Git 根的 `master` 与既有 `origin/master`；不操作标签、关联 Worktree、签名凭据或发布渠道。
- 依赖与风险：依赖 G04；推送前必须确认远端没有未知前进，失败时不得强推或覆盖。
- 完成验证：`git push origin master` 成功，远端分支哈希等于本地 `HEAD`，`git status --porcelain=v1 --untracked-files=all` 为空。
