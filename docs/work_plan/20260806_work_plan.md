# 2026-08-06 工作计划

## 目标与路径

采用标准路径，把人工维护文本的文件规模治理从“400 行硬上限”调整为两级规则：超过 500 行必须复核高内聚、职责单一和职责相近性，不满足时重构拆分；超过 2000 行无条件触发机械门禁并强制拆分。Rust 模块拆分继续使用 `<module>/mod.rs` 目录结构。

- 当前任务路径：`标准`

## Todo

### TODO-L01（done）：修订权威规则与执行入口

- 预期行为：Product Spec、工程规则、ADR、Agent 摘要及相关 Skills 对 500/2000 两级规则和 Rust `mod.rs` 结构使用一致表述。
- 影响边界：`docs/ENGINEERING_RULES.md`、最新 Product Spec/ADR/Changelog、`AGENTS.md`、`README.md`、相关技术文档与 Skills。
- 风险：旧的 400 行硬门禁残留在现行事实源，导致 Agent 继续错误拆分或错误阻断。
- 验证：现行引用扫描、Markdown 链接与 Harness 治理契约检查。

### TODO-L02（done）：调整机械门禁和回归测试

- 依赖：TODO-L01。
- 预期行为：统一检查器把 501 至 2000 行文件列为必须语义复核的候选但不机械失败；2001 行及以上返回阻断违规，保持 Git 可见范围、生成文件封闭排除、符号链接和失败关闭语义。
- 影响边界：`.agents/skills/implement-change/scripts/check_file_line_limits.py` 及其测试、Harness validator 行数接线和治理断言。
- 风险：软复核候选误触发失败，或 2001 行文件被错误放行。
- 验证：覆盖 500/501/2000/2001 边界、CLI 退出码、Harness 报告转换和默认 Python 回归。

### TODO-L03（done）：完整静态收口

- 依赖：TODO-L01、TODO-L02。
- 预期行为：现行规范不再把 400 行描述为当前约束；全部人工维护文本满足 2000 行硬门禁，500 行以上文件均有显式语义复核结果。
- 影响边界：全仓现行事实源、验证入口和最终差异。
- 风险：历史 Verification 被误写，或旧日期历史事实被当成当前规则修改。
- 验证：`python3 -m unittest discover -s scripts`、`python3 scripts/validate_harness.py`、统一行数检查器、当前引用扫描与 `git diff --check`。历史 Verification 仅保留当时事实，不改写。

## 范围边界

- 本次只调整工程治理阈值与拆分判断，不改变产品使命、接口契约、业务行为或发布状态。
- 不因新阈值主动合并当前已拆分且职责清晰的文件。
- 不运行冒烟、E2E、发布构建或人工里程碑复核。
