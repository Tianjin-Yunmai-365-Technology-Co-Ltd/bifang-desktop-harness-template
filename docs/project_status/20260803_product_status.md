# 项目状态

> 记忆日期：2026-08-03

## 当前阶段

- 产品规格：Approved；2026-08-03 已确认 Rust CLI 构建默认三平台、派发前条件不足时回退当前平台、条件签名和根 `release/` 构建结果目录。
- Harness 版本：根 `Version.md` 仍记录 `202607301002` / `Unreleased`；本轮不进入版本或正式发布准备。
- 当前变更阶段：TODO-A01 至 TODO-D03 已全部完成；构建 Skill 优化 M1 与文档/Skills 中文化 M2 均绑定源码提交 `d2cb44f293fec888d7e66c38345b4df8baa4ebaf` 完成自动复验，并于 2026-08-03 获得项目负责人批准，结论均为 `Milestone accepted`。
- Git 状态：候选已提交到 `master`；项目负责人已授权提交审批记录并推送 `origin/master`。未创建 Worktree、标签、真实远端构建或发布上传。
- 当前计划：见 [`docs/work_plan/20260803_work_plan.md`](../work_plan/20260803_work_plan.md)。

## 本次范围

- 将 `docs/` 与 `.agents/skills/` 中面向读者或 Agent 的英文自然语言翻译为中文；保留命令、代码、路径、文件名、配置键、协议字段、枚举、固定状态值、测试标识符、版本、哈希、第三方产品名与技术缩写。
- `$build-rust-release` 默认选择 Windows/macOS/Linux 原生 Rust CLI 候选矩阵；只有派发前提供方/权限/运行器/结果取回条件不可用才回退当前平台。
- 构建前把规范化的独立 Git 根目录下旧 `release/` 原子隔离并创建全新空目录；签名后的归档/校验和/清单先在同根唯一暂存区形成，再通过目录级原子重命名将完整暂存区提交为 `release/`。最终只保留当前构建身份的候选、哈希、签名状态/证据和清单；允许 `pending`，不凭目录存在判断 `ready`。
- 远端工作流必须检出并复核显式批准的 40 位源码提交，关闭检出凭据持久化；每个平台只上传清单声明的归档/校验和/清单三件套。
- 已有批准的非交互签名钩子、工具和已授权凭据可用时尝试签名并验证；签名条件不具备时记录 `unsigned`，签名尝试失败不降级。
- 当前统一矩阵只覆盖 Rust CLI；TUI/MCP/GUI 跨平台统一产物继续保持限制。

## 当前自动证据

- 发布辅助程序的 7 个用例中当前可运行的 4 个已通过，覆盖原子刷新、根符号链接拒绝、Git 顶层目录拒绝和 PowerShell 高风险静态门禁；3 个 PowerShell 运行用例因当前宿主无 `pwsh` 跳过。
- 发布校验器的 6 个回归已通过，覆盖 `/release/` 精确一次、过宽规则拒绝、默认三平台和矩阵启动后失败边界。
- 工作流校验器的 22 个正负向/真实文件测试已通过，覆盖 Python 3 运行时选择、源码提交固定、原子刷新与候选提交顺序、条件签名/证据、精确传输文件集与 `fail-fast: false`。
- 完整 Harness/发布单元回归 50 个全部通过；Skill Creator 对 20 个 Skills 的结构检查全部通过；五组 Skill 辅助程序测试共 57 项通过、3 项因当前宿主没有 `pwsh` 跳过；精确 Rust 1.90 的格式、锁定依赖检查、Clippy、7 个非空测试和发布构建通过。43 个必需文件与文档链接的完整 Harness 校验器、23 个 Python 文件语法、21 个 YAML 文件解析、全部 POSIX Shell 语法、PowerShell BOM、工作流摘要和最终差异检查均通过。上述本地证据不能推断真实三平台运行器或签名通过。

## 未完成与剩余风险

- Windows PowerShell 辅助程序、真实 GitHub Windows/macOS/Linux 运行器、提供方取回、真实签名钩子/凭据和已签名二进制文件验证均为 `Unverified`。
- 中文化自动检查与项目负责人的 M2 人工翻译质量复核均已完成。
- 当前工作流仍以单一 Rust CLI 二进制文件为产物模型；LIM-015 的 TUI/MCP/GUI 缺口尚未解决。
- 2026-07-31 M1 原先的 `Awaiting human review` 历史事实保留；项目负责人已于 2026-08-03 追加批准，当前结论为 `Milestone accepted`。
- 未运行发布构建、产品冒烟、Computer Use E2E、真实签名、公证、tag、发布或上传；`release/` 当前没有生成产品候选。

## 下一步

1. 按项目负责人授权提交审批记录并推送 `origin/master`，随后确认本地与上游提交一致且工作树干净。
2. 若用户另行要求版本或正式发布，再按 `$prepare-release` 执行；当前批准不自动扩张到标签、远端矩阵、签名、公证或发布。
