# 项目状态

> 记忆日期：2026-08-03

## 当前阶段

- 产品规格：Approved；开发流程已改为快速、标准、里程碑三档风险分级，低风险任务走最短可靠闭环，持久文档、完整验收和人工复核按事件、风险或用户选择启用。
- 当前任务：敏捷流程优化的标准路径 Todo 已全部完成；规则、Skills、初始化选择、机械门禁和回归测试已同步。此前 M1/M2 的验收历史保持在 `docs/VERIFICATION.md`，本轮不改写。
- Harness 版本：根 `Version.md` 仍为 `202607301002` / `Unreleased`；本轮没有准备版本或正式发布。
- Git 状态：本任务变更已完成并通过提交前复核，提交与推送结果以 Git 记录为准；未创建写入型 Worktree、标签、远端构建或发布上传。
- 完成计划：见 [`docs/work_plan/20260803_work_plan.md`](../work_plan/20260803_work_plan.md)。

## 本次完成

- 用户可显式选择任务档位；未选择时 Agent 按范围、可逆性、风险、外部副作用和交付目的自适应。安全、隐私、数据迁移、破坏性操作、生产/付费/凭据、公开契约和发布硬门禁只能升级。
- 快速路径可直接从清楚的请求进入实施，不再强制范围文档、持久计划、全套项目记忆或 `$verify-delivery`；标准路径维护精简 Todo；里程碑路径保留真实候选与完整验收。
- 项目记忆改为事件触发；代码行为变化仍要求相关非空测试，纯文档、元数据和机械变更可以使用相称的链接、解析、静态或差异检查。
- 初始化提供“推荐敏捷预设 / 自定义”二选一；推荐值一次物化到现有四字段，自定义才逐项选择，最终仍拒绝 `pending`，不扩张 `schema_version: 1`。
- 校验器与回归覆盖无计划快速路径、精简标准计划、里程碑不可降级、过早验收/发布就绪拒绝，以及推荐/自定义两条初始化分支。

## 当前自动证据

- `python3 -B -m unittest scripts.test_validate_harness scripts.test_agile_workflow scripts.test_release_validation -v`：64/64 通过。
- 五组 Skill 辅助程序：身份改名 4/4、开发环境 12/12、升级器 27/27、并行 Worktree 10/10、发布目录当前可运行用例 4/4 通过；发布目录另有 3 个 PowerShell 原生用例因当前 macOS 无 `pwsh` 跳过。
- Skill Creator 对 20 个 Skills 的结构检查全部通过；20 个 Skill UI YAML、24 个 Python 文件和 2 个 POSIX Shell 文件的解析/语法检查通过。
- `python3 -B scripts/validate_harness.py` 通过，检查 44 个必需文件、20 个 Skills、本地 Markdown 链接和当前治理门禁；`git diff --check` 通过。8 个文件拆分提示为非阻断审查项。

## 未运行与剩余风险

- 本任务不改运行时资产，也不产生产品候选；Rust 构建、冒烟、Computer Use E2E、真实签名、远端矩阵、公证、tag、发布和上传均未运行。
- 3 个 PowerShell 原生用例、Windows/Linux、真实三平台运行器和签名通路仍为 `Unverified`；本地静态与隔离测试不能替代这些证据。
- 三档自动分类与推荐/自定义初始化交互目前只有规则、校验器和隔离回归，尚无真实下游前向证据；由 LIM-008 跟踪。
- 标准路径任务不要求人工签署，也不产生 `Milestone accepted`、`ready` 或发布结论。

## 下一步

1. 在首个真实下游分别记录推荐/自定义初始化，以及快速、标准、里程碑三条路径的时间、Token 和漏检情况，再按证据收紧或放宽判定条件。
2. 本批次没有剩余必需实施步骤；若维护者准备版本或正式发布，另行进入 `$prepare-release` 和里程碑路径。
