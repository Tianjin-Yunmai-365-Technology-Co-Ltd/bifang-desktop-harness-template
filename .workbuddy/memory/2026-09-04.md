# 2026-09-04 — bifang_manager 实例化

## 完成

- 通过 `$desktop-instantiate-project` 把 Harness 实例化为 `毕方管家` / `Bifang Manager` / `project_id = bifang_manager`。
- `targetRoot = /Users/manonloki/Documents/tmp/bifang_manager`。
- 中文名由用户直接提供；英文名由 Agent 自动翻译并经汇总确认。
- 用户路径输入 `~/Documents/tmp`，helper 解析为 `tmp/bifang_manager`（末级 `tmp` ≠ `project_id`）。
- 目标平台：Windows / macOS / Linux（全部）；接口仅 GUI（不附加 CLI）。
- 推荐预设：`superpowers=disabled` / `parallel_worktree_subagents=enabled` / `milestone_smoke=enabled` / `milestone_e2e=disabled`。
- GUI 九项：`system_tray=enabled` / `system_notification=enabled` / `autostart=enabled` / `about_page=enabled` / `sponsor_page=enabled` / `single_instance=enabled` / `deep_link=enabled`（派生 `app-bifang-manager://restore`）/ `global_shortcut=enabled`（Rust-only，空 `actions = []`）/ `sidebar_mode=compact`（输入 "compac" 按 typo 接受）。
- 负责人：`manonloki`；`confirmed_at = 2026-09-04`。
- 维持文件清单快照：`/Users/manonloki/.workbuddy/tmp/instantiate_snapshot_bifang_manager.json`（197 文件）。
- 源验证：`python3 -B scripts/validate_harness.py` 通过（27 非阻塞警告）。
- 身份改名预览 → 应用：3 文件 `LICENSE.en.md` / `LICENSE.zh-CN.md` / `README.md` 修改；无路径改名；无残留；14 二进制资产跳过。
- `docs/AGENT_POLICY.md` 原子写入：`schema_version=1`、`decision_mode=reuse_then_infer_then_ask`、`confirmed_by=manonloki`、`confirmed_at=2026-09-04`；四项选择均已解析，无 `pending`。
- `AGENTS.md` 重写为轻量启动路由器：9112 字节 / 72 行（在 20000/120 上限内）。
- 残留扫描：README / RELEASE 中 Harness 历史日期 (`202609020957` 等) 与源机器路径已清除；`example_tool` 在 `RUST_CLI_TEMPLATE.md` 等处作为通用占位有意保留；`desktop-prepare-gui-support-surfaces/assets/brand-support/media/*` 等中性 PNG/JPG 由 rename 跳过（身份中立）。
- `desktop-instantiate-project/`、`desktop-test-gui-initialization-e2e/`、`desktop-curate-harness-memory/`、源 `.git/`、`node_modules/`、`Version.md`、`docs/adr/`、`docs/changelog/`、`docs/product_spec/`、`docs/work_plan/`、`docs/VERIFICATION.md`、`docs/verification/`、`docs/HARNESS_ENGINEERING.md`、`docs/harness_engineering/`、`scripts/validate_harness.py`、`scripts/harness_validation/`、所有 `__pycache__`、`.DS_Store` 均已排除。
- `desktop-initialize-rust-project/` 排除整个目录但保留 `assets/gui/macos-dmg-background.png`（中性初始化基线 DMG 背景）。

## 未完成 / 待办

- 仍未进入 `$desktop-initialize-rust-project`：调用 `desktop-check-development-environment` 检查/安装 Git、Rust、Node.js、pnpm；写入根 `Cargo.toml` 与 `[workspace.metadata.agent-first-harness]`；创建 `bifang_manager_core` 与 `bifang_manager_gui`；GUI 初始化九项 profile 写 `docs/GUI_APP_PROFILE.md`；调用 `$desktop-prepare-gui-app-identity` 生成 3 个 Logo 候选（需要用户选择）；调用 `$desktop-add-gui-adapter` + `$desktop-add-gui-{system-locale,updater,window-state,dialog,system-tray,system-notifications,autostart,single-instance,deep-link,global-shortcut}`；运行 `$desktop-test-gui-initialization-e2e`（真实 Tauri debug 构建 + Computer Use）；调用 `$desktop-configure-git-commits` 设置 Git 身份 + 模板；创建唯一本地基线提交；最后裁剪掉 `desktop-instantiate-project`、`desktop-initialize-rust-project`、`desktop-test-gui-initialization-e2e`、`scripts/validate_harness.py`、`docs/HARNESS_ENGINEERING.md`、`docs/harness_engineering/` 等模板专用入口。
- 用户当前未提供 Git 身份 / GitHub 用户名；按 Skill 规则从设备用户名派生同名 `user.name` 与 `<username>@gmail.com`，只写 repo-local。
- GUI 启动器 Logo 选择（3 候选 → 用户挑 1）尚未发生；截图、徽标等需通过 ImageGen + Computer Use 接入。
- 产品业务输入（目的、规则、UI、远程地址、凭据、发布渠道等）仍未提出，初始化完成后切到 `targetRoot` 通过 `$desktop-define-product` 重新发起。

## 关键路径

- Harness 源：`/Users/manonloki/Documents/ym-work/harness/agent-first-harness-template`
- 下游唯一根：`/Users/manonloki/Documents/tmp/bifang_manager`
- 维持文件快照：`/Users/manonloki/.workbuddy/tmp/instantiate_snapshot_bifang_manager.json`
- 复制脚本：`/Users/manonloki/.workbuddy/tmp/instantiate_bifang_manager.py`