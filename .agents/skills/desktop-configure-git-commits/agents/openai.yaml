interface:
  display_name: "配置 Git 提交规范"
  short_description: "仅在实际提交代码前即时安装、检查并使用当前独立仓库的提交消息模板"
  default_prompt: "使用 $desktop-configure-git-commits；仅当我明确要求配置 Git，或下一步将实际创建提交时，才为当前独立仓库安装并检查有意义的提交模板。"
