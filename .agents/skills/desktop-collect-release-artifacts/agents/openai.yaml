interface:
  display_name: "收集发布产物"
  short_description: "安全合并并验证当前构建结果，同时保留里程碑候选状态"
  default_prompt: "使用 $desktop-collect-release-artifacts，在根 release 目录中汇总已完成并核验的当前构建结果，同时保留 pending、rejected 或 accepted 里程碑状态。"
